steps:
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - build
      - -f
      - backend/src/HIS.API/Dockerfile
      - -t
      - ${_IMAGE}
      - backend
images:
  - ${_IMAGE}
options:
  logging: CLOUD_LOGGING_ONLY
timeout: 1200s
