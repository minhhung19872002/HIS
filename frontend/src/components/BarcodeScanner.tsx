/**
 * ⚠️ TRANSITIONAL SHIM (his-fe-convention §4a) — file đã dời về `form/BarcodeScanner/`.
 * Giữ lại CHỈ để các importer v1 (pages/) không phải đổi — v1 đang retire (#204).
 * Code MỚI import từ `components/form/BarcodeScanner`. Xóa shim khi v1 gỡ bỏ.
 */
export { default } from './form/BarcodeScanner';
