/**
 * TRANSITIONAL SHIM (his-fe-convention §4a) — file đã dời về modules/system/components/NotificationBell.
 * Giữ lại CHỈ để các importer v1 (MainLayout) không phải đổi — v1 đang retire (#204).
 * Code MỚI import từ modules/system/components/NotificationBell. Xóa shim khi v1 gỡ bỏ.
 */
export { default } from '../modules/system/components/NotificationBell';
