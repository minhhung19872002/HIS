/**
 * ⚠️ TRANSITIONAL SHIM (his-fe-convention §4a) — file đã dời về `upload/WebcamCapture/`.
 * Giữ lại CHỈ để importer v1 (pages/Reception) không phải đổi — v1 đang retire (#204).
 * Code MỚI import từ `components/upload/WebcamCapture`. Xóa shim khi v1 gỡ bỏ.
 */
export { default } from './upload/WebcamCapture';
