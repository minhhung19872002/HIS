/**
 * TRANSITIONAL SHIM (his-fe-convention §4a) — file đã dời về modules/administration/components/DoctorLicenseBanner.
 * Giữ lại CHỈ để các importer v1 (pages/) không phải đổi — v1 đang retire (#204).
 * Code MỚI import từ modules/administration/components/DoctorLicenseBanner. Xóa shim khi v1 gỡ bỏ.
 */
export { default } from '../modules/administration/components/DoctorLicenseBanner';
