/**
 * ⚠️ TRANSITIONAL SHIM (his-fe-convention §4a) — file đã dời về `form/VoiceDictation/`.
 * Giữ lại CHỈ để các importer v1 (pages/) không phải đổi — v1 đang retire (#204).
 * Code MỚI import từ `components/form/VoiceDictation`. Xóa shim khi v1 gỡ bỏ.
 */
export { default } from './form/VoiceDictation';
