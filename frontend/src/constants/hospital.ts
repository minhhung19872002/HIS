/**
 * Hospital configuration constants — re-export từ app.config.ts (nguồn duy nhất).
 * Giữ file này để backward-compat với import path cũ.
 */
export { HOSPITAL_NAME, HOSPITAL_ADDRESS, HOSPITAL_PHONE } from '../config/app.config';
