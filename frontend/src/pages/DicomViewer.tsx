/**
 * #413: DicomViewer đã chuyển về pages-v2/dicom-viewer/DicomViewer.tsx.
 * File này giữ lại để route v1 (/radiology/viewer) vẫn hoạt động trong thời gian chuyển tiếp.
 * 1 nguồn duy nhất — 2 route (v1 + v2) cùng dùng chung component.
 */
export { default } from '../pages-v2/dicom-viewer/DicomViewer';
