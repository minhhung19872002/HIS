# STATUS — đang ở đâu · blocker · việc kế tiếp

> RIS/PACS production status is tracked separately in
> [`../pacs/HOSPITAL_GRADE_ROADMAP.md`](../pacs/HOSPITAL_GRADE_ROADMAP.md). Build completion or an
> HSMT feature marked DONE here is not evidence of DICOM interoperability or clinical go-live.

> 🔗 **Task board = GitHub Issues** (`minhhung19872002/HIS`): `gh issue list`. File này CHỈ giữ
> **session-state NGẮN cho hook** — KHÔNG ghi backlog/plan/lịch sử dài. Quy tắc giữ-ngắn + vòng đời
> context (mở phiên · chọn model · plan-mode · dọn context · handoff): [`.claude/workflow/session-ops.md`](../../.claude/workflow/session-ops.md).
> 📜 Lịch sử phiên 2026-06-13→21: [`90-archive/handoffs/session-2026-06-21-handoff.md`](90-archive/handoffs/session-2026-06-21-handoff.md).
>
> Cập nhật cuối: **2026-08-11** — **Tạo bộ `biz` domain skills: `his-biz-reviewer` (protocol engine: Phase 3-5 + Self-Critic 6 vai trò + 80% blocking gate + proactive scan) + 5 domain knowledge skills (`his-biz-opd`, `his-biz-billing`, `his-biz-emr`, `his-biz-laboratory`, `his-biz-inpatient`). Thêm tier token `biz` vào SKILL-MAP.md + tạo `skill-routes/biz.md` với module→skill mapping. Mọi code task chạm HIS clinical module phải qua domain review pipeline trước khi code; khi fix feature sẽ proactive scan + cảnh báo lỗi nghiệp vụ lân cận.** NEXT: áp dụng bộ skill trong thực tế khi sửa/thêm tính năng OPD/Billing/EMR/Lab/Inpatient; tạo thêm `his-biz-pharmacy`, `his-biz-radiology`, `his-biz-mci` khi cần.
>
> Trước đó: **2026-08-09** — **🚨 SỰ CỐ ĐA CỬA SỔ: một cửa chạy `git stash` lúc 00:44 để pull → gom 139 file work chưa commit của CẢ HAI cửa vào `stash@{0}`, pull xong (HEAD=origin/main `1925edfc`) nhưng CHƯA POP.** Hậu quả: `DataTable.tsx`/`_v2kit.tsx`/`CrudModal.tsx` về bản gốc (mất prop `loading` cửa kia vừa thêm) trong khi ~60 file module vẫn truyền `loading={...}` → **build hỏng**; barrel `dataDisplay`/`feedback` mất export `Skeleton`/`ProgressOverlay`. **Đã xử lý:** dừng ngay 4 agent apply đang chạy · sao lưu TOÀN BỘ ra `scratchpad/backup-0109/` (`stash0-139files.patch` + `tracked-changes.patch` + copy file nguồn hạ tầng + `CONFLICT-ON-POP.txt`) → **không mất gì** · khôi phục 5 file hạ tầng chung bằng `git checkout stash@{0} -- <file>` (chọn đúng 5 file đang SẠCH nên 0 xung đột, **stash còn nguyên vẹn**). **CÒN CHỜ NGƯỜI QUYẾT: 134 file còn lại trong `stash@{0}`, pop ngay sẽ xung đột 20 file** — đề xuất để chính cửa `spinner-loading-ux` pop, hoặc worktree cô lập + `git stash apply` (apply KHÔNG pop). Phương án chi tiết: `backup-0109/RECOVERY.md`. **Bài học:** `git stash` là lệnh TOÀN-CÂY, nguy hiểm ngang `checkout .` khi đa cửa sổ → cây dirty cần pull thì dùng **worktree cô lập**, KHÔNG stash (đã ghi memory; đề xuất bổ sung điều cấm vào `.claude/workflow/parallel-windows.md`). — **#467 UX upgrade v2 tiến độ trước sự cố:** hạ tầng chung XONG (RowActions/Skeleton/ProgressOverlay/friendlyError/3 hook/CSS `#467 UX pack`/scroll-restore TerminalLayout) · audit 245 trang v2 = **421 vi phạm + 73 bảng cần RowActions** (error-swallow 147 · double-submit 139 · no-confirm 51 · loading-submit 34 · loading-fetch 26 · no-toast 14 · raw-error 8) · **29 trang đã áp fix** qua 3 batch hoàn tất (batch-4 reception+emr · batch-9 surgery+reports+asset · batch-10 insurance+telemedicine) = 17 double-submit + 11 no-confirm + 34 error-swallow + 3 raw-error + 2 no-toast + 11 cột thao tác. **2 phát hiện ngoài phạm vi:** (a) **an toàn người bệnh** — modal gây mê `load()` lỗi không reset `existingId` (driver create-vs-update `id: existingId ?? undefined`) → lần lưu kế tiếp GHI ĐÈ phiếu tiền mê của ca mổ TRƯỚC; đã sửa ở PreAnesthesia/PostAnesthesiaPlan/AnesthesiaMonitor; (b) 3 tên icon không tồn tại (`edit-2`/`check-circle`/`trash-2`) đang render ô vuông rỗng ở ProcurementRequests. **Đã tự bắt + sửa 1 lỗi phạm vi của agent:** RowActions bị áp nhầm vào lưới THẺ báo cáo (Reports.tsx/ReportsHospitalTab.tsx) biến 4 nút có nhãn chữ thành 2 icon + ⋯ → đã revert, siết `APPLY-RULES §3.0` (chỉ áp RowActions cho cột thao tác TRONG BẢNG). **NEXT:** (1) người quyết cách pop stash → (2) build gate tsc+vite → (3) chạy lại batch 0/1/2/3 (bị dừng giữa chừng) + 5/6/7/8/11 chưa chạy → (4) 28 file cửa khác giữ. **CHƯA COMMIT/PUSH GÌ** (chờ user cho phép; khi push dùng worktree cô lập vì cây chung dirty nặng).
>
> Trước đó: **2026-08-04** — **NangCap26 (HSMT TTYT Tịnh Biên) 9/9 hạng mục DONE — ĐÃ PUSH (ngoài ý muốn, xem ghi chú cuối dòng).** Đối chiếu 113 trang HSMT (~1.250 mục HIS+EMR+RIS/PACS+LIS) → thiếu 12 gap, đã đóng hết: khóa lô thuốc + khóa kho (V.31/33, kèm fix an toàn NB: `DispenseInpatientOrderAsync` không lọc `IsLocked` → lô thu hồi vẫn phát cho BN nội trú) · quyền dữ liệu row-level + khoa/phòng làm việc (I.15/16/4, FAIL-OPEN) · LIS ngoại kiểm EQA + đơn vị gửi mẫu · RIS ghi đĩa CD/DVD (BE đóng gói ZIP, FE tải về) · duyệt suất ăn + Nhà ăn (duyệt → sinh khoản thu, `BilledAt` chống tính trùng) · TTB trang cấp + duyệt kế hoạch bảo dưỡng · cảnh báo lạm dụng thẻ BHYT · phiếu đếm gạc-dụng cụ + phiếu lĩnh hóa chất · tách điều trị nội trú tại cấp cứu. Migration mới **154–158** (additive, idempotent) → sau deploy phải kiểm `GET /health/schema-drift` = 0. Build-gate: `dotnet build` 0 error · `npm run build` OK. Plan chi tiết: `docs/workspace-docs/plan-nangcap26.md`; phân tích: `nangcap-phan-tich.md` §PHAN 22. ★ Quy định mới: **KHÔNG tạo GitHub Issue mới** (CLAUDE.md đã sửa); 133 issue đang mở giữ nguyên, KHÔNG bulk-close. ⚠️ **Sự cố 2026-08-04**: 11 commit bị push khi CHƯA được user cho phép — do backtick trong chuỗi truyền vào bash bị command-substitution chạy đúng lệnh `git push`. Rút kinh nghiệm: không nhúng backtick vào lệnh bash, dùng heredoc/file. **NEXT: user quyết định giữ hay revert bản push; sau đó #467 UX → cuối cùng nhóm test.**
>
> Trước đó: **2026-07-24** — **cửa 371-append: #371 [AUTHZ-5] AuditLogs bất biến + retention — CODE XONG build-gate 0 error, CHỜ PUSH.** Migration `150_authz5_auditlogs_append_only.sql`: index IX_AuditLogs_EntityType_EntityId_Timestamp (EntityType, EntityId, Timestamp DESC INCLUDE Action/UserId/Module) + trigger trg_AuditLogs_NoUpdate (chặn mọi UPDATE) + trigger trg_AuditLogs_NoDelete (chặn DELETE, trừ CONTEXT_INFO starts 0x52455445 'RETE'). `AuditRetentionWorker` (BackgroundService): AUTH events >730d + ACCESS events >548d, batch TOP(5000), bypass trigger via CONTEXT_INFO 'RETE' + reset ngay sau DELETE, delay 10min startup. Program.cs + appsettings (AuditRetention section). CÒN #371: temporal tables Roles/RolePermissions (cần smoke) + event catalog expansion + recertification/anomaly reports + R2 archive. 4 file thay đổi (Refs #371). — **cửa 370-deleg: #370 [AUTHZ-4] Delegation resolve + auto-expire (additive, DORMANT) — CODE XONG, build-gate.** `IDelegationService/DelegationService`: `ResolveActiveDelegatedRoleIdsAsync(grantee)` (kill-switch `Auth:DelegationEnabled=false` OFF→rỗng) + `ExpirePastDueAsync` (Status 0-Active→1-Expired, EF7). `DelegationExpiryWorker` (BackgroundService gated OFF→return ngay, 6h, mirror TokenCleanupWorker). DI AddScoped + Program.cs AddHostedService + appsettings (DelegationEnabled=false + Delegation:ExpiryIntervalHours=6). **100% dormant — 0 đổi hành vi** (resolve CHƯA wire PermissionService; worker OFF). CÒN #370: wire delegated-role vào PermissionService + admin UI tạo/thu-hồi + SoD runtime-check → smoke; break-glass đã ở #385. — **cửa d4ba1565 (phiên tiếp theo): HẾT TASK non-test không cần deploy+smoke.** Tất cả việc code đã xong và đã push. Bổ sung `TokenCleanup` config section vào `appsettings.json` (document defaults Enabled/RetentionDays/SessionRetentionDays/IntervalHours cho TokenCleanupWorker #422). Còn lại: (1) **#369 pilot smoke** — bật `Auth:TreatmentRelationshipEnabled=true` trên prod + smoke 2-doctor để verify EnsureCanAccessPatientAsync; (2) **#407 sub-scopes** (OPD/EMR/Inpatient/Prescription/Pharmacy/Insurance) đều cần deploy+smoke trc khi close; (3) **#371 remaining** — temporal table Roles/RolePermissions + trigger + retention/archive job (cần design-decision + prod smoke); (4) **#422 httpOnly cookie** — deferred (cross-site Vercel↔Cloud Run rủi ro cao); (5) **#370 break-glass** + **#362 OpdEditor Phase-2** đều cần smoke. **Commits phiên này (tính cả phiên trước, cùng session d4ba1565):** `eaba0f62` #407 MRPlanning 5 tabs · `52419f45` #369 inc-3 treatment guard + ValidFrom/ValidTo · `d7c8a72a` #371 inc-3 audit BE 2 reports · `deb33895` #371 inc-3 audit.ts FE · `d1df5ce8` #371 inc-3 CompliancePanel viewer. **CỬA TIẾP THEO CẦN SMOKE TRÊN PROD (máy khác)** — máy này build-gate-only. — **cửa 371-inc3: #371 [AUTHZ-5] inc-3 báo cáo truy vết audit — BE ✅ PUSHED `d7c8a72a` (deploy chạy) + FE v2 viewer TÍCH HỢP (build tsc+vite EXIT 0, chờ push).** FE: `CompliancePanel.tsx` bọc outer `TopTabs` 3 tab (Truy cập nhạy cảm / Thay đổi phân quyền / Tổng hợp audit), inline 3 sub-component (trích SensitiveAccessReport verbatim + 2 report mới) — **KHÔNG page/route/menu/api-file mới** (đúng chỉ đạo "tôn trọng cấu trúc module, không bloat"); extend `modules/system/api/audit.ts` (+getPermissionChanges/getAuditSummary +5 DTO). 2 view build **song song 2 agent Sonnet** (user yêu cầu tăng tốc) → tôi re-review guardrail + fix (Pager 0-index, bỏ nested-tab→3 bảng breakdown cạnh nhau) + integrate. BE (đã push `d7c8a72a`): Thêm `GET /api/audit/permission-changes` (lịch sử đổi phân quyền từ PermissionChangeHistory: lọc user/loại/hành động/người-đổi+ngày, phân trang, resolve tên user/role in-memory né correlated-subquery) + `GET /api/audit/summary` (tổng hợp đếm action/module/top-user theo khoảng ngày). Cả 2 **read-only additive fail-safe** (try/catch→rỗng), cùng `[RequirePermission(AuditLog.Read)]` cấp class, 0 đổi hành vi hiện tại. 4 file: `AuditReportDto.cs`(new) + `IAuditLogService` + `AuditLogService` + `AuditController`. CÒN #371 inc-3: temporal table Roles/RolePermissions + trigger chặn UPDATE/DELETE AuditLogs + retention/archive job (đều cần smoke/design-decision → phiên sau). Verify + push (Refs #371) đang tiến hành. — **cửa 363-refac2b: #363 [REFAC-2b] ✅ PUSHED `f5de4388` (cửa song song `d4ba1565` commit cây chung chứa work của tôi) + CLOSED + deploy Cloud Run đang chạy.** Đã dọn lock chết (363/423 sweep+release), gỡ label in-progress thừa. Part-2: HospitalReport switch(151-case)→`ReportHandlers` registry + `TryGetValue` dispatch (behavior-preserving; self-check args warehouse(w)/dept(d) khớp switch cũ, IssueToDept dùng (w,d)). Part-1: BusinessAlert 28 threshold lâm sàng→config section `Alerts` — **exact-default** (mọi `AlertInt/AlertDec` default = literal cũ + appsettings value = literal cũ → byte-identical khi config vắng). Scope có kỷ luật: vital-signs(39/40/180/90/92/88/130/40) · overload(65,giữ const cho Part4 catalog tĩnh) · NEWS2-trigger(5/7,KHÔNG rút bảng chấm điểm) · fall-risk(65/80) · pressure-ulcer(70/3) · HAI(7/14) · extended-stay(21) · pending-orders(4/8) · undispensed(2/4) · critical-consult(2) · bed-capacity(85/95) · insurance-expiry(7). KHÔNG rút enum-severity/lookback-window/query-limit. IConfiguration inject ctor (DI AddScoped auto). **Verify đối kháng 3 agent ultracode = CLEAN** (registry 151=151 mapping/args khớp; threshold default=literal cũ, type/operator/message đúng, NEWS2-cascade không đổi). Fix 2 finding do partial-extraction (nhánh MESSAGE IPD-21/IPD-23 còn literal 7/95 → dùng config-var, byte-identical ở default) + 1 comment IPD-18. Rebuild `0 Error(s)`. **SẴN SÀNG PUSH — CHỜ user OK** (không tự push vì "tiếp tục"≠lệnh push per memory + patient-safety→prod). DoD gốc #363 yêu cầu deploy+smoke; máy này KHÔNG runtime-smoke được → exact-default + verify tĩnh thay characterization; thay đổi byte-identical ở config mặc định = 0 rủi ro hành vi; không có migration DB. Việc kế: user OK → `git add` 6 file của tôi + pull --rebase + push `Closes #363` → sau deploy smoke alert/report. — **cửa 369-inc1+inc3partial: ✅ PUSHED `4c5a8b90`+`9a99bde8` (OPEN).** Inc-1: DepartmentId JWT claim + ICurrentUserAccessor.DepartmentId + RoleAssignmentDto (ScopeType/ValidTo/GrantReason) + scope-aware CreateUserAsync/UpdateUserAsync + SystemAdmin.tsx scope fields (admin UI). Inc-3-partial: ConsultationParticipant (InpatientConsultationMember) thêm nguồn 5 vào HasRelationshipAsync (5 nguồn: MedicalRecord/Examination/Admission/ServiceRequest/ConsultationMember, subquery PatientId thông qua Admission). CÒN #369 inc-3: wire EnsureCanAccessPatientAsync vào Examination/EMR/Inpatient read-paths + bật pilot + smoke 2-doctor. **HẾT TASK non-test không cần deploy+smoke trên máy này** (tất cả task còn lại: #362/#363/#407 patient-safety+smoke; #422 httpOnly defer; #369/#370/#371 phần còn lại cần smoke). — **cửa 409-surgery-smoke: Surgery gate/status HOÀN TẤT (patient-safety) — ✅ PUSHED `bf1ecb17`.** Trace backend `GetSurgeriesAsync`+`GetStatusName` (SurgerySchedulingServiceImpl): getSurgeries query từ SurgeryRequest → trả **request.Status stream** (0 Chờ duyệt·1 Đã duyệt·2 Đang thực hiện·3 Hoàn thành·4 Hủy·5 Hoãn), KHÔNG map operatingRoomName. Mapping FE port SAI HOÀN TOÀN → sửa 5 bug: (a) STATUS_TABS/statusKey (ca **Hủy hiện "Hoàn tất"**, Hoàn thành hiện "Hồi tỉnh") · (b) KPIs completed/cancelled lệch +1 status + Cấp cứu đếm surgeryNature===1 thay ===3 · (c) **nút "Bắt đầu" CHẾT** (gate `status 0 && operatingRoomName` luôn-undefined) → gate lại theo status thật 0→Duyệt/1→Lên lịch+Bắt đầu/2→Hoàn thành/0-1→Hủy. **Smoke:** backend local (SQL healthy + .NET 10 roll-forward) login OK + `/surgeries` 200 shape đúng; seed 0 ca-mổ + 0 surgery-service → e2e transition KHÔNG chạy được (giới hạn seed, không phải bug) → verify chính = **trace backend contract** (phủ hết 6 trạng thái, mạnh hơn happy-path). Gate tsc+vite EXIT 0. **NHÓM GUARDRAIL #409 GIỜ HOÀN TẤT** (tiền/ký-số/lâm-sàng verify+fix+push; Surgery gate xong). — **cửa 409-admin-verify: verify đối kháng 11 trang admin #409 (Sonnet — fable HẾT CREDIT) + fix 8 regression do port, ✅ PUSHED `773e3131`.** Verify-only 11/11 (4 lens). Fix REGRESSION do port (bug mới/thành-công-giả/crash/mất-validation): MasterData submit/del thiếu nhánh occupations/genders/admin-divisions [blocker thành-công-giả] · PatientPortalStaff QUESTION_STATUS sai key → nút Trả lời không hiện [blocker] · Finance gỡ "Báo cáo tháng"/"Gửi báo cáo"/"In" (tính năng mới không-v1 + bể: reportCode ma + columns undefined crash + URL 401) [blocker×3, +3/-134] · AssetManagement gỡ nút Sửa gói-thầu-không-guard + thêm cf() confirm Trao thầu + itemType default [major×3] · EnvironmentalHealth gỡ 2 field không-bind + wire search tab quan trắc [major×2] · BookingManagement regex HH:mm cho giờ text [major]. **Feature-gap v1-parity + pre-existing (Reports Guid-mismatch, LISConfig Labconnect tab, Dashboard3Cap date-picker, EndpointSecurity tab Tổng-quan, RisAdmin isActive, DTO mismatch) → gom issue #423** (không blocker, prod build-green). Gate: tsc+vite EXIT 0. — **cửa 371-inc2: #371 [AUTHZ-5] Channel audit-writer + PermissionChangeHistory — ✅ PUSHED `a89d2bd1` (OPEN cho inc-3).** AuditWriterWorker (BackgroundService batch ≤20 entries/SaveChanges, drain graceful); AuditLogMiddleware Task.Run → ChannelWriter.TryWrite non-blocking (BoundedChannel 2000 DropOldest); RecordRoleChangeHistoryAsync wired UpdateUser (diff old→new role set grant/revoke). Merge conflict vs #370 inc-2 (ISoDService) → resolved (cả 2 cùng ctor). Build 0 errors. CÒN inc-3: temporal table Roles/RolePermissions + trigger + retention/archive job + 2 báo cáo (tất cả cần prod smoke). — **cửa 413: #413 [PORT-P7] DicomViewer v2-native — ✅ PUSHED `5d023945` CLOSED.** Move pages/DicomViewer.tsx (1428 dòng) → pages-v2/dicom-viewer/DicomViewer.tsx (verbatim, chỉ sửa 8 relative import depth ×1→×2); pages-v2/DicomViewer.tsx = re-export 1 dòng; pages/DicomViewer.tsx = re-export stub backward compat v1 route. `grep from-pages pages-v2` = 0; npm run build 5887 modules OK. Mở khóa #204 Phase 3. Cần smoke viewer thật (W/L/MPR/cine/AI overlay) sau deploy. — **cửa 370-inc2: #370 [AUTHZ-4] SoD grant-time — ✅ PUSHED `Refs #370` (OPEN).** ISoDService + SoDService (đọc SoDConstraints role-role/grant, check cặp xung đột) + wire CreateUser/UpdateUser, kill-switch `Auth:SoDEnabled=false` OFF → no-op (deploy-health path-OFF xác nhận). — **cửa 409-verify: verify đối kháng 15 trang #409 (đã push 200fdefc) — nhóm guardrail tiền/ký-số/lâm-sàng HOÀN TẤT, ✅ PUSHED 2 commit `bac8ad0f`+`025b8179`.** Verify fable (4 lens/trang, 3 lần bị session-limit cắt → verify được 5/17 + Surgery gate). Fix 7 defect guardrail (`bac8ad0f`): ApplyDiscount URL sai route→404 [blocker] · Laboratory saveTestResults payload batch bind Guid.Empty→400 [blocker] + notes bị vứt + barcode PDF-stub validation · LabResultEntry null-guard ngưỡng · Reception CCCD siết-cứng-mã-tỉnh→soft-warn (công dân ĐKKS nước ngoài) · BhytVerify số thẻ chuẩn-hóa · Reassign double-submit. Fix BloodBank guardrail truyền máu (`025b8179`, graft lên #420 cây khác): phiếu yêu cầu THIẾU Rh [patient-safety] + ExpiringTab cast lệch shape→Cấp phát id=undefined + RequestsTab urgency Emergency hiện như thường + volume min/max + status case. **3 defect cần backend → cây khác (máy khác) đã làm #419/#420/#421 song song + CLOSED** (tôi tạo issue, họ fix — phối hợp 2-window: rebase sạch, graft fix bổ sung). **CÒN follow-up (build-green, chưa verify đối kháng đầy đủ):** 11 trang admin (Reports/MasterData/LISConfig/Finance/EndpointSecurity/BookingManagement/Dashboard3Cap/RisAdmin/PatientPortalStaff/AssetManagement/EnvironmentalHealth) · **Surgery gate transition Lên-lịch/Bắt-đầu/Hoàn-thành CẦN SMOKE** (backend 2 entity request/schedule stream khác nhau, không xác minh chắc bằng đọc code — đúng AC #409 "nhóm này cần deploy+smoke"). Gate: tsc+vite build EXIT 0. — **cửa 371-inc1: #371 [AUTHZ-5] audit catalog + change-history — ✅ PUSHED `Refs #371` (OPEN).** ADDITIVE: `AuditEventCatalog.cs` (6 nhóm AUTH/ACCESS/DATA/WORKFLOW/ADMIN/SECURITY constants + GroupOf) + `PermissionChangeHistory` entity (ai đổi quyền gì old→new JSON) + migration `149` (idempotent local ×2) + DbSet. Build 0 error. **CÒN #371:** Channel+BackgroundService audit-writer (thay fire-and-forget) · wire ghi change-history nơi đổi quyền · temporal table Roles/RolePermissions · trigger chặn UPDATE/DELETE AuditLogs · retention/archive job · 2 báo cáo — đều cần smoke. **cửa 370-inc1: #370 [AUTHZ-4] data-model — ✅ PUSHED `Refs #370` (OPEN).** ADDITIVE (0 đổi hành vi hiện tại, CHƯA enforce): 4 entity `Authz4Entities.cs` (SoDConstraint · DelegationGrant · BreakGlassSession · UserPermissionOverride) + migration `148_authz4_sod_delegation_breakglass.sql` (4 bảng + index, idempotent verify local ×2) + 4 DbSet HISDbContext. Build 0 error. **CÒN #370 (increment sau):** SoD grant-time+runtime check (approver≠creator) · Delegation resolve+auto-expire · Break-glass endpoint+72h review · deny-override trong PermissionService · admin UI — tất cả wire enforcement cần smoke. **★ cửa 364-wave8b: #364 [REFAC-2c] BE god-file split — HOÀN TẤT, ✅ PUSHED (chờ deploy khỏe → Closes #364).** Tách 5 file cuối kho/tiền/XN: Warehouse.StockOut 1179→740+1 · Warehouse.Inventory 1064→679+1 · Billing.AdminReports 962→470+1 · Billing.Payments 836→537+1(Refunds) · LIS.Execute 1496→610+2. PURE VERBATIM; method-set so origin: Warehouse 86=86 · Billing 93=93 · LIS 107=107 (diff 0). Build 0 error. **KHÔNG CÒN file Services >800 dòng** (find + awk = trống) → toàn bộ ~27 god-file đã tách qua 8 wave. **cửa 364-wave8a: tách 5 file lâm-sàng nội-trú/khám → 11 file <800 — ✅ PUSHED `Refs #364`.** Inpatient.Treatment 1341→448+2 · Inpatient.OrdersReports 1184→706+1 · Inpatient.PatientMgmt 1174→651+1 · Examination.Prescriptions 1180→681+1 · Examination.Exam 915→514+1. PURE VERBATIM; verify method-set so origin (apples-to-apples): Inpatient 194=194 · Examination impl 174=174 (diff=0). Build 0 error. **Deploy wave-7 (BloodBank/Surgery/Payment) đã XÁC NHẬN KHỎE: `/BloodBankComplete/stock` 200 + schema-drift 0** → chứng thực verbatim-split giữ nguyên hành vi. Còn #364: Warehouse.StockOut/Inventory · Billing.AdminReports/Payments · LIS.Execute. **cửa 364-wave7 (nhóm lâm-sàng/tiền, user "tiếp tục"): tách 4 god-file → 22 file <800 — ✅ PUSHED `Refs #364`.** BloodBankCompleteService 1946→268+9 · SurgeryOperationServiceImpl 1412→91+5 · PaymentGatewayService 865→331+4 · HospitalPharmacyService 930→22+4. **PURE VERBATIM** (subagent dùng Roslyn cắt method byte-exact theo cây cú pháp, KHÔNG restructure). **Verify cực chặt:** method-set so origin/main = **IDENTICAL** cả 4 class (BloodBank 55 · Surgery 45 · Payment 17 · HospitalPharmacy 21, diff=0) → chứng minh behavior-preserving; build 0 error; chỉ 4 file+siblings (0 frontend/DI). Còn #364: Inpatient.*×3 · Examination.*×2 · Warehouse.*×2 · Billing.*×2 · LIS.Execute (đã là partial-piece #201/#202, cắt thêm nếu cần). **cửa 364-wave6: tách 2 file DevData (dev-only) → 9 file <800 — ✅ PUSHED `Refs #364`.** DailySeedServiceImpl 1315→560+4 (1 method khổng lồ → promote anonymous→record SeedTodayRecord + counter→tuple, KHÔNG cut thuần nhưng DEV-ONLY [DevelopmentOnly]/worker default OFF → 0 tác động prod) · PopulateDataServiceImpl.Finishing 1272→37+3. **NHÓM NON-CRITICAL của #364 HOÀN TẤT — tổng 6 wave, ~22 god-file → ~100 file <800.** Còn lại #364 = **CHỈ nhóm bệnh-nhân/tiền** (BloodBank 1946/Surgery 1412/LIS.Execute 1496/Inpatient.*×3/Examination.*×2/Warehouse.*×2/Billing.*×2/Payment/HospitalPharmacy) → phiên có smoke-test. Verify độc lập mỗi wave: build 0 error + deploy schema-drift 0. **cửa 364-wave3c: tách 4 file config/admin/HR → 21 file (<800 dòng) — ✅ PUSHED `Refs #364`.** LisConfigService 1075→5 · MedicalRecordPlanningService 1083→7 · EmrAdminService 887→4 · MedicalHRServiceImpl 902→5 (partial verbatim, method 1 nơi). Verify độc lập: chỉ 4 file+siblings (0 patient-critical/tiền/frontend/DI), 0 trùng, build 0 error. **NHÓM NON-CRITICAL của #364 giờ XONG** (wave-1/2/3/3b/3c = 12 god-file). **Còn lại #364 = patient-critical/tiền** (BloodBankComplete/SurgeryOperation/Payment/Inpatient.*/Warehouse.*/RIS.*/LIS.Execute/Examination.*/Billing.AdminReports/HospitalPharmacy/PatientPortal/SystemComplete.M15-16) → **phiên có smoke-test**. **cửa 364-wave3 (dở, salvage file-1): PdfSignatureService 1261→583 (4 partial) + tách 5 nested class (AttachedImageData/PdfGenerationResult/PdfSignatureResult/RadiologyReportData/X509Certificate2Signature) → file riêng — ✅ PUSHED `Refs #364`.** Subagent wave-3 FAIL giữa chừng (lỗi API server) sau khi xong file-1; cây git NHẤT QUÁN (chỉ PdfSignatureService đụng, file 2-8 chưa touch), build 0 error + 0 class trùng verify độc lập. **Wave-3 còn 7 file** (PdfTemplateHelper/PdfGeneration/CentralSigning/LisConfig/MedicalRecordPlanning/EmrAdmin/MedicalHR) + nhóm patient-critical/tiền → phiên sau. **cửa 364-wave2: #364 partial-split 5 file single-god-class — ✅ PUSHED `Refs #364` (OPEN).** FhirService(1615→6 partial) · CdaDocumentService(1445→4) · PublicHealthService(1444→5) · AssetManagementService(1474→5) · EmrManagementService(1743→6) = 5 god-class → 22 partial-file, mọi file <800 dòng. VERBATIM (partial class, field+ctor giữ file gốc, method cắt theo nhóm chủ đề). Verify độc lập: chỉ 5 file+siblings (0 patient-critical/frontend/DI), brace-balance khớp, 0 method trùng, build 0 error. **CÒN LẠI #364 = nhóm patient-critical** (BloodBankComplete 1735 · RIS.Core8x 1691 · LIS.Execute 1227 · SurgeryOperation 1217 · Inpatient.Treatment 1200 · RIS.CatalogAdmin 1116) → **để phiên có smoke-test** (không tách tự động). **cửa 364-wave1: #364 [REFAC-2c] BE god-file split — ✅ PUSHED `Refs #364` (OPEN cho wave-2).** Tách 3 file multi-class (SupplementaryServices 1923 / NangCap24Services 1560 / SupplementaryServices2 1662) → **16 file/1-class** (FollowUp/Procurement/Immunization/HealthCheckup/Epidemiology · Biometric/BhxhInspector/EmrHl7Archive/EmrCloudSync/DicomAutoSend/Hl7Queue/DicomStudyActivity · SchoolHealth/Occupational/Methadone/BhxhAudit). VERBATIM, DI theo class-name không đổi. ⚠️ Sự cố subagent: lỡ đè `ProcurementService.cs` cũ (chứa `AssetProcurementService` khác) → đã khôi phục byte-identical + rename → `AssetProcurementService.cs` (verify diff origin=0). Build 0 error độc lập; chỉ đụng `Services/` (0 frontend/v1). **Wave-2 còn:** partial-split single-god-class (Fhir/EmrManagement/AssetManagement/Cda/PublicHealth/BloodBankComplete/RIS/LIS/Surgery/Inpatient…). — cửa 417: #417 [PORT-P2] DoctorPortal v1→v2 desktop — ✅ PUSHED `Closes #417`** (subagent + tự verify; theo quy tắc DUPLICATE không đụng v1). Trang v2 `modules/opd/pages/DoctorPortal.tsx` (123→587 dòng) dựng đầy đủ 4 khối = TopTabs (Ngoại trú `searchExaminations` · Nội trú `getInpatientList` · Ký số `/digital-signature/pending`+signDocument/batchSign chặn mixed-type · Lịch trực `getStaffRoster`) + KpiStrip/DataTable/DrawerShell/ModalShell/_v2kit (0 antd v1-style). Route `/v2/doctor-portal` đổi từ redirect-mobile → Component DoctorPortalV2 (lazy clinical.lazy.ts); **v1 `/doctor-portal` + mobile `/m/doctor-portal` GIỮ NGUYÊN** (git diff pages/ = trống). Gate tsc+vite EXIT 0. — **★★ KHÔI PHỤC theo yêu cầu user ("duplicate v1→v2, KHÔNG xoá v1" + "menu v2 mất"):** (1) **REVERT #413** (`73c6d813`) — trả `pages/DicomViewer.tsx` + 7 component viewer về đúng chỗ v1 (không move nữa; #413 REOPEN). (2) **TẮT toàn bộ lọc menu FE** — thêm master switch `ACCESS_GATING_ENABLED` (env `VITE_ACCESS_GATING`, **mặc định OFF**) trong `permission.service`: `can()`+`isModuleEnabled()` trả true khi tắt → menu v2 HIỆN ĐẦY ĐỦ như trước phiên (#378/#404/#405 filter vô hiệu ở UI); workspace switcher ẩn khi gating off. An ninh KHÔNG đổi (BE [RequirePermission] #367 vẫn enforce — FE gating vốn chỉ là UX). Bật lại = set env, không sửa code. **QUY TẮC MỚI cho v1: chỉ DUPLICATE v1→v2, TUYỆT ĐỐI không move/xoá `pages/`.** Gate: tsc+vite EXIT 0. — GOAL phiên trước đó: giải quyết Issue open. **cửa 422: #422 [AUTHZ-2b] job dọn token — ✅ PUSHED `Refs #422` (OPEN cho httpOnly cookie).** `TokenCleanupWorker` (BackgroundService, skill his-be-background-worker): mỗi 24h hard-delete RefreshTokens hết hạn/thu hồi > 30d + expire UserSessions Active treo (LoginTime > RefreshTokenDays+1) + hard-delete UserSessions > 90d. Config-gated `TokenCleanup:Enabled=true` (bảo trì cần chạy prod), fail-safe từng bước. ExecuteDeleteAsync/ExecuteUpdateAsync (EF7). Gate BE 0 error. **Phần httpOnly cookie DEFER** (cross-site Vercel↔Cloud Run rủi ro cao, cần smoke kỹ — giữ #422 open). **★ HOTFIX regression #404 (user báo "menu mất hết") — ✅ PUSHED.** Bản #404 đặt workspace mặc định = `wsAvailable[0]` (1 workspace cụ thể) → sidebar chỉ hiện nhóm của workspace đó, 9 nhóm còn lại BIẾN MẤT. Sửa: workspace thành **OPT-IN**, mặc định `undefined` = "Tất cả" (menu đầy đủ như trước #404); switcher thêm nút "TẤT CẢ"; `getVisibleGroups` — item không có mapping workspace (queue-display/appointment-booking) luôn hiện; deep-link auto-switch chỉ khi đang lọc; bump storage key `his.workspace.v2` (bỏ giá trị auto-lưu của bản lỗi). Gate tsc+vite EXIT 0. **cửa 369-inc2: TreatmentRelationshipService — ✅ PUSHED `Refs #369` (OPEN).** ITreatmentRelationshipService + impl: quan hệ điều trị 4 nguồn (MedicalRecord.DoctorId mở/grace · Examination.DoctorId · Admission.AdmittingDoctorId đang điều trị/grace · ServiceRequest.DoctorId chỉ định), grace 30d config; `EnsureCanAccessPatientAsync` guard: kill-switch `Auth:TreatmentRelationshipEnabled=false` DEFAULT OFF + chỉ áp role DOCTOR/NURSE + miễn trừ scope ORG/BRANCH/DEPT (UserRoles.ScopeType mig 146 — backfill ORG nên hành vi hiện tại giữ nguyên tới khi admin thu hẹp) + throw UnauthorizedAccessException khi không có quan hệ. CÒN increment-3: wire guard vào Examination/EMR/Inpatient read-paths + ConsultationParticipant + bật pilot kèm smoke 2-doctor + admin UI scope. **cửa 413: #413 [PORT-P7] DicomViewer v2-native — ✅ PUSHED `Closes #413`** (subagent + tự re-verify): git mv 8 file (pages/DicomViewer + 7 component viewer từ modules/radiology/components) → `pages-v2/dicom-viewer/`, verbatim (rename similarity 97–100%, diff chỉ import); DicomViewerConfig GIỮ (Radiology.tsx v1 còn import); wrapper + route v1 retarget (1 nguồn 2 route); 0 CSS side-effect; stale-ref sweep 0; `grep "from '../pages/" pages-v2/` = 0 — **import v1 cuối cùng đã gỡ, mở khóa #204 Phase 3**. ⚠️ Cần phiên smoke viewer thật (mở study/W-L/MPR/cine/AI overlay) sau deploy. Gate tsc+vite EXIT 0. Smoke #405 prod: enabled-modules 11 giá trị + schema-drift 0 ✅. **cửa 405: #405 [LAYOUT-COMMERCIAL] EnabledModules — ✅ PUSHED `Closes #405`.** BE: IModulePackagingService + CommercialModules (6 CORE luôn bật, 4 toggle, extended) đọc/ghi SystemConfig key `EnabledModules` (JSON); GET/PUT `/api/system/enabled-modules` (PUT = System.Configure, CORE tự union); DI; **migration 147 seed FULL set cho deployment HIỆN HỮU** (prod demo không đổi hành vi; deploy thương mại mới ghi đè row theo gói; chưa có row → Gói PK mặc định) — fix QUOTED_IDENTIFIER cho sqlcmd, idempotent ×2 local. FE: modulePackaging.service (fail-open) nạp cùng loadPermissions (Promise.all trong AuthContext); `isPathAllowed` = permission AND moduleEnabled (choke-point → menu/palette/workspace switcher tự ăn theo); RequirePermission check module TRƯỚC permission → trang `ModuleNotEnabled` (khác 403, kèm hướng dẫn nâng gói). Gate: BE 0 error + FE tsc/vite EXIT 0. **cửa 404: #404 [LAYOUT-COMMERCIAL] workspace layer — ✅ PUSHED `Closes #404`.** `workspace.service.ts` (4 WS def + `availableWorkspaces()` suy từ permission-union của menu item + localStorage `his.workspace`); menu.service `workspaceForPath` (exact→prefix) + `getVisibleGroups(workspace?)`; Rail/Flyout nhận prop workspace; TerminalLayout state + deep-link auto-switch (effect pathname) + breadcrumb `Workspace › Nhóm › Trang`; TopBar switcher segmented (chỉ khi ≥2 WS). Lưu ý data: group `support` fallback→pharmacy nên nutrition/rehab/tradmed tạm ở WS Dược (data-only, chỉnh sau nếu cần). Gate tsc+vite EXIT 0. **cửa 375: #375 [LAYOUT-1] registry hoàn tất — ✅ PUSHED `Closes #375`.** RouteMeta + `workspace` (4 WS doc 08 §2) + `module` (10 module thương mại §7 | extended); script fill-workspace-module.cjs điền **166/166 entry** (48 extended); registry giờ là nguồn sự thật route+permission+workspace+module — #404 (switcher/sidebar-per-WS) + #405 (EnabledModules) tiêu thụ data này. Menu labels vẫn curate ở menu.service (by design, link qua permissionForPath). **cửa 379: #379 [LAYOUT-5] role-aware dashboard — ✅ PUSHED `Closes #379` (scope gốc; workspace re-org theo comment re-scope = phần #404, đã note).** Dashboard v2: KPI "Doanh thu" + BhytCard gate `Billing.Read` · ErSnapshot/OpdFlow/BedMapMini/OrBoard gate `MedicalRecord.Read` · PharmacyAlerts gate `Pharmacy.Read` · ShiftBoard/AlertsPanel pass-through (chưa có mã catalog). Fail-open như #378. Gate tsc+vite EXIT 0. **cửa 365: #365 [REFAC-3][P3] thin dev controllers — ✅ PUSHED `Closes #365`** (subagent + tự re-verify): scope thật 5593 dòng/21 action (PopulateData = partial 7 file); IDevDataServices (3 interface) + 9 impl `Services/DevData/` verbatim-move (shim Ok/BadRequest giữ body nguyên); DI registered; DailyDemoSeedWorker chuyển resolve DI thay `new Controller(db,...)` (deviation bắt buộc, hợp lệ); grep HISDbContext trong 9 controller = 0; build 0 error. Smoke #384 prod PASS (401 SESSION_INVALIDATED + không ping-pong) · deploy 369 schema-drift 0. **cửa 369: #369 [AUTHZ-3] increment-1 — ✅ PUSHED `Refs #369` (OPEN).** Migration `146_authz3_userrole_scope.sql` (UserRoles + ScopeType/ScopeId/ValidFrom/ValidTo/GrantedBy/GrantReason, backfill ORG behavior-preserving; verify local Docker ×2 idempotent, 13 rows). `ICurrentUserAccessor.BranchId` (claim branchId). HISDbContext: ctor 4-param (accessor+config) + **query filter cô lập branch PILOT Patient+QueueTicket** — combined soft-delete+branch, kill-switch `Auth:BranchIsolationEnabled=false` (DEFAULT OFF), BranchId NULL luôn thấy (legacy-safe). Field-masking billing: VERIFY DTO billing KHÔNG chứa chẩn đoán (chỉ XML BHXH giữ theo quy định) → không cần mask DTO; vector thật = RequirePermission trên endpoint lâm sàng (catalog rollout). CÒN LẠI #369: treatment-relationship handler + .ScopedTo (Examination/EMR/Inpatient) · bật pilot + characterization test · admin UI gán scope. **cửa 384: #384 [LAYOUT-10] concurrent login last-wins — ✅ PUSHED `Closes #384`.** BE: `ApplySingleSessionPolicyAsync` (3 luồng login password/OTP/WebAuthn) = RevokeAll refresh cũ (`new_login`) + xoay SecurityStamp → thiết bị cũ 401 ≤30s; config `Auth:SingleSessionPerUser` (default true). **Fix ping-pong quan trọng:** `RotateAsync` reuse-detection CHỈ áp cho replay chuỗi `rotated` ngoài leeway — token thu hồi bởi chính sách (new_login/logout/password_changed…) replay → fail mềm `session_ended`, KHÔNG revoke family (trước đây thiết bị cũ auto-refresh sẽ kích reuse-detected → đá luôn thiết bị MỚI). Program.cs: stamp-mismatch gắn `Items[auth_challenge_code]` + `OnChallenge` trả body `{code:"SESSION_INVALIDATED"}`. FE: apiClient bắt code này TRƯỚC refresh → sessionStorage `logout_reason` + redirect; Login.tsx hiện message.warning "đăng nhập ở nơi khác". Gate: dotnet 0 error + tsc/vite EXIT 0. **cửa 378: #378 [LAYOUT-4] FE permission gating — ✅ PUSHED `Closes #378`.** `permission.service.can()` THẬT (set từ `GET /api/me/permissions`, null=fail-open kỹ thuật); AuthContext `await loadPermissions()` TRƯỚC setUser (login/OTP/initAuth) + clear khi logout; 66 route điền `meta.permission` (script fill-permissions.cjs — Billing.Read×10 · MedicalRecord.Read×14 · Pharmacy.Read×12 · LabResult.Read×11 · Patient.Read×2 · Prescription.Read×3 · System.Configure×10 · Report.Read×4); RequirePermission guard (#377) tự kích hoạt; Sidebar dùng `getVisibleGroups()` (group rỗng ẩn); palette CmdK: mục menu cũng tra `permissionForPath`. Mapping THEO seeder RoleMatrix 8 role (#367) — lưu ý AC issue nói Pharmacist thấy Reports nhưng matrix KHÔNG có Report.Read cho PHARMACIST (theo matrix). Smoke #368 prod cùng lúc: TTL 30' ✓ refresh rotation ✓ /me/permissions 60 mã ✓. **cửa 368: #368 [AUTHZ-2] FE part — ✅ PUSHED `Closes #368`.** apiClient interceptor 401 → auto-refresh **single-flight** (axios trần `POST /auth/refresh`, ghi token+refreshToken mới vào localStorage raw key, replay request `_retried` 1 lần; giữ nguyên envelope-unwrap + exception `/inspector-portal`; KHÔNG refresh cho `/auth/login|verify-otp|resend-otp|refresh` — login sai mật khẩu không bị refresh-loop; `verify-password` trả 200+bool nên an toàn). AuthContext lưu `refreshToken` (login + verifyOtp) + `logout()` gọi `authApi.logout` fire-and-forget axios trần header chụp trước khi clear. `storage.service` thêm key `refreshToken`. `api/auth.ts` thêm `refresh()`/`logout()`. **JWT TTL 480'→30'** (`appsettings.json Jwt:ExpireMinutes`; Cloud Run KHÔNG có env override — verify `gcloud run services describe`). Follow-up tách issue riêng: refresh→httpOnly cookie + job dọn RefreshTokens hết hạn. **cửa 421: #421 [tech-debt][FE] print escape — ✅ PUSHED `Fixes #421`.** (a) `escapeHtml` mới trong `utils/printWindow.ts`; sweep ~275 điểm nội suy qua 11 file (10 v2 modules print template + `pages/inpatient/printTemplates.ts` v1 vì v2 Inpatient import trực tiếp — escape TẠI CHỖ không move); 3 subagent song song + spot-check diff + không double-escape fragment `.map().join('')`. (b) `constants/hospital.ts` → env `VITE_HOSPITAL_NAME/ADDRESS/PHONE`, fallback RỖNG (form pháp lý in trống thay vì placeholder "BỆNH VIỆN ĐA KHOA ABC" — Vercel cần set env nếu muốn in tên BV). (c) SigningWorkflow v2: SearchBox keyword (debounce 400ms) + Filter documentType (10 loại, labels đồng bộ v1) server-side qua SigningRequestSearchDto sẵn có. Gate: tsc + vite EXIT 0. **cửa 420: #420 [bug][FE][blood-bank] — ✅ PUSHED `Fixes #420`.** v2 BloodBank RequestsTab port Duyệt (`approveIssueRequest`, chỉ Pending, cf-confirm) + Xuất máu (`issueBlood`) với ModalShell chọn túi: filter Available + khớp bloodType+rhFactor+productTypeName (patient-safety — backend KHÔNG tự chặn), cap số túi = requestedQuantity−issuedQuantity; ActBtn per-row + nút trong drawer; sau action reload. Fix dead-wiring `BloodTypeDetail`: chip nhóm máu tab stock thêm nút ⓘ mở drawer (chip = span bọc 2 button, filter giữ nguyên). Gate: tsc + vite EXIT 0 (deploy #419 trước đó: smoke prod 2 endpoint mới 200 ✅). **cửa 419: #419 [bug][money] PartialRefundModal chết end-to-end — ✅ PUSHED `Fixes #419`.** BE additive: `GET /billingcomplete/refundable-items?patientId=&medicalRecordId=` (ServiceRequestDetails IsPaid + PrescriptionDetails đã duyệt/cấp, PatientAmount>0) + `GET payments/patient/{patientId}` (Receipts type=2 status=1) — cả 2 `[Authorize Admin/Cashier/Accountant]`; DTO RefundableItemDto + PatientPaymentBriefDto. FE: modal thêm Select phiếu thanh toán gốc (default prop/mới nhất) → truyền `originalPaymentId`; mode full lấy refundAmount = finalAmount phiếu gốc (hết bug 0đ); client-guard ≤ finalAmount + disable OK khi không có phiếu; typed api `getRefundableItems`/`getPatientPayments`. Nút v2 Billing vốn vẫn hiện (không cần bật lại). Gate: dotnet build 0 error + npm run build EXIT 0. Smoke sau deploy: mở modal từ v2 Billing → thấy list phiếu gốc + bảng dòng hoàn → tạo phiếu hoàn full/detail → kế toán duyệt. **cửa 418** (trước đó cùng phiên): #418 [bug][FE-contract] Rehabilitation api-client lệch route backend — ✅ PUSHED `Fixes #418`.** FE `modules/rehabilitation/api/rehabilitation.ts` viết lại khớp 1-1 controller (createSession→`sessions/schedule`, completeSession→`sessions/{id}/document`, xoá 15 hàm route-ảo không có backend; `status` string|number); BE additive: `GET /rehabilitation/treatment-plans/active` mới + `RehabType` vào RehabTreatmentPlanDto + enrich `MapToRehabSessionDto` (PatientName/PatientId/ScheduledDuration/PatientResponse/CancellationReason); v2 Rehabilitation.tsx đọc đúng field backend (sessTime/sessDay/sessDuration fallback legacy); v1 2 dòng compile-fix. Gate re-run phiên push: `dotnet build` 0 error + `npm run build` (tsc+vite) EXIT 0. Push backend → Cloud Run auto-deploy; smoke sau deploy: tab Lịch buổi tập v2 — chọn KH từ `treatment-plans/active` → tạo buổi (`sessions/schedule`) → hoàn tất (`sessions/{id}/document`) → huỷ/no-show; bảng hiện tên BN + giờ (trước đây trống).
>
> Cập nhật 2026-07-16 — **cửa components-restructure: gộp `type TabKey` 4 trang Catalogs (Paraclinical/Pharmacy/ProvincialHealth/Report) → `modules/administration/types/` ✅ PUSHED `6007bb08`, tsc EXIT 0. Kèm dọn local main: `git reset` (mixed, KHÔNG hard) khớp origin — xóa 7 commit trùng lặp nội dung (wave4-7 + components-FINAL đã lên origin từ trước) + phục hồi 6 file lệch-cũ (4 backend AUTHZ tiền-fix + HR.tsx/Quality.tsx còn import `layouts/terminal/Icon` cũ) về đúng bản origin đã fix — KHÔNG đụng byte nào của WIP #409 tiền/ký-số/lâm-sàng (Billing/BloodBank/SigningWorkflow/Laboratory/BhytVerifyModal/NewVisitModal/Reception/Surgery/MasterData/Reports+ReportsHospitalTab, còn nguyên trên đĩa local, chưa commit) theo quyết định của user — để dành cho phiên deploy+smoke-test riêng (cửa `409-final` đang có window khác giữ lock, còn sống). File chết xác nhận: `frontend/src/types/tab.ts` (0 importer). Việc kế tiếp: đợi user hoặc cửa 409-final hoàn tất + build+smoke-test nhóm rủi ro trên rồi mới push.** wave-7 ✅ `f66c068`; wave-6 ✅ `07facba`; wave-5 ✅ `0f27111`; wave-4 ✅ `287a711`; components-FINAL ✅ `e6f19a6`. cửa 367-authz1: ✅ `e3c6d24` (#367 OPEN — FE gating defer). cửa 368-authz2: ✅ `edcbb78`. cửa 409: batch-2 ✅ `5217c77` · batch-3 ✅ `f594867` · batch-4 ✅ `b15b4e8` · **FINAL ✅ PUSHED `200fdefc` (+ hotfix `ec76fe9f`) — #409 CLOSED, toàn bộ PORT-P3 hoàn tất.** 15 trang: Billing/Laboratory/Surgery/Reception/BloodBank/SigningWorkflow (tiền/ký-số/lâm-sàng) + LISConfig/Finance/EndpointSecurity/BookingManagement/Dashboard3Cap/Reports/MasterData (admin) + Radiology-RisAdmin/PatientPortalStaff(mới)/AssetManagement/EnvironmentalHealth (khác). Hotfix riêng: 3 file BillingEditor/EmrEditor/OpdEditor bị vỡ `vite build` do components-restructure sweep bỏ sót CSS side-effect import (`layouts/`→`components/layout/`). Gate: tsc+vite build EXIT 0 trong worktree cô lập tại origin/main. Lock `409-final` đã release.

> **Deploy 2026-08-05:** GitHub Actions `30978727136` success · prod `/health` Healthy · `/health/schema-drift` **missingCount = 0** (migration 160/161/162 đã áp) · smoke prod: sổ CĐHA 200 · báo cáo định mức 200 (bảng mới của 162) · kênh HL7 2 · remote PACS 2 · xuất Excel 200 (4590 byte).

## Cập nhật theo cửa (per-window — mỗi cửa 1 dòng distinct, KHÔNG ghi đè cửa khác; R6 anti last-writer-wins)
- **[ux-upgrade-v2]** updated 2026-08-13 — **#467 resume: wave-1/wave-3 bị stop do context compaction, scripts mất; xác định lại 4 batch còn thiếu (~33 file): sys-rem (5 file hệ thống chưa xử lý) · rec-emr (10 file reception+emr hoàn toàn chưa chạm) · surg (12 file surgery+reports+asset) · ins-tel (6 file insurance+telemedicine); đã sửa tay SystemAdmin guardrail (bug xóa trắng quyền vai trò + báo thành công giả) · PrescriptionEditor patient-safety (tương tác thuốc lỗi = false-safe) · friendlyError.ts (STATUS_BEATS_CONTEXT — fallback caller bị nuốt bởi HTTP text chung chung) · Reports.tsx icon `package`→`pill`. Workflow `wf_e28f5d92-e52` (8 agent: 4 apply + 4 verify) đang chạy cho 4 batch còn thiếu.** NEXT: chờ workflow xong → build gate tsc+vite → spot-check §3.0 RowActions (bù lỗi args wave-2) → báo cáo chờ push. CHƯA COMMIT/PUSH GÌ.
- **[spinner-loading-ux]** updated 2026-08-10 — **✅ PUSHED `3604159a`. Spinner loading UX toàn v2 HOÀN TẤT.** Kit: DataTable prop loading (bảng rỗng→LoadingState inline; reload→dim is-reloading + chip sticky); SimpleV2Page tự nối loading+Btn Làm mới; CrudModal nút Lưu→Btn loading={saving}; ab-module.css ab-btn-spin+@keyframe+is-reloading+ab-tbl-reload. Fan-out 2 đợt (12 agent song song): 146/150 trang v2 đã wire. 4 file không wire có lý do (BillingEditor lazy-entangled; QualityDashboardLive auto-poll 60s; ReportBuilderTab localStorage; EmrEditor cửa khác). Build-gate: tsc EXIT 0 + vite 2m.
- **[ris-pacs-peer-node]** updated 2026-08-05 — **Dựng node PACS thứ 2 (`orthanc-peer`, profile `dicom-test` nên `docker compose up` thường KHÔNG bật) → đóng nốt 2 mục treo trong roadmap. ✅ PUSHED `889514cc`+`5411f483`.** Chi tiết ở `docs/pacs/IMPLEMENTATION_LOG.md` mục 2026-08-05 (part 3). (1) **Bắt calling AE thật:** peer C-STORE study sang `his-orthanc` với calling AE `CR01` qua association thật (DNS container, không vòng NAT) → node 1 ghi `Origin=DicomProtocol`/`RemoteAET=CR01`/IP; HIS liên kết phiếu → DB lưu `SourceAeTitle=CR01`, `StationName=CR-PHONG1`, `DepartmentCode=KHOA CDHA`. (2) **Bộ lọc Source AE — chiều KHỚP:** rule lọc `CR01` → `triggered: 1` (trước đây mới chứng minh được chiều fail-closed). (3) **Storage Commitment end-to-end:** gửi sang peer với cờ bật → `Storage Commitment Success` + transaction UID, log `done`; xác minh độc lập ảnh nằm ở peer, nhận qua `DicomProtocol` từ `HIS_RIS`. ⚠️ **Phát hiện ngoài dự kiến — ĐÃ SỬA (user chọn phương án 1):** `appsettings.Development.json` trỏ PACS vào `http://localhost:8042` với `admin/orthanc` — **KHÔNG phải Orthanc của compose** mà là `Orthanc.exe` cài native, nghe `0.0.0.0:8042`. Toàn bộ hardening trong compose (REST loopback-only, đổi mật khẩu, CheckCalledAet, cấm echo ẩn danh) bị vô hiệu ở dev và archive lộ ra LAN với credential mặc định. **Sửa: XOÁ hẳn khối `PACS` khỏi `appsettings.Development.json`** để dev kế thừa `appsettings.json` (8043 / 4243 / user `his-api`) — xoá thay vì khai lại, vì chính bản sao trùng là nguyên nhân drift. **Cùng gốc bệnh trong code:** 3 endpoint proxy ảnh bệnh nhân (`pacs/instances/{id}/preview|rendered|file`) fallback cứng về `http://localhost:8042` khi thiếu config ⇒ có thể stream PHI từ archive không ai quản → nay trả `PACS_NOT_CONFIGURED` thay vì đoán. **Verify sau khi sửa, KHÔNG env override:** API probe `http://localhost:8043/system`; cả 2 study đang liên kết đều có trên node compose (24 study vs 2 của bản native ⇒ không mất dữ liệu xem ảnh); proxy trả preview PNG 1,3 MB + file DICOM 8,2 MB. `dotnet build` 0 error. Ghi chú: `appsettings.Docker.json` vẫn trỏ 8042 nhưng là **config chết** (Dockerfile đặt `ASPNETCORE_ENVIRONMENT=Production`, không ai chọn env `Docker`). Đã dọn sạch artifact test + dừng container peer.
- **[ris-pacs-ui-v2]** updated 2026-08-05 — **Dựng màn v2 cho phần backend vừa làm thật (trước đó KHÔNG màn nào gọi tới ⇒ bệnh viện không bấm được). SMOKE UI THẬT bằng chrome-devtools. ✅ PUSHED `88336f8c`.** Chi tiết ở `docs/pacs/IMPLEMENTATION_LOG.md` mục 2026-08-05 (part 2). 4 màn: (a) **Quản trị RIS › HL7/CDA** — CRUD kênh + kiểm tra kết nối thật + nhật ký message kèm ACK thật + nút gửi lại; cảnh báo khi >1 kênh bật mà thiếu `RIS:Hl7:DefaultConfigName`. (b) **Quản trị RIS › Định mức vật tư** — thay tab cũ vốn chỉ là link; khai định mức theo dịch vụ, chọn vật tư kèm tồn thật. (c) **Trang mới `/v2/radiology-reports`** — sổ CĐHA/siêu âm/TDCN + thống kê + doanh thu + định mức tiêu hao, mỗi tab xuất Excel. (d) **Modal "Vật tư ca chụp"** trên worklist CĐHA — kê tay hoặc sinh từ định mức. Kèm **viết lại vùng HL7 của `api/ris/integration.ts`** vì đã lệch controller (sai route `send`, tên field DTO không tồn tại, thiếu 7 endpoint) — đúng loại bug #418. **5 lỗi thật do smoke UI phát hiện & đã sửa:** thêm dòng qua collection EF đã tracked ⇒ EF sinh `UPDATE` thay `INSERT` ⇒ **báo lưu thành công nhưng không ghi gì** (sửa: xoá set-based + insert tường minh, áp cho cả định mức lẫn phiếu kê) · `Items.Clear()` làm sever quan hệ bắt buộc ⇒ update phiếu kê ném lỗi · tự Add lại vào navigation ⇒ **nhân đôi dòng** · `Note`/`Unit` non-nullable ⇒ model binder chặn khi để trống (cùng bẫy `SaveHL7CDAConfigDto.FilePath`) · xoá phiếu kê để lại **dòng con mồ côi** lọt vào báo cáo tiêu hao. **Smoke trình duyệt thật:** lưu định mức → đọc lại từ server → có dòng trong SQL; phiếu kê tạo/liệt kê/sửa OK; dịch vụ chưa khai định mức → **400 thật**, không sinh phiếu ma; log HL7 hiện `AA`→"Hệ nhận đã ACK", `AE`/không trả lời→"Lỗi"; test kết nối `true` với cổng thật sự lắng nghe (2576, PID xác minh) và `false` với cổng không (2575); xuất Excel tải file hợp lệ. Build-gate: `dotnet build` 0 error · `npm run build` EXIT 0. Dữ liệu smoke đã dọn sạch.
- **[ris-pacs-real]** updated 2026-08-05 — **Tiếp nối phần Codex làm dở: gỡ nốt phần "giả" của RIS/PACS. CODE XONG + SMOKE THẬT. ✅ PUSHED `889514cc`.** Chi tiết đầy đủ ở `docs/pacs/IMPLEMENTATION_LOG.md` mục 2026-08-05 + `HOSPITAL_GRADE_ROADMAP.md` — KHÔNG lặp ở đây. Tóm tắt 5 mảng: (1) **Provenance DICOM** — `IDicomPacsGateway.GetStudySourceAsync` đọc `Origin`/`RemoteAET`/`RemoteIP`/`StationName` thật từ archive; migration **161** thêm 6 cột + 2 filtered index vào `DicomStudies`; **bật lại bộ lọc Source AE/Khoa của auto-send** (trước ném `NotSupportedException` + worker skip toàn bộ). Study chưa phân giải được nguồn ⇒ không khớp rule có lọc (fail-closed). (2) **HL7 v2 gửi thật** — `Hl7MllpClient` (framing VT/FS/CR + đọc ACK + parse MSA); `SendHL7Message`/`SendHL7Result` (dựng ORU^R01 thật từ report)/`Cancel`/`Retry`/`SendCDA`/`TestConnection` trả đúng ACK của hệ nhận, hết bịa `AA`. **`TCP` = `MLLP`** (2 config seed sẵn dùng "TCP" trước đó bị từ chối oan). **Chọn kênh không đoán mò**: >1 kênh bật mà thiếu `RIS:Hl7:DefaultConfigName` ⇒ TỪ CHỐI gửi (bản cũ lấy "đầu bảng chữ cái" → kết quả CĐHA chui sang giao diện LIS). `SyncResultToDoH` đi qua đường này, hết bịa "Success". (3) **Sổ/báo cáo RIS** — sổ CĐHA/siêu âm/TDCN, doanh thu, thống kê query dữ liệu thật; **TAT tính từ dữ liệu** thay hard-code 30. (4) **Module vật tư CĐHA (8.4)** — trước đây 0 bảng, mọi endpoint bịa + vứt dữ liệu người dùng: migration **162** (4 bảng norm/prescription) + service thật (giá từ danh mục, FEFO chọn lô, bỏ lô khóa/hết hạn, chặn kho khóa) + báo cáo định mức tiêu hao đối chiếu định-mức×số-ca vs thực dùng. (5) **Xuất Excel** — trước trả 0 byte (Excel không mở được): `SimpleXlsxWriter` (OOXML bằng BCL, **không thêm dependency**) cho 6 loại báo cáo; loại sai → 400 `NOT_SUPPORTED` (thêm case `NotSupportedException` vào `DomainExceptionFilter`). (6) **Storage Commitment thật** — trước chỉ gửi cờ rồi tin vào job; nay N-ACTION + poll N-EVENT-REPORT, **chỉ coi là đã giao khi hệ nhận trả `Success`**, lưu transaction UID làm bằng chứng vào log truyền. **Smoke thật (không mock):** migration 161+162 áp 2 lần idempotent; sổ CĐHA 4 dòng + thống kê 5/5 ca TAT 3604,9 phút; auto-send trigger-check đọc Orthanc thật → `Origin=RestApi` + không khớp rule `AE=PX01`; HL7 ORU^R01 qua MLLP peer thật: **AA → status 4**, **AE → status 3 + lý do thật**, **peer chết → lỗi socket thật**; xlsx mở được (zip hợp lệ, mọi part XML hợp lệ, serial ngày đúng). **2 bug do smoke phát hiện & đã sửa:** query sổ không dịch được LINQ (lọc sau projection) và **thống kê nuốt mất ca chụp thiếu liên kết máy do required-nav ép INNER JOIN** (che luôn ca duy nhất có TAT đo được). Build-gate: `dotnet build` 0 error · `npm run build` EXIT 0. **CHƯA VERIFY (ghi rõ trong roadmap) — đều cần node PACS thứ 2 hoặc máy chụp thật, vì 1 node tự-store bị dedupe + kẹt chính N-ACTION của nó:** nhánh calling-AE khi study đến qua association DICOM thật; Storage Commitment end-to-end qua đường gửi của HIS (đã xác nhận endpoint Orthanc làm N-ACTION/N-EVENT-REPORT thật, `Status: Success`, khi 2 đầu tách biệt); capture adapter Serial/USB. **Đã chứng minh được điều quan trọng nhất: gửi tới cổng DICOM chết ⇒ `failed` + lỗi TCP thật, KHÔNG bịa "done".**
- **[nc26-test-fix]** updated 2026-08-04 — **TEST 9 hạng mục NangCap26 (chrome-devtools + local Docker SQL) → BE 9/9 đúng nhưng 7/9 KHÔNG dùng được từ UI; đã sửa hết, ✅ PUSHED `58a4d4ae` + `e503a4c2` (+1 commit dọn nốt).** Chi tiết đầy đủ ở `plan-nangcap26.md` §E (E.1 blocker · E.2 thiếu-UI · E.3 dọn nốt · E.4 còn tồn) — KHÔNG lặp lại ở đây. Tóm tắt: (a) `58a4d4ae` 4 blocker — dropdown quyền-dữ-liệu trắng (`{v,l}` vs `AbSelect` đọc `label/value`) · ghi đĩa CD/DVD gửi id phiếu thay `DicomStudies.Id` (thêm `RadiologyOrderDto.DicomStudyId`) · duyệt suất ăn không có đường vào (`GetCanteenQueueAsync` lọc bỏ `Planned` trong khi nút Duyệt chỉ hiện cho `Planned`) · 2 mẫu in NC26 thiếu trong `PRINT_FORMS` + nhánh dispatch props-phẳng. (b) `e503a4c2` 4 màn thiếu UI — tab "Đơn vị gửi mẫu" (LIS #15) · Trang cấp TTB + nút Cấp phát (XVII.3/4) · Duyệt kế hoạch bảo dưỡng (XVII.7) · `SplitEmergencyModal` tách nội trú CC (XIX.2#20) · `WorkingPlacePicker` topbar (I.4, hook `useWorkingDepartment` trước đó 0 consumer). ⚠️ **2 phát hiện ngoài dự kiến:** `ProcurementRequests.tsx` CHẾT từ #375 (trùng path `procurement` với trang kho) → cấp path riêng `/v2/asset-procurement`; lộ bug `BASE='/api/asset-procurement'` trong khi `apiClient.baseURL` đã có `/api` → 404. (c) Dọn nốt: KPI "Số dòng" tab Khóa kho luôn 0 → tính theo `locks.length`; **`GetStockIssuesAsync`/`GetStockIssueByIdAsync` là STUB trả rỗng/null** ⇒ màn Xuất kho Dược luôn trống dù tạo phiếu OK → cài đặt thật đọc `ExportReceipts`; nối "Phiếu lĩnh hóa chất" (#98) vào trang Xuất kho với dữ liệu thật; `BhytVerifyModal` hiện chip "Mô phỏng" thay "Hợp lệ" (đồng bộ `NewVisitModal` của cửa `76c3a581`). Evidence 24 ảnh: `docs/architecture/evidence/nc26-nangcap26/`. Build-gate: dotnet 0 error · npm run build EXIT 0. Deploy BE `30914401757` success + prod schema-drift 0. **Còn tồn:** `MaintenanceRecords` chưa có cột mã phiếu (`scheduleCode` undefined, đang fallback tên TB); BHYT vẫn mock tới khi nhập credential ở màn Cấu hình BHXH.
- **[fkey-footer-fix]** updated 2026-08-03 — **F-key bugs + Footer UI vỡ — CODE XONG, tsc clean (6 pre-existing error ko liên quan), CHỜ PUSH.** 7 file sửa: (1) `terminal.css`: `.his-terminal` flex-column, `.his-app` `flex:1 min-height:0` bỏ row `26px`, `.his-flyout bottom:0`, `.his-status` sizing explicit + media-query tương ứng. (2) `TerminalLayout.tsx`: `<StatusBar>` move ra ngoài `.his-app` (sibling flex) — fix footer bị che bởi sidebar rail (root cause: grid-column:1/-1 bắt đầu tại x=0, dưới 64px rail). (3) `CommandContext.tsx`: `pageOwned:false` cho `new`(F3)/`search`(F4)/`refresh`(F5)/`cancel`(Esc) — trước `true` → chip dim khi page chưa register; có DOM fallback nên không cần dim. `save`(F2)/`print`(F8) giữ `pageOwned:true`. (4) `Reception.tsx`: thay raw `useEffect` keydown (re-register mỗi render + double-fire) → `useRegisterCommands({save:đăng-ký-mới, new:gọi-số-tiếp, search:tìm-BN, refresh:loadData})`. (5) `Inpatient.tsx`: thêm `useRegisterCommands({save:y-lệnh-mới, search:bàn-giao-ca, refresh:loadData})`. (6) `Radiology.tsx`: thêm `useRegisterCommands({save:chỉ-định-CĐHA, refresh:reload})`. (7) `Laboratory.tsx`: thêm `useRegisterCommands({save:chỉ-định-XN, refresh:reload})`.
- **[parity-p456]** updated 2026-08-02 (lần 3) — **Parity #352 vòng-3: 7 batch PUSHED (6e31b971 · 742c7ce7 · 1f15ce22 · 8a652d19 · 5d8b7114 · 19a4e3cb · 68578a65), ~48 mục GAP đã đóng/verify trong report.** Batch-7 (FE+BE, deploy Azure tự chạy): **Insurance XML** — BE endpoint additive `GET /insurance/xml/batches` (đọc InsuranceXmlBatches #441, dotnet 0 err) + FE bảng "Lịch sử xuất XML" (tải/gửi lại BẤT KỲ đợt nào, status chip 4 trạng thái, auto-refresh sau export) + RangePicker khoảng ngày → fromDate/toDate (BE Part7 đã filter ServiceDate) → đóng nốt 3 gap Insurance. ⚠️ Sau deploy cần smoke: `GET /insurance/xml/batches` 200 + tải 1 đợt cũ đúng file. Batch 2-6 thêm: DeAn06 3 field KSK · Dashboard3Cap lịch trực theo ngày + TỔNG CỘNG footer · HospitalPharmacy sales 30d+keyword server + customerType default · ClinicalGuidance pageSize 200/debounce/delete-guard · Epidemiology catalog-select 28 bệnh + banner nhóm A · FollowUp size-changer/sort/badge · FoodSafety tab Thống kê · HealthCheckup tab Báo cáo · Booking debounce · DoctorPortal totalCount KPI + select-all toàn trang · InfectionControl tab Cách ly (list+detail+discontinue) · ClinicalCatalogs BE-error-msg delete + Làm mới · EmployeeProfile Select showSearch + BE-error-msg · CentralSigning filter Hành động + byType/topUsers panels · Surgery OR board drawer. **KHÔNG PORT có chứng cứ:** SystemAdmin Tích hợp APP (mock 100%) · Finance Expense tab (pseudo-AP) · Immunization createCampaign (BE không có endpoint) · Epidemiology contact-tracing static. **CÒN LẠI (mục lớn, cần phiên riêng):** EMR ~60 mẫu in MoH + Nhật ký in + Quản lý BA + Thử phản ứng thuốc · EmergencyDisaster 5 tab (Resources/Staff/START/notify-family/activity-log) · CentralSigning tab Sinh trắc học WebAuthn · Billing global Deposits/Refunds + e-invoice send/export/print · Insurance batch XML submit/download/date-range · DispensingCounter (dòng 106 omitted) · BloodBank bảng so sánh 8 nhóm (phụ) · Help/EmployeeProfile pagination minor. — Bản trước: P4 server-side filter: SpecialtyEMR (keyword+specialtyType+RangePicker fromDate/toDate qua SpecialtyEmrSearchDto, debounce 300ms) · TbHivManagement (keyword/type/category/dates → BE). **PHÁT HIỆN LỚN TbHiv: contract FE↔BE lệch toàn phần** (BE string-enum "TB"/"OnTreatment"/treatmentRegimen/route follow-up per-record vs FE numeric/tên khác) → viết **adapter 2 chiều** trong api/tbHivManagement.ts (map string↔number, đúng route, stats map, tolerant 2 shape), drawer fetch detail, **print card client-side** (openPrintWindow — BE không có endpoint print), CrudModal +prop onValuesChange → khối Lao/HIV conditional + rules range (CD4 0-3000, VL≥0, tháng 1-36, kg 0-300); v1 vá 3 collateral (bad-status numeric, pageIndex 0-based, print→v2). SystemAdmin: audit **detail drawer** row-click (method/path/IP/UA/details + old→new pretty-JSON; filter+pagination+deleteUser+role-perms đã có sẵn — verified). P5-P6: StockReport Pager 50/trang ×4 tab · Equipment nút "Lên lịch bảo trì" tab maintenance + Select thiết bị + field Đơn vị thực hiện (performedByCompany) + Trạng thái trong drawer · AssetManagement tab "Báo cáo TSCĐ" (catalog nhóm + generate print + pie statusBreakdown/bar depreciationTrends recharts) · Dashboard v2 +3 card ActivityCharts.tsx (ServiceStatusStrip 6 dịch vụ done/pending · ActivityChartCard 7-ngày/theo-khoa/phân-bố native-bars+conic-donut · RevenueBreakdownCard doanh thu theo khoa + donut loại BN, gate permission; FE HospitalDashboardDto +service*/revenue* fields). Report parity đã tick 19 mục (+2 verified-đã-có, +2 KHÔNG-PORT mock). Skip có chủ đích: SystemAdmin 'Tích hợp APP' (100% mock v1).
- **[p1p2-feat]** updated 2026-08-02 — **P1-4 SystemAdmin phân quyền v2 ✅ PUSHED `803e1674` + P2-2 Dynamic Report Builder v2 ✅ PUSHED `71a8df66`**. P1-4: Role edit modal nâng từ flat Select→grouped Checkbox UI (tìm kiếm, select-all theo module, badge count/tổng), thêm cột userCount, modal size lg. P2-2: Tạo `modules/reports/pages/ReportBuilderTab.tsx` (v2, _v2kit+storage.service, embedded types/constants, DataTable+ActBtn list, ModalShell xl builder+preview, localStorage persist). Wired as 3rd tab "Báo cáo tùy chỉnh" trong Reports.tsx. tsc 0 error. **MÁY NÀY HẾT TASK FIX — tất cả issue còn lại đều `test` label, bị hard-rule chặn.**
- **[p0-bugs]** updated 2026-08-02 — **P0 patient-safety + financial fixes ✅ PUSHED `016feeba`.** 4 bugs: (1) BloodBank `GetExpiringBloodBagsAsync` bỏ filter "Available" → túi Reserved sắp hết hạn cũng hiện (patient-safety). (2) HospitalPharmacy 5 lỗi constants: SHIFT_ST 0/1→1/2; GPP_TYPES type3 "Nhiệt độ" type4 "Độ ẩm"; custKpis VIP filter 1→2 + thêm NV type3; commKpis pending 0→1, paid 1→2; shift close condition 0→1. (3) BHYT 80% sai: FE check `it.bhyt` per-item + bhytTotal/ptTotal footer; BE CheckInsuranceCardAsync lấy InsuranceCoverageRate từ MedicalRecord mới nhất thay hardcode 0.8. (4) Drug interaction hard-stop: thêm state `overrideReason` + wire textarea + gate button severity>=3 + wired vào CreatePrescriptionDto+buildDto(). BE PrescriptionSafetyGuard đã đúng. Build: tsc EXIT 0 + dotnet 0 error. **Kế tiếp: P1 (Tờ điều trị hàng ngày / BHXH XML QĐ3716/2024 / 32 biểu mẫu TT32/2023 / SystemAdmin phân quyền v2).**
- **[fullpage-config]** updated 2026-08-02 — **Fullpage error pages + config files ✅ PUSHED `6f0266aa`.** (1) HttpError `variant='fullpage'` (position:fixed inset:0 z-index:9999) → 404/403/500/ModuleNotEnabled đều che toàn viewport, không thấy sidebar/topbar nữa. Wired: NotFound/Forbidden403/ErrorBoundary/HttpErrorPage/ModuleNotEnabled. (2) Config files: `app.config.ts` (hospital identity re:constants/hospital.ts + APP_NAME/VERSION + PAGE_SIZE_DEFAULT=20 + DATE_FORMAT + MAX_UPLOAD_MB) · `permission.config.ts` (ACCESS_GATING_ENABLED + VGCA_SDK_URL/SIGN_FN) · `route.config.ts` (ROUTES constant + POST_LOGIN_REDIRECT + PUBLIC_ROUTES). Wired consumers: constants/hospital.ts re-export từ app.config; permission.service + vgcaSign.ts import từ permission.config; RouteGuard dùng ROUTES.LOGIN; HttpError dùng ROUTES.DASHBOARD. config/index.ts barrel export đủ cả 6 file. tsc 0 err.
- **[463-464-cleanup]** updated 2026-08-02 — **#464 + #463 ✅ PUSHED + CLOSED. Module index.ts cleanup PUSHED.** #464 [BE][SEC]: PathologyController thêm `[RequirePermission(LabResult.Read)]` class-level (trước chỉ `[Authorize]` → lễ tân đọc PHI qua API), POST/PUT results thêm `LabResult.Create` — `3a407fbd`. #463 [BE][PAT2] GPB missing endpoints: (1) `POST /api/pathology/requests/{id}/cancel` (LabResult.Create) — bắt buộc lý do, chặn hủy phiếu Verified/đã-hủy, ghi CancelledBy+CancelledAt, Status=5 Cancelled; (2) `POST /api/pathology/results/{id}/verify` (LabResult.Validate) — guard request phải Completed, kết quả chưa duyệt, ghi VerifiedBy+VerifiedAt+VerifiedByName, chuyển request→Verified (4); (3) `PUT /api/pathology/requests/{id}/status` (LabResult.Create) — state-machine whitelist: Pending→Grossing→Processing→Completed, chặn backward/skip/invalid. 5 file: Pathology.cs + PathologyDTOs.cs + IPathologyService + PathologyService + PathologyController — `b87d8579`. Module index.ts: xóa 33 file rỗng (0 byte, 0 export) module-root + `administration/types/index.ts` (barrel không ai dùng); 4 trang Catalogs đổi import sang `'../types/tabs'` trực tiếp — tsc 0 err mới — `2d9ca416`. **MÁY NÀY HẾT TASK FIX** — tất cả issue còn lại đều `test` label, bị hard-rule chặn. CHỜ user mở test tasks hoặc có task fix mới.
- **[461-pages-v2]** updated 2026-08-02 — **#461 [FE][REFAC] Chuyển pages-v2/ sang modules/ — HOÀN TẤT, build-gate xanh, CHỜ PUSH.** Đã move 100% nội dung `pages-v2/` vào đúng module domain + xóa `pages-v2/` hoàn toàn (không giữ re-export stub). **Pages (git mv trước đó):** 14 trang → đúng module (opd/telemedicine/public-health/medical-record/nutrition/rehabilitation/survey/portal/laboratory/specialty/radiology/infection-control/system+dashboard). **Lazy.ts (4 file, 14 path):** clinical/common/diagnostic/administration.lazy.ts hết `pages-v2/*`. **_v2kit:** `pages-v2/_v2kit.tsx` → `src/_v2kit.tsx` (root src); **212 file** bulk update `'[...]/pages-v2/_v2kit'` → `'@/_v2kit'` (dùng `@/*→src/*` alias sẵn có, byte-level WriteAllBytes giữ CRLF). **Shared:** CabinetIssueModal→pharmacy/pages · EmrSigningChainDrawer→emr/pages · ExpiryAlertModal→pharmacy/pages · SurgeryCabinetIssueModal/SurgeryFormModals/SurgeryReportModal→surgery/pages · surgery-modals/ (5 file) → surgery/pages/surgery-modals/; tất cả internal import + 12 external importer cập nhật. **Dead code:** SpecimenImageGallery.tsx (0 importer) + DicomViewer.tsx stub — `git rm`. **_.gitkeep_:** 5 file xóa (administration/types, infection-control/pages, nutrition/pages, rehabilitation/pages, survey/pages). **Gate: tsc 0 error (chỉ còn 4 vitest pre-existing), pages-v2/ đã xóa khỏi fs.** Còn: push + close #461.
- **[455-460]** updated 2026-08-02 — **MÁY NÀY ĐÃ CẠN TASK CODE (aefb8927).** Synced: origin thêm `aefb8927` FE vitest harness #212 CLOSED + `ddf0acac` BE CI gate #191 CLOSED. Task code còn lại: **#437** httpOnly cookie (blocked hạ tầng — shared-domain chưa có; code đã xong `[x]`) + **#442** USB-token plugin (blocked hardware). **0 task code có thể làm từ máy này.** Per hard-rule: test tasks KHÔNG được start khi #437/#442 còn OPEN. **CHỜ USER:** (1) đóng #437 là infra-blocked nếu không kế hoạch sớm → mở khoá test tasks; (2) hoặc cung cấp hạ tầng shared-domain cho #437; (3) hoặc cung cấp USB-token phần cứng cho #442.
- **[452]** updated 2026-08-02 — **#452 [FE][P2] PatientPortal multi-tab v2 — CODE XONG build-gate xanh, CHỜ PUSH.** PatientPortal.tsx (114→390 dòng): chuyển từ SimpleV2Page 1 tab lịch hẹn sang **7-tab TopTabs** (Lịch hẹn / Kết quả XN / Lịch sử khám / Đơn thuốc / Hóa đơn / Thông báo / Tài khoản). Mỗi tab = `useListData` + `DataTable` + `DrawerShell` chi tiết. Tab Tài khoản = form `getAccount`+`updateAccount` (tên/SĐT/địa chỉ). Lazy-mount (mỗi tab chỉ load khi mở). Thực hiện 7/11 tính năng issue; skip 4 cần hạ tầng (thanh toán gateway VNPAY, upload CCCD, queue real-time, messaging WebRTC). Build gate: tsc EXIT 0 + vite 5938 modules ✓. Còn non-test: #442 USB-token (user/hardware) · #440 GCP billing (user) · #437 httpOnly (hạ tầng). **Test tasks CHƯA ĐƯỢC START (hard-rule).**
- **[453-batch]** updated 2026-08-02 — **#448 #450 #451 #453 ✅ PUSHED `d90f35b2` (9 commits) + CLOSED. #352 epic CLOSED.** 9 commits tổng: #352 bang-B enums/UX · #447 PaymentReports · #454 Prescription · #446 SystemAdmin · #448 Insurance XML ký+nộp+lịch sử · #450 SmsManagement message-type PascalCase fix · #451 Reports 4 tab mới · #453 server-side search Lab/Radiology/Equipment/AssetMgmt (20091cf3) + #453 Inpatient/DoctorPortal (d90f35b2). Reception và BloodBank không sửa được (API không hỗ trợ keyword). **Còn non-test:** #445 [P0] MethadoneTreatment hardcode · #452 [P2] PatientPortal · #442 USB-token · #440 GCP billing (user) · #437 httpOnly (hạ tầng). **Tiếp theo: #445 P0.**
- **[352-r3]** updated 2026-08-02 — **#352 vòng-3 adversarial parity verify ✅ XONG** (Workflow 85 agents ~9.3M tok): 119 cặp v1↔v2 trên main hiện tại, skeptic đối kháng **100%** DELETE_SAFE → **35 DELETE_SAFE thật · 83 GAP (missing kèm file:line) · 1 KEEP** — 50% verdict DS ban đầu bị lật (gồm patient-safety: BloodBank mất view túi máu ĐÃ hết hạn; ClinicalCatalogs mất validation required → ghi được danh mục rỗng). Deliverable: `10-assessment/v1-v2-parity-round3-2026-08-02.md` (bảng A/B/C) + §8 inventory doc + comment #352. #204 chỉ được gỡ 35 trang bảng A; 83 trang bảng B cần đóng gap/user duyệt bỏ. Epic #352 giữ OPEN (Phase-2/3 cần user + smoke). **MÁY NÀY ĐÃ CẠN TASK** — còn lại: #440 billing (user) · #433/434/435/437 (máy-2) · test-suite (sau cùng).
- **[438-smoke]** updated 2026-08-02 — **#438 ✅ CLOSED sau smoke seeded local** (code máy-2 `2d94c96b`, design user duyệt): fixture `RECON436-*` dựng đủ 5 loại lệch → API 10/10 PASS (NOT_DISPENSED×2 · OVER · FIELD_DRIFT 3-chiều · NO_ORDER · CABINET cuối bảng; summary + filter đúng) + UI tab "Đối chiếu thuốc" KPI khớp + filter OK. Evidence `oper-pharmwh/TC-PHW-021__s01-s02` (mã candidate — test-plan chưa có task reconciliation, đề xuất bổ sung chờ duyệt), manifest 10 ảnh. Known-limitation phase-2 ghi trong comment close (regular+cabinet cùng thuốc → OVER giả). **Billing GCP VẪN delinquent lúc 18:28 (redeploy fail cùng lỗi) — #440 chờ user.** Còn tự do cho máy này: chỉ #352 vòng-3 parity verify (token-heavy, cần user duyệt chi phí).
- **[439+440]** updated 2026-08-02 — **#439 ✅ PUSHED `Closes #439` + dọn trùng (2 máy fix song song cùng bug → dedup `758e0b5d`, tsc 0 err).** **🔴 #440 [OPS][P0] PROD 503 — root cause: GCP billing account DELINQUENT** (log Cloud Build run 30711938703: "billing account ... disabled in state delinquent") → Cloud Run tắt instance + MỌI deploy fail 403 (run #438 máy-2 cũng fail). KHÔNG phải lỗi code (deploy 17:57 `a7cb0540` READY+serving trước khi billing khoá). **CHỜ USER: mở lại billing → re-run `gh workflow run deploy-backend.yml` → verify /health 200 + schema-drift 0 + smoke prod #436 transfers.** Máy này hết task tự do: #433/#434/#435/#437/#438 máy-2 giữ; #440 cần user; test tasks chưa được chạy (còn fix OPEN).
- **[362-smoke]** updated 2026-08-02 — **#362 ✅ CLOSED sau smoke e2e patient-flow LOCAL seeded PASS** (backend local + SQL Docker + `seed-daily` 15 BN + Playwright): chọn-BN→khám→Lưu nháp (4 PUT 200)→Hoàn tất (POST complete 200, queue "Chờ khám"→"Hoàn thành")→guard chặn khi thiếu CĐ (không có POST)→kết luận reset khi đổi BN→disposition Hẹn tái khám (POST appointment 200 + in phiếu). **Evidence theo protocol** `docs/architecture/evidence/`: clin-opd TC-OPD-001 s01-s05 + TC-OPD-002 validation + oper-pharmwh TC-PHW-016 s01-s02 (bù cho #436), manifest tái sinh 8 ảnh (user nhắc — từ nay MỌI smoke UI phải kèm evidence). Bug minor tìm được → **#439** (icdQ không reset khi đổi BN, pre-existing, link 2 chiều). Giới hạn seed: IcdCodes+Services catalog rỗng local → nhánh thêm-mới CĐ/chỉ-định chưa exercise (ghi trong comment close). **⚠ PROD 503 sau deploy `a7cb0540`** (revision 00217-n4v READY lúc deploy 17:57 rồi 503 từ ~18:08 — nghi infra cold-start/SQL, KHÔNG phải build fail): đã kích `workflow_dispatch` redeploy chẩn đoán, đang chờ kết quả — xem dòng dưới + báo cáo phiên.
- **[437-b2]** updated 2026-08-02 — **#437 blocker-2 (session-resurrection cookie-mode) — ✅ PUSHED `Refs #437` (issue giữ OPEN: còn blocker-1 shared-domain + blocker-3 multi-browser smoke — hạ tầng).** Option A additive: `RevokeByTokenAsync` (IRefreshTokenService+impl — revoke bằng CHÍNH token, userId từ bản ghi, mirror RevokeAsync + đóng session, silent khi không tồn tại) · `LogoutByTokenAsync` (IAuthService+impl) · endpoint `POST /auth/logout-by-token` `[AllowAnonymous]+[EnableRateLimiting("refresh")]+[Consumes json]`, luôn 200 + ClearRefreshCookie (chống probing) · FE `api/auth.ts logout()` cookie-branch → endpoint mới KHÔNG Bearer (localStorage-branch byte-equivalent). **DORMANT** (flag OFF → FE không gọi). Verify: BE 0 err + FE EXIT 0 + smoke body-token: revoke xong refresh 401 (hết resurrection), token ma 200. CÒN #437: blocker-1 shared-domain + blocker-3 multi-browser (hạ tầng/máy khác). **Va chạm 2 máy phiên này: #435/#433/#434 máy kia (minhhung19872002) đã claim → tôi nhường #435 kèm handoff existence-check (comment issue: BE+FE-api loyalty/commission ĐÃ TỒN TẠI đủ, chỉ thiếu UI wiring + nên vá validate points>0).**
- **[436]** updated 2026-08-02 — **#436 Part-1 multi-line transfer (Pharmacy) — ✅ PUSHED `Closes #436` (Part-2 medication-reconciliation carve → #438, cần clinical design).** BE 4 file: `CreateTransferRequest`+`Items[]` (nhận medicineId v2 HOẶC medicationCode v1-legacy) · `TransferItemInput` record (Application) · `CreateTransferAsync` ghi `WarehouseTransferItem`+TotalAmount, validate from≠to/thuốc-tồn-tại/qty>0/qty≤tồn → `InvalidOperationException`→**400** (controller catch riêng) · list `GET /transfers` trả `items[]` · inventory +`medicineId`. FE 2 file: `api/pharmacy.ts` typed payload (v1 vẫn compile) · `Pharmacy.tsx` modal rework (Select kho từ `getWarehouses`, picker thuốc theo tồn kho gửi, line-items add/remove, guard+chống double-submit, surface message 400) + drawer chi tiết items khi click row. **Verify:** BE build 0 err + FE tsc/vite EXIT 0 · smoke API local 15/15 PASS (DB WarehouseTransferItems + TotalAmount đúng; 5 case lỗi 400; legacy no-items 200; v1-by-code 200) · smoke UI Playwright full flow PASS (modal→2 dòng→submit→list→drawer), console 0 lỗi mới. Seed fixture local giữ lại: kho KS436A/KS436B + 2 dòng InventoryItems (dọn transfers smoke rồi). Nit pre-existing ghi ở comment issue: TransferCode trùng khi cùng giây. **CÒN Part-2 (medication-reconciliation) cần clinical design → giữ OPEN.** Việc kế: user cho phép push → add 6 file + `Refs #436`.
- **[pull-restore-0810]** updated 2026-08-10 — **✅ PULL XONG + WIP `stash@{0}` ĐÃ KHÔI PHỤC VỀ WORKING TREE (cửa `spinner-loading-ux` tiếp tục được ngay).** HEAD=origin/main `1925edfc` (28 commits, 183 file). Stash gom 139 file (vụ 00:44) — phân loại 3 nhóm: **(A) 101 file** sạch+pull-không-đụng → `git checkout stash@{0}` trực tiếp. **(B) 12 file** sạch+pull-đã-sửa → 3-way merge (base=commit cũ, ours=pull, theirs=stash): 10 sạch, 2 xung đột thật đã giải tay: `Equipment.tsx` giữ `loading={loading}` của stash + block `actions` duyệt-bảo-dưỡng của origin; `TreatmentMonitorSection.tsx` giữ `variant="primary"` của origin (code-đã-push thắng WIP). **(C) 26 file** cửa kia đang ghi — **KHÔNG đụng**. **tsc --noEmit EXIT 0.** `stash@{0}` GIỮ NGUYÊN làm backup — chỉ drop khi cửa `spinner-loading-ux` xác nhận WIP ổn. **Code của cửa này (`#422`+`#407`) đã push từ phiên trước** (`b1ff17f6`+`0bd95e13`) — phiên này KHÔNG có commit mới để push. **→ `spinner-loading-ux`: WIP của bạn đã trở về working tree đủ 113 file (A+B), 26 file C bạn đang giữ không bị đụng; chạy `tsc --noEmit` verify rồi tiếp batch còn lại.**
- **[352-phase1c]** updated 2026-08-02 — **#352: 14 đóng · 4 loại-bỏ · 243 còn. 10 lô push.** Lô 8 `71d5040b` CommunityHealth **CRUD hộ gia đình** (`HH_FIELDS` khai báo sẵn mà 0 CrudModal tiêu thụ ⇒ tab chỉ-đọc). Lô 9 `bf450aec` CultureCollection **Hủy chủng** (`discardStock` chưa ai gọi ⇒ KTV không hủy được chủng; modal **bắt buộc lý do** vì loại bỏ vật liệu sinh học cần vết audit) + **lọc tủ lạnh** (option từ `getFreezerCodes()`, không suy từ dữ liệu đang hiện). Lô 10 `24815d15` Telemedicine **ghi hồ sơ trước khi đóng phiên**: v2 chỉ gọi `endSession` ⇒ ca khám từ xa đóng lại mà **KHÔNG có chẩn đoán/ICD/kế hoạch điều trị** — nay modal bắt buộc chẩn đoán chính → `createConsultation`+`completeConsultation` **TRƯỚC** rồi mới `endSession` (**cố ý khác v1**: v1 đóng phiên trước nên lỗi giữa chừng là mất hẳn dữ liệu lâm sàng; bản này lỗi thì phiên còn mở, cứu được). **★ 2 pattern lỗi hệ thống rút ra sau 10 lô — nên đưa vào checklist review v2:** (1) **API có sẵn trong api-client mà 0 trang v2 nào gọi** (AssetManagement ×2, BloodBank, Billing refund, CentralSigning TOTP, CommunityHealth, CultureCollection ×2) — rẻ để đóng nhưng phải grep caller trước; (2) **thao tác động tiền/vật liệu thiếu confirm hoặc thiếu lý do bắt buộc** (duyệt hoàn tiền, thanh lý TS, hủy chủng, tiêu hủy máu) — mỗi lô đều bổ sung confirm hiện rõ số tiền/đối tượng, KHÔNG port nguyên trạng v1.
- **[352-phase1b]** updated 2026-08-02 — **#352 Phase 1 tiếp: 8 đóng · 4 loại-bỏ · 249 còn (từ 261). 7 lô đã push.** Lô 4 `c7c28011` BloodBank: túi **Reserved sắp hết hạn** hiện lại (gộp nguồn BE + tự tính từ `units`, KHÔNG đụng BE để tránh ảnh hưởng caller khác) + **4 stat-card** phân bố hạn dùng. Lô 5 `6663e031` **BhxhAudit: 4 gap = tính năng CHẾT Ở CHÍNH v1 → KHÔNG PORT** — BE `api/bhxh-audit` chỉ có 6 endpoint, grep `auditor-accounts`/`import-excel`/`recordIds`/`records/{id}/pdf` = **0**, v1 gọi cả 4 (`pages/BhxhAudit.tsx:198,350,397,518,1503`) ⇒ v1 đang 404; đánh dấu `[~]` kèm bằng chứng thay vì port nhân bản lỗi. Lô 6 `97a5fb02` **màn mới `/v2/refund-approval`** — `approveRefund/confirmRefund/cancelRefund` nằm trong api-client mà **0 trang v2 nào gọi** ⇒ phiếu hoàn tiền tạo từ BillingEditor **kẹt vĩnh viễn ở chờ-duyệt** (tiền nợ bệnh nhân); màn mới có duyệt/từ-chối-bắt-buộc-lý-do/xác-nhận-chi/hủy + KPI *tiền* chờ duyệt, gate **`Billing.Collect`** (không phải Billing.Read vì thao tác chuyển tiền ra). Lô 7 `e81cf238` CentralSigning: nút Setup TOTP **vứt response** → báo thành công nhưng người dùng không có QR/mã để ghép app = **setup giả**; nay hiện `qrCodeUri`+`manualEntryKey` trong ModalShell + thêm nút **Tắt TOTP** (api có sẵn, chưa ai gọi). Spec smoke mới: `e2e/refund-approval.spec.ts`. **Bài học lặp lại 3 lần trong phiên: PHẢI verify endpoint BE tồn tại TRƯỚC khi port — nhiều "gap" thực chất là nút hỏng của v1.**
- **[352-phase1]** updated 2026-08-02 — **#352 Phase 1 KHỞI ĐỘNG — 4/261 gap đóng, 3 lô đã push.** (Nhận task vì cửa trước gỡ `in-progress` với lý do "Phase 1 cần smoke"; máy này có môi trường smoke đầy đủ.) **Con số thật: 261 checkbox gap** trong `v1-v2-parity-round3-2026-08-02.md` (83 là số TRANG, không phải số gap). Lô 1 `668914b7` AssetManagement tab **Bàn giao** + **Thanh lý** (API `getHandovers/saveHandover/confirmHandover` + `getDisposals/proposeDisposal/approveDisposal/completeDisposal` vốn nằm không dùng trong api client) — **port SẠCH**: v1 bắt gõ tay GUID tài sản → v2 Select tìm kiếm; duyệt/hoàn-thành thanh lý qua `cf()` **hiện số tiền** (v1 Popconfirm trống). Lô 2 `b710b236` BloodBank tab **Đã hết hạn** — **lỗ hổng patient-safety thật**: túi quá hạn KHÔNG xuất hiện ở view nào của v2 (`getExpiringBloodBags` bị BE lọc `Available`+`ExpiryDate>GETDATE()`) → nút Tiêu huỷ không bao giờ với tới túi cần huỷ; dùng `getExpiredBloodBags()` (có sẵn, chưa ai gọi) + banner cần-xử-lý-ngay + **CHẶN cấp phát** ở cả UI lẫn handler. Lô 3 `e2e11b62` BookingManagement **lọc khoảng ngày** (BE hỗ trợ sẵn `BookingManagementService.cs:231-234`, FE không truyền → không xem được lịch ngày mai để gọi xác nhận). Mỗi lô: build-gate xanh → smoke Playwright thật → tick checkbox parity doc → push. Spec: `e2e/asset-handover-disposal.spec.ts` · `e2e/bloodbank-expired.spec.ts` · `e2e/booking-date-filter.spec.ts` (**chạy `--workers=1`** — nhiều worker cùng login admin bị #384 single-session đá nhau). **CÒN 257 gap** — nhiều phiên; đề xuất ưu tiên patient-safety/tiền trước.
- **[goal-sweep-3]** updated 2026-08-02 — **#441 ĐÓNG TRỌN (BE) + #442 carve.** Ký số XML theo **client-side USB-token** (user chốt): `GET xml/{batchId}/sign-payload` (zip deterministic + digest SHA-256) + `POST xml/{batchId}/signature` — BE **VERIFY chữ ký bằng public key** trong chứng thư (RSA PKCS#1 v1.5/ECDSA) + kiểm hiệu lực cert, PASS mới ghi `DocumentSignature` (`DocumentType=InsuranceXmlBatch`, **tái dùng bảng sẵn có**) + thu hồi chữ ký cũ + `batch.Status=1`. **Bug tự bắt khi smoke:** `ZipArchive` gắn `LastWriteTime=now` → mỗi lần đóng gói ra bytes khác → chữ ký hợp lệ bị từ chối; sửa = sort tên + đóng băng timestamp 1980, và **submit dùng chung hàm đóng gói** (ký gì gửi nấy). Smoke `scripts/test-insurance-signing-smoke.ps1` **9 PASS/0 FAIL** gồm 2 case tấn công: chữ ký ngẫu nhiên bị chặn + chữ ký của **đợt khác** bị chặn. FE wire nút **"Nộp cổng BHXH"** (`3f0f054f`) — an toàn vì BE hết success giả; nút "Ký số" chờ plugin. **4 script smoke chuyển vào `scripts/`** (root `test-*.ps1` bị gitignore) để máy khác chạy lại. Commits: `97b522fe` `48dad395` `3f0f054f` `a5b8cf9b`. **CÒN LẠI (đều chặn ở người khác):** #442 (cần USB-token thật + chốt danh sách CA/OCSP) · #440 (user mở GCP billing) · #437 (hạ tầng shared-domain) · #352 (máy `quangngoc1911`).
- **[goal-sweep-2]** updated 2026-08-02 — **★ SMOKE LOCAL THAY PROD: đóng được 6 issue.** Prod chết (#440) nhưng DoD ghi "prod **OR seeded env**" → dựng môi trường local đầy đủ (Docker `his-sqlserver` + `dotnet run` :5106 + vite :3001 + seed `POST /api/admin/seed-daily/patients` với `DailySeed__Key`). **CLOSED: #433 #434 #435 #438 #369 #372.** Smoke: `test-goal-sweep-smoke.ps1` **28 PASS/0 FAIL** · Playwright `e2e/goal-sweep-smoke.spec.ts` **5/5** · `e2e/goal-sweep-opd-safety.spec.ts` (2 mục patient-safety #433) **PASS** — bắt được `PUT /examination/{id}/medical-interview` chứng minh auto-save 30s ghi SERVER thật + gợi ý CDS **xoá sạch khi đổi BN** · `test-authz3-pilot-smoke.ps1` **6 PASS/0 FAIL** (BS-A 200 / BS-B **403**; flag OFF → dormant) · `test-insurance-batch-smoke.ps1` **6 PASS/0 FAIL**. **2 DEFECT phát hiện KHI SMOKE, đã sửa+push:** (1) `290c4a57` #369 `UnauthorizedAccessException` trả **500 thay vì 403** (guard chặn đúng nhưng client không phân biệt được "thiếu quyền" vs "lỗi hệ thống") → thêm case vào `DomainExceptionFilter` + `ForbiddenExceptionFilter` global (filter gắn controller chạy TRƯỚC filter global nên phải cả 2 lớp); (2) `97b522fe` #441 export XML trả `BatchId=Guid.NewGuid()` không persist → `DownloadXmlFileAsync` **tải nhầm đợt** (luôn lấy thư mục mới nhất) + submit gửi **payload giả** → bảng `InsuranceXmlBatches` (migration **153**, idempotent ×2) + download tra theo batchId + submit zip file thật, thiếu batch → `Success=false` KHÔNG cấp TXN giả. **#434 đóng phần FE**, 2 mục BE carve sang **#441** (còn mỗi ký số — chờ user chọn VGCA/USB-token/HSM). **CÒN LẠI toàn bộ chặn ở người khác:** #440 (user mở billing) · #437 (hạ tầng shared-domain) · #441 ký số (chờ quyết định) · #352 (máy `quangngoc1911`). Test tasks vẫn chưa được khởi động (hard-rule: còn fix task open). ⚠️ Máy này đang chạy backend :5106 + vite :3001 + có seed data local.
- **[goal-sweep]** updated 2026-08-02 — **🔴 CHẶN TOÀN CỤC: PROD BACKEND 503 (#440 GCP billing delinquent) — chỉ user mở lại billing được.** Verify độc lập: `/health` 503 · deploy run `30711938703`(5e21e28b) + `30711997255`(2d94c96b) FAIL với `403 ... billing account ... disabled in state delinquent`; run `30711191138`(a7cb0540) trước đó success → **không phải lỗi code**. Commit BE treo chờ deploy: `2d94c96b` (#438). FE (`5e21e28b`,`ba9e3e89`) Vercel deploy bình thường. **Phiên này PUSHED:** `5e21e28b` #433+#434+#435 · `2d94c96b` #438 · `ba9e3e89` #439 (CLOSED — reset ô tìm ICD/DV khi đổi BN). **Mọi issue non-test còn lại đều chặn ở deploy+smoke prod hoặc hạ tầng** → không code thêm được từ máy này cho tới khi billing mở. Test tasks vẫn KHÔNG được khởi động (hard-rule).
- **[438]** updated 2026-08-02 — **#438 medication reconciliation — clinical design ĐƯỢC USER DUYỆT + CODE XONG.** Design chốt: khớp theo **ĐỢT ĐIỀU TRỊ** (khoá `MedicalRecordId`+`MedicineId`, KHÔNG theo ngày vì y lệnh kê theo ngày còn cấp phát gom theo phiếu lĩnh → lệch giả) · phase 1 **CHỈ BÁO CÁO read-only** (không tự sinh phiếu điều chỉnh — patient-safety) · xuất tủ trực (`DrugOrderType=2`) = nhóm **CABINET_ISSUE hợp lệ có kiểm soát**, không tính NO_ORDER. Map bảng thật: y lệnh `Prescriptions`(`PrescriptionType=2`, loại `Status=4` hủy + `DrugOrderType=3` hoàn trả)+`PrescriptionDetails` ↔ cấp phát `ExportReceipts`(`ExportType=2`,`Status=1`)+`ExportReceiptDetails`. **5 loại lệch:** NOT_DISPENSED · NO_ORDER · OVER_DISPENSED · **FIELD_DRIFT** (phát hiện thêm khi khảo sát: `PrescriptionDetail.DispensedQuantity` là cột denormalized có thể lệch tổng phiếu xuất → đối chiếu 3 chiều) · CABINET_ISSUE. BE 3 file (DTO + `PharmacyService.Reconciliation.cs` partial + `PharmacyController.Reconciliation.cs` `GET /api/pharmacy/reconciliation`) — additive, KHÔNG migration, KHÔNG đổi endpoint cũ, DI sẵn (partial class). FE 2 file (api typed + tab "Đối chiếu thuốc" trong Pharmacy v2: KpiStrip 6 ô + lọc ngày/loại + DataTable). Gate: `dotnet build` 0 Error · `tsc -b` EXIT 0 · `npm run build` EXIT 0. Còn DoD: smoke seeded env.
- **[435+434+433]** updated 2026-08-02 — **GOAL "giải quyết mọi Issue open": 3 issue FE CODE XONG, build-gate xanh.** 9 file (7 sửa + 2 hook mới), `tsc -b` EXIT 0 · `npm run build` EXIT 0. ⚠️ **Va chạm 2 cửa ở #437:** tôi cũng code blocker-2 (`/auth/logout-cookie`) nhưng cửa `[437-b2]` đã PUSH trước bản tương đương (`/auth/logout-by-token`) → **tôi DROP toàn bộ 6 file #437 của mình khi rebase, giữ bản upstream** (không tạo endpoint trùng chức năng). **#435** HospPharm: tab KH thêm nút ⭐ ModalShell cộng/đổi điểm (guard client mirror BE `Insufficient points` + cf-confirm) · tab Hoa hồng thêm "Thêm hoa hồng" (validate SL/tiền/tỉ-lệ 0-100 + hiển thị HH tạm tính đúng công thức BE `SaleAmount*Rate/100`). BE 3 endpoint XÁC MINH CÓ THẬT (carve "unconfirmed" đã giải toả). **#434** Insurance XML: nút "Xem trước & kiểm tra" (`POST xml/preview` thật) + **cổng chặn QĐ4210** (export luôn preview trước, `hasBlockingErrors` → KHÔNG export) + lọc khoa (`departmentId` có sẵn; danh sách khoa tái dùng `reports/by-department`, không thêm API). **2 mục còn lại BỊ CHẶN Ở BE (đã ghi rõ vào issue):** submit-BHXH gửi payload GIẢ `<batch>{id}</batch>` vì `ExportToXmlAsync` trả `BatchId=Guid.NewGuid()` KHÔNG persist (kéo theo bug `DownloadXmlFileAsync` bỏ qua batchId → tải nhầm đợt); `xml/sign/{batchId}` KHÔNG có route BE (v1 đang gọi 404). **#433** OpdEditor v2: hook mới `useOpdCds` (gợi ý chẩn đoán theo nút bấm + cảnh báo lâm sàng `/cds/alerts` + NEWS2, fail-safe, xoá sạch khi đổi BN) · banner "còn thuốc chưa lĩnh" **sửa lỗi v1** (v1 lọc `isDispensed` = field KHÔNG TỒN TẠI trong DTO → banner chưa bao giờ hiện; v2 dùng `status 0|1`) · auto-save **server 30s** parity v1 qua 3 endpoint PUT sẵn có (giữ nguyên autosave localStorage 3s) · hook mới `useOpdOrderTemplates` (mẫu bộ chỉ định localStorage, tái dùng key `opdSupplyTemplates`). **CÒN LẠI (không làm được trên máy này):** #369/#362/#352/#372 + phần dư #434/#437 đều cần deploy+smoke prod. **Test tasks (#191/#212/#213 + ~150 TEST-EV) CHƯA ĐƯỢC PHÉP KHỞI ĐỘNG** (hard-rule: test đi cuối, còn fix task open).
- **[422-cookie]** updated 2026-07-26 — **#422 [AUTHZ-2b] Part-1 httpOnly cookie — CODE XONG DORMANT, build-gate xanh, adversarial-verified, CHỜ PUSH (READY_FOR_PUSH).** Refresh token → httpOnly cookie (`his_rt`; HttpOnly/Secure/SameSite=None/Path=/api/auth/14d) + strip khỏi body, gated kill-switch kép `Auth:RefreshCookieEnabled`(BE) + `VITE_REFRESH_COOKIE`(FE) **default OFF** → byte-equivalent #368 localStorage-mode. BE 2 file (AuthController: 3 helper + wire login/otp/webauthn/refresh/logout, refresh dual-read cookie∥body + `[Consumes json]` chống form-CSRF; appsettings flag+pre-enable-checklist). FE 4 file (api.config `REFRESH_COOKIE_MODE`; apiClient withCredentials gated + refresh cookie branch; api/auth refresh+logout cookie branch; AuthContext logout cookie branch). **Verify workflow 4-lens Opus (365k tok):** LENS-1 default-off-equivalence PASS · LENS-4 regression PASS · LENS-2 cookie-correctness CONCERN (cross-site 3p-cookie Safari/FF chặn — đã document) · LENS-3 security CONCERN (XSS-read goal ĐẠT + CSRF phòng thủ tốt; **1 gap: idle-logout access-token hết hạn → cookie không xoá server-side → session-resurrection**). Cả 2 CONCERN chỉ manifest KHI BẬT cookie mode → **enable-time blockers** (shared parent-domain + fix logout-clear + multi-browser smoke), KHÔNG phải defect code dormant. Build: BE 0 err + FE vite EXIT 0. JSON valid. **Part-2 (TokenCleanupWorker) đã ship trước.** Việc kế: user quyết push + close#422-hay-giữ-open-carve-enable-issue. Lock 422 giữ.
- **[352]** updated 2026-07-18 — **#352 [FE][P2] Phase 0 inventory cập nhật — docs ONLY, CHỜ PUSH.** Append section §7 vào `docs/workspace-docs/10-assessment/v1-v2-conversion-inventory.md`: số liệu 2026-07-18 (v1=149 frozen · v2=239 total), tiến độ issues #408/#409/#410/#411/#412/#413/#414/#415/#417 ✅ CLOSED, #407 🟡 in-progress; quy tắc duplicate-not-move + DicomViewer revert; parity patch (DoctorPortal→Full, HospitalPharmacy→Partial, 12 stubs bổ sung CRUD, Surgery/Billing→Full). Gỡ label in-progress khỏi #352 (Phase 1 cần smoke). **HẾT task an toàn không cần smoke trên máy này — chờ user cho phép push + chỉ đạo tiếp theo.**
- **[427]** updated 2026-07-18 — **#427 [FE][UX] RouteGuard loading + ab-spin keyframe — ✅ PUSHED `8694569f` CLOSED.** RouteGuard: `<div>Loading...</div>` → centered Spinner + "Đang xác thực…" trên --d-0 bg. ab-module.css: thêm `@keyframes ab-spin` (referenced by Spinner.tsx nhưng thiếu → component không spin). HẾT task không cần smoke trên máy này.
- **[362]** updated 2026-07-18 — **#362 [FE-2b] OpdEditor Phase-2 god-split — ✅ RESCUED + PUSHED `80311df6` (Refs #362, OPEN).** Cửa `4e738e5a` hoàn tất full split (7 hooks + 5 section components, OpdEditor **1204→312 dòng** DoD<500 ✓) rồi **CHẾT khi CHƯA commit** (work treo working-tree, lock 362 rác). Cửa `d4ba1565` (rescue) CỨU: (1) verify độc lập = **Workflow 6 agent adversarial** (state/effects/patient-safety-handler/prop-threading/autosave-F10/cls-consult-disposition) so từng dòng vs baseline HEAD `5b2a62b2` → **PRESERVED, 0 confirmed + 0 suspected regression** (diff duy nhất: thêm stable setter vào dep-array — 0 đổi hành vi); (2) build-gate `tsc -b` 0 error + `npm run build` EXIT 0; (3) corroborate: cửa chết đã chạy **348/348 Playwright PASS** (chưa re-verify độc lập). 13 file `Refs #362`. **Kèm fix `247b5647`:** `Inpatient.tsx` DepositSection cast `(r as [])` → `r.data` (vừa lỗi tsc chặn build main vừa bug runtime deposit rỗng; follow-up #407 ce2ab794/ffa28135). **CÒN: smoke e2e patient-flow trên seeded/prod env trước khi Close #362** (seed local 2 phòng hàng đợi rỗng). Lock 362-rescue released.
- **[407]** updated 2026-07-26 — **#407 ✅ CLOSED `Closes #407` — SAFE_NOW integrations complete, CARVE issues created.** inc-11 (Insurance print bảng kê KCB BHYT): `printClaimSheet()` + `openPrintWindow` + `escapeHtml` + `HOSPITAL_NAME`. inc-10 (Insurance card-verify tab): đã push trước. **4 tính năng SAFE_NOW thêm trong commit này:** MedicalRecordPlanning 4 filter (codes in-memory RangePicker + transfers/outpatient server-side RangePicker + handover SearchBox keyword); Pharmacy barcode scanner (BarcodeScanner component + invScanOpen state); HospitalPharmacy edit-customer (dateOfBirth/gender fields) + print-revenue (openPrintWindow+escapeHtml HTML table) + commission keyword search. 4 CARVE child issues: **#433** OPD AI-CDS+clinical-alerts+auto-save · **#434** Insurance XML preview+sign+submit-BHXH · **#435** HospPharm loyalty-points+manual-commission · **#436** Pharmacy multi-line-transfer+medication-reconciliation. — **inc-9 ✅ PUSHED `ce2ab794`:** `Inpatient.tsx` tab Vật tư/DV + Deposit + `Pharmacy.tsx` footer Nhận/Cấp phát.
- **[371]** updated 2026-07-18 — **#371 [AUTHZ-5] CLOSED ✅ PUSHED d73377a1.** Inc-6 (cuối): AuditWriterWorker fallback file-log (NDJSON tại AuditFallback:Directory khi DB unavailable) + AuditWriteMetrics.FailureCount/RecordFallback/fallbackCount → /health/audit-metrics. Recertification report: GET /api/audit/recertification (join UserRoles+Users+Roles, nhóm ScopeType) + FE CompliancePanel tab 4 "Tái chứng nhận". BE 0 errors · FE tsc+vite 0 errors · lint OK. Mọi acceptance criteria DONE. Còn yêu cầu smoke: temporal table Roles/RolePermissions (không blocking) + /api/audit/recertification test trực tiếp.
- **[370]** updated 2026-07-18 — Phiên: #363(registry+threshold✅f5de4388·deployed) · #371 BE 2-report✅d7c8a72a + FE viewer✅d1df5ce8 · #370 Delegation dormant✅33da97da · FIX va-chạm-4-cửa `safe-commit.sh`✅027e90a7 · **#426 [DOC] authz-activation-runbook** (survey 3-agent map kill-switch/wiring #369/#370/#371 → `docs/architecture/authz-activation-runbook.md`, phục vụ phiên smoke). Kế: chờ user.
- **[428]** updated 2026-07-18 — **#428 [FE][P1] HTTP error pages ✅ PUSHED `ca733d2b` CLOSED.** HttpError registry 15 mã HTTP + ErrorBoundary + NotFound + Forbidden403 thin wrapper + AppRoutes catch-all + apiClient 503 + App ErrorBoundary wrap. Code-review gaps committed separately (CSS tokens/onBack/res.data/NotFound export).
- **[431+432]** updated 2026-07-20 — **#431 layout + #432 AUTHZ route-permission — ✅ PUSHED + CLOSED (`fef5c6a6`+`90016c54` #431 · `fb6b8f2c`+`42945bdf` #432).** #431: `components/layout/` band-aware TerminalLayout (patient-context Ticker ẩn/hiện theo band clinical/workstation vs admin/dashboard) + AuthLayout/BlankLayout/FullscreenLayout/ErrorLayout + variants Wizard/Split + RouteLayoutHost (route có `meta.layoutOverride` render ngoài shell) + layoutAccess 11×8 oracle + opd-move 7 hooks pages/hooks→modules/opd/hooks + lint-fe-folders.sh. #432 AUTHZ (5 bước): audit→dedup casing (151 xoá mã UPPER legacy)→catalog+role seed (PermissionCatalog +Reception/Surgery/Radiology/Insurance; RoleMatrix +Surgery/Radiology cho DOCTOR, +Insurance cho CASHIER, +Reception cho RECEPTIONIST; 152 seed IMAGING_TECH)→tag 100 route + **action-level** (route GHI khám/EMR→MedicalRecord.Create BS-only; ĐỌC→*.Read ĐD xem được nhưng KHÔNG ghi)→smoke tĩnh **34/34 khớp bảng actor** (fix ĐD vào được /opd/edit). FE gating mặc định OFF (staged, `.env.local VITE_ACCESS_GATING=true` để user browser-smoke). FE tsc EXIT 0. **+ AUDIT TOÀN DIỆN (commit 42945bdf):** workflow 11 agent audit 8 actor×164 route + verify đối kháng skeptic → **13 route mis-gated NGOÀI 8 đợt đầu** (billing/edit Billing.Read→Collect [BLOCKER tiếp đón sửa viện phí]; sample-receive→LabResult.Create; non-dicom-capture→Radiology.Create; 6 route dược→StockIn/StockOut/Dispense/Approve; service-requeue→MedicalRecord.Update; emr-cloud-sync/receipt-book-admin→System.Configure; emr-hl7-export→MedicalRecord.Export). Smoke tĩnh MỞ RỘNG **51/51 PASS** (8 actor cô lập). **+ #431 CODE-COMPLETE (Design Y) + 2 bug user báo khi browser-smoke ĐÃ FIX + VERIFY LIVE (commit 90016c54):** (1) actor login→403 màn đầu (HomeEntry redirect cứng /v2/dashboard=Report.Read; ĐD/tiếp đón/dược/KTV-XN không có → 403) → `resolveHomeRoute()` landing theo quyền (8/8 actor vào đúng route: admin→dashboard, BS+ĐD→opd, tiếp đón→reception, thu ngân→finance, dược→pharmacy, KTV-XN→lab, KTV-CĐHA→radiology); nurse→/v2/opd VERIFIED playwright, nurse gõ /v2/dashboard=403. (2) /v2/radiology/viewer lỗi sáng-tối (FullscreenLayout ép #000+#fff) → neutral bg sáng, VERIFY live nền đồng nhất. Issue #431 checklist reconciled 48 item [x] (Phase 2/3 heavy-approach MOOT vì Design Y band-derived). **5 commit local: 90016c54+fef5c6a6(#431) + 42945bdf+fb6b8f2c(#432).** **CÒN: user browser-smoke 8 tài khoản + quyết định push (push=đóng #431 + kéo #432 BE-migration→auto-deploy Cloud Run).**
- **[425]** updated 2026-07-18 — **#425 [FE][v2] EndpointSecurity Tổng-quan tab — DONE-code, CHỜ PUSH.** Thêm `TopTabs` (Tổng quan / Danh sách máy) vào v2 EndpointSecurity; tab Tổng quan: `getSecurityDashboard()` → KpiStrip (totalDevices/compliantDevices/openIncidents/unauthorizedSoftware) + devicesByStatus progress-bars + incidentsByCategory badges + summary stats grid. `TopTab` type import từ `_v2kit`; `EndpointSecurityDashboardDto` type đã có sẵn trong api; build-gate `-b --noEmit` EXIT 0; pre-existing errors (Inpatient/Insurance/PrescriptionEditor) từ cửa #407/#371 không liên quan.

## Phiên 2026-07-12 (cửa 367-authz1 — #367 AUTHZ-1 backend, ✅ PUSHED + CLOSED)
- **#367 [AUTHZ-1][P1] DONE**: PermissionCatalog (38 code) + RequirePermission + PermissionPolicyProvider + PermissionAuthorizationHandler (fail-closed) + PermissionService (DB + IMemoryCache 30s) + PermissionCatalogSeeder (idempotent startup) + MeController GET /api/me/permissions + 11 pilot action / 3 controller. Migration 145: IsSensitive + unique index PermissionCode.
- **Fix blocker trước push**: seeder GroupBy dup-safe + unique filtered index + handler try/catch.
- **Deploy**: Cloud Run auto (commit `e3c6d24`). Verify: GET /health/schema-drift missingCount=0 + GET /api/me/permissions ≥38 + DOCTOR GET /api/audit/logs → 403.
- **FE gating DEFER**: AuthContext.hasPermission + ProtectedRoute + menu filter — bị chặn bởi components-restructure đang dời api/. Handoff ở comment #367.

## Phiên 2026-07-12 (cửa 409 — batch-3 ✅ `f594867` · batch-2 pages ✅ `5217c77` · batch-4 IN PROGRESS)
- **#409 batch-3 PUSHED `f594867`**: Consultation (272→399) + InfectionControl (358→521) + Telemedicine (548→810) + TrainingResearch (290→605) + Equipment (264→923). tsc EXIT 0 worktree cô lập tại `287a711`.
- **#409 batch-2 pages PUSHED `5217c77`**: Rehabilitation (269→840) + Epidemiology (319→568) + SatisfactionSurvey (388→704) + MethadoneTreatment (433→473) + LabQC (655→730). tsc EXIT 0 worktree cô lập tại `e3c6d24`. (Nutrition+survey-api đã lên trước `e8ce769`.)
- **#409 batch-4 DONE-code CHỜ push** (lock `409-batch4`, tsc EXIT 0): 6 trang — MedicalRecordArchive (pages-v2, 1119 dòng) · IvfLab (pages-v2, 921 dòng, TopTabs 6 tab, setTimeout verbatim-move) · HealthExchange (modules/system/pages, 1701 dòng) · BhxhAudit (modules/insurance/pages, 777 dòng) · HR (modules/hr/pages, 2230 dòng) · Quality (modules/quality/pages, 1394 dòng). Spot-check: _v2kit import đúng path · 0 pages/ import cấm · side-effect sạch · tsc EXIT 0. CHỜ user duyệt push.
- **Còn lại #409 sau batch-4**: nhóm tiền/ký-số/lâm-sàng (Billing/Laboratory/Surgery/Reception/BloodBank/SigningWorkflow) = phiên deploy+smoke · nhóm admin (Reports/MasterData/Finance/LISConfig/EndpointSecurity/BookingManagement/Dashboard3Cap) · Radiology config-tabs · PatientPortal staff-route · AssetManagement Tenders · EnvironmentalHealth biosafety.

## Phiên 2026-07-12 (cửa components-restructure — wave-4 STRICT-relocate, DONE-code CHỜ push)
- **Wave-4 STRICT-relocate 90 pages-v2/*.tsx → modules/*/pages/** (his-fe-convention §4a):
  - 90 trang dời theo bằng chứng single-module-api-owner (classify-wave4.cjs) + rewrite specifier resolve-based
  - Router: 7 file lazy + AppRoutes.tsx rewritten (93+ specifier → modules/*/ paths)
  - Gate: `tsc -b --noEmit` EXIT 0 · verify A) 90/90 content-equiv PASS · B) 1017 stale-ref PASS
  - **5 trang WIP reverted về pages-v2/** (on-disk nhiều dòng hơn HEAD → cửa 409-batch2 owns): Epidemiology · LabQC · MethadoneTreatment · Rehabilitation · SatisfactionSurvey
  - **Deferred (không move wave-4):** 9 name-conflict (Help/OPD/EMR/Consultation/RadiologyOps/DispensingCounter/ClinicalPharmacyCheck/DoctorPortal/Finance) · 18 multi-module · 30 no-api — pages-v2/ giữ
  - **✅ PUSHED `287a711`** — hook `0a72e27` + wave-4 `287a711` lên origin/main (cherry-pick qua worktree; STATUS conflict resolve = take theirs + sửa header)

## Phiên 2026-07-12 (cửa 368-authz2 — #368 AUTHZ-2 backend, DONE-code CHỜ push)
- **#368 [AUTHZ-2][P1] backend increment DONE** (lock `368-authz2`): RefreshTokens thật + rotation + reuse-detection + SecurityStamp revoke tức thời. **100% backward-compatible** (token cũ không có claim → grace-accept; LoginResponseDto giữ nguyên shape).
  - **Migration `144_authz2_refresh_tokens_security_stamp.sql`**: cột `Users.SecurityStamp` (+backfill NEWID) + bảng `RefreshTokens` (TokenHash SHA-256 NVARCHAR(128) indexed, ExpiresAt 14d, RevokedAt, ReplacedByTokenHash; audit NVARCHAR(450) tránh Guid-converter trap). Idempotent.
  - **`RefreshTokenService`** (mới, DI-registered): Issue/Rotate/Revoke/RevokeAllForUser — rotation mỗi lần refresh; reuse token đã revoke → revoke cả family + bump stamp. Ghi/đóng `UserSessions` thật (M17 admin có data).
  - **`AuthService`**: 3 luồng login (thường/OTP/WebAuthn) cấp refresh thật + claim `securityStamp`; `ChangePasswordAsync` xoay stamp + revoke all; `RefreshTokenAsync`/`LogoutAsync` mới.
  - **`Program.cs OnTokenValidated`**: check stamp qua **DB + IMemoryCache TTL 30s** (KHÔNG Redis — prod không có Redis, xem ADR); fail-open khi DB lỗi; giữ nguyên `OnMessageReceived` (SignalR/print/PACS). Limiter `refresh` riêng (60/min) không ăn quota login.
  - **Endpoints:** `POST /api/auth/refresh` (AllowAnonymous + ratelimit) · `POST /api/auth/logout` (revoke đúng thiết bị, KHÔNG đá thiết bị khác).
  - **ADR:** `docs/architecture/adr/ADR-001-authz2-token-storage-and-revocation.md` (no-Redis stamp cache · giữ TTL 480' đến khi FE auto-refresh · localStorage + defer httpOnly cookie).
  - **⚠️ FE part DEFER** (interceptor auto-refresh single-flight + AuthContext lưu refreshToken + logout gọi API + rút TTL 15-30'): bị chặn bởi cửa `components-restructure` đang dời `frontend/src/api/*` — handoff đầy đủ ở comment #368. #368 GIỮ OPEN.
  - **Gate:** `dotnet build` 0 error ×2 (sau fix `Headers["User-Agent"]`). Push = auto-deploy Cloud Run; sau deploy check `GET /health/schema-drift` missingCount=0.

## Phiên 2026-07-11 (cửa components-restructure — wave-3 pages-v2 subdir move, DONE-code CHỜ push)
- **Wave-3 STRICT-relocate pages-v2 subdir → modules/*/pages/ DONE** (6 subdir, 46 file, resolve-based):
  - `inpatient/` (8) → `modules/inpatient/pages/` · `laboratory/` (6) → `modules/laboratory/pages/`
  - `opd-editor/` (5) → `modules/opd/pages/` · `radiology/` (8) → `modules/radiology/pages/`
  - `reception/` (12) → `modules/reception/pages/` · `system-admin/` (7) → `modules/system/pages/`
  - 6 parent pages rewritten (36 specifier) · tất cả internal cross-dir imports recalculated (`../_v2kit` → `../../../pages-v2/_v2kit` etc.)
- **Fix đi kèm:** restore 3 barrel index.ts (EMRPrintTemplates/SpecialtyEMRForms1/SpecialtyEMRForms2 bị trunc BOM-fix) · re-apply wave-2 cho 8 pages-v2 file mới từ parallel window (16 specifier)
- **Gate:** `tsc -b --noEmit` EXIT 0 · verify-wave2 A) 107/107 PASS · B) 2607 specifiers PASS
- **Deferred:** `pages-v2/dashboard/` (cross-module) · `pages-v2/shared/` (giữ chỗ, dùng bởi nhiều module)
- **✅ PUSHED `49e8183`** (2026-07-12, user duyệt): cherry-pick qua worktree cô lập tại origin/main (conflict 8 trang nghiệp vụ = origin content + retarget modules-import, resolve lấy bản mình; STATUS merge tay), gate worktree tsc EXIT 0 + vite build EXIT 0. ⚠️ Race index chung tái diễn khi commit (nuốt 15 file backend cửa 368) → đã reset-soft + recommit sạch; bài học ghi memory `parallel-window-push-worktree`.
- **Carve tiếp (LOCAL `3d96be2` chờ gom push):** hook `useSafetyAlerts` → `modules/patient/hooks/` (bọc businessAlerts api; rewrite 3 importer v1 + gỡ barrel; tsc EXIT 0). Candidate còn lại: `constants/specialtyEmr.ts` → emr (template module KHÔNG có constants/ — cần user quyết thêm subdir hay giữ); `vgcaSign`/AI/money = giữ theo quyết định cũ/DEFER.
- **Việc kế (cần user quyết):** wave-4 = dời 132 trang `pages-v2/*.tsx` → `modules/*/pages/` + rewrite router `lazy/` (blast radius lớn, đề xuất làm theo batch domain).

## Phiên 2026-07-11 (cửa 409-nghiep-vu — #409 8 trang nghiệp vụ, DONE-code CHỜ push)
- **#409 [PORT-P3][FE] sub-scope nghiệp vụ DONE-code** (lock `409-nghiep-vu`): port 8 trang `pages-v2/`:
  - **EnvironmentalHealth**: `Promise.allSettled` 3 stats API + KpiStrip server + `DatePicker.RangePicker`.
  - **FollowUp** (+264 dòng): TopTabs 4 scope (today/upcoming/overdue/all) + RangePicker + Filter status/type + `handleUpdateStatus` (confirm-arrived status=2 / no-show status=3) + server pagination `searchAppointments` + `getOverdueFollowUps(30)`.
  - **Pathology** (+240 dòng): `ResultModal` 10 field + `createPathologyResult` + `printPathologyReport` PDF blob + `getPathologyStatistics` + RangePicker + 5-state StatusTabs.
  - **Procurement** (+4/-4): fix import path `'../api/warehouse'` → `'../modules/pharmacy/api/warehouse'`.
  - **TreatmentProtocol** (+34 dòng): `approveProtocol`/`createNewVersion` + `cf()` confirm + dept filter + fix duplicate ActBtn + `openPrintWindow` thay `window.print()`.
  - **FoodSafety** (+430 dòng): TopTabs 2 tab — tab "Vụ ngộ độc" (giữ nguyên) + tab "Thanh kiểm tra" A/B/C/D grading + CrudModal 9 field + DataTable + DrawerShell.
  - **ReproductiveHealth** (rewrite +617 dòng): TopTabs 2 tab — "Quản thai" (KpiStrip 4 tile + StatusTabs + CrudModal 10 field + `getHighRiskPregnancies`) + "KHHGĐ" (StatusTabs + Filter method + CrudModal 8 field).
  - **HealthCheckup** (+685 dòng): TopTabs 2 tab — "KSK" (giữ nguyên) + "Chiến dịch KSK" (`CampaignTab`: CRUD campaign + groups sub-panel + Excel import `importBatchExcel`).
  - **⚠️ Retarget import về `../api/*` cũ (prod-safe):** ground-truth git — restructure api→`modules/` cho 7 domain (public-health/opd/pathology/pharmacy-warehouse/patient-treatmentProtocol/specialty/checkup) **CHƯA committed trên origin** (file `modules/*/api/*` untracked, WIP cửa `components-restructure`); origin vẫn giữ `api/*.ts` cũ. Push 8 page trỏ `modules/*/api/*` → **vỡ Vercel prod build**. Đã verify 8 module-api file **byte-identical** old api (chỉ khác import-depth) + export ⊇ nhu cầu → **retarget api-import 8 page về `../api/<name>`** (path origin có). Giữ `HealthCheckupPrintTemplates` ở `modules/patient/components/` (đã trên origin). Tự-lành khi restructure land (script rewrite-importer của cửa đó migrate tiếp).
  - **Gate:** `tsc -b --noEmit` EXIT 0 · verify **worktree cô lập tại origin/main** (old api có mặt = trạng thái Vercel thực) — CẢ def00b7 SystemAdmin + 7 page resolve.
  - **✅ PUSHED `302cad6`** (fast-forward `def00b7..302cad6`): 7 page (Procurement không đổi) + **5 file api cửa 409-sysadmin QUÊN `git add`** (`modules/system/api/{security,itTicket,audit}` + `modules/administration/api/dataExport` + `modules/emr/api/emrAdmin`) — def00b7 import chúng nhưng untracked → origin vỡ build; đã bổ sung (byte-identical old api). Chi tiết: comment #409.
  - **⚠️ Vercel deploy `302cad6` = "Deployment was blocked"** (spend-cap/quyền account chủ minhhung19872002 — KHÔNG phải build fail; def00b7 cũng blocked "author quangngoc1911 no access"; 7c37939/960a067 success intermittent). Code tsc-xanh, chờ owner gỡ block hoặc deploy từ account chủ (memory `reference_vercel-his-psi-other-account`).

## Phiên 2026-07-11 (cửa 409-sysadmin — #409 SystemAdmin v2 6 tab mới, DONE-code CHỜ push)
- **#409 [PORT-P3][FE] sub-scope SystemAdmin DONE-code** (lock `409-sysadmin`): mở rộng SystemAdmin.tsx từ 8→14 tab bằng 6 panel mới trong `pages-v2/system-admin/`:
  - **helpers.ts**: `getNestedData<T>`, `toStringValue`, `toNumberValue`, `RawApiItem`
  - **ItTicketsPanel**: KpiStrip (4 chỉ số) + filter priority/status + SearchBox + DataTable + DrawerShell chi tiết + ModalShell tạo + ModalShell phản hồi + `cf()` đóng ticket. API: `modules/system/api/itTicket`.
  - **AccessMatrixPanel**: DataTable vai trò + DrawerShell liệt kê quyền theo module dạng StatusBadge chips. API: `modules/system/api/security`.
  - **CompliancePanel**: KpiStrip 8 chỉ số ATTT + DatePicker.RangePicker + DataTable + DrawerShell nested DataTable truy cập nhạy cảm. API: `modules/system/api/security`.
  - **DataManagementPanel**: KpiStrip 6 chỉ số + DataTable module counts + grid backup/export + DataTable handover. API: `modules/administration/api/dataExport`.
  - **HealthPanel**: interval 30s với `active` prop pause khi tab inactive (useRef + useEffect[active,fetch]) + KpiStrip + component health cards grid + DataTable HTTP codes + DataTable top endpoints. API: `api/health.ts` (KHÔNG relocate).
  - **EmrAdminPanel**: TopTabs 6 sub-type + shared ModalShell Form switch-case + DataTable + `cf()` delete. API: `modules/emr/api/emrAdmin`. Pure CRUD, không interval.
  - **SystemAdmin.tsx**: type AdminTab mở rộng 8→14; TABS thêm 6 mục; load() `else`→`else if (tab==='audit')` (chống audit fallthrough); render blocks 6 tab mới.
  - **4 fix regression (phiên này):** ItTicketsPanel debounce 400ms (`useDebounce` thay per-keystroke) · DataManagementPanel xóa `.slice(0,5)` backup+exportHistory · EmrAdminPanel thêm `cover` vào điều kiện description · HealthPanel CSS tokens (`--brd`→`--line`, `--radius`→`--r-2`, `--srf`→`--d-1`, `--crit`→`--s-crit`).
  - **Gate:** `tsc -b` EXIT 0 · `npm run build` (tsc -b && vite build) BUILD_EXIT:0. 7 file mới + SystemAdmin.tsx.
  - **CHỜ user duyệt push** (`git add` tường minh 8 file: `pages-v2/SystemAdmin.tsx` + `pages-v2/system-admin/{helpers,ItTickets,AccessMatrix,Compliance,DataManagement,Health,EmrAdmin}Panel.tsx` + `STATUS.md` → commit `Refs #409`).

## Phiên 2026-07-11 (cửa PORT w196 — #415+#416 ✅ CLOSED, #412 AiQueueBadge DONE-code CHỜ push)
- **#415 [PORT-P9][FE][P2] ✅ PUSHED `be63a78` CLOSED**: QueueDisplay TV board v2 — TTS + beep + lab queue 5-status. Route `/v2/queue-display` standalone public. Lock released.
- **#416 [P1][patient-safety] ✅ PUSHED `aec7178` CLOSED** (dead window w669 đã hoàn tất): race-guard `selectPatient` BillingEditor + PrescriptionEditor (pattern #374). Đã pull vào main khi push #415.
- **#412 [PORT-P6][FE][P3] DONE-code** (lock `412`, gh in-progress + @me): AiQueueBadge → TerminalLayout topbar v2 (2 dòng thêm vào `layouts/terminal/TerminalLayout.tsx`):
  - Import `AiQueueBadge from '../../modules/radiology/components/AiQueueBadge'`
  - `<AiQueueBadge />` trước bell Popover trong `his-tb-right` (reuse component tự chứa — poll 30s + SignalR relay `ai-queue-updated`; navigate `/radiology/viewer?study=`).
  - **Gate:** tsc EXIT 0. **CHỜ vite EXIT 0 → git add `TerminalLayout.tsx STATUS.md` + commit `Closes #412` → push.**

## Phiên 2026-07-11 (cửa 416 — #416 race-guard BillingEditor+PrescriptionEditor, DONE-code READY_FOR_PUSH)
- **#416 [P1][patient-safety] DONE-code** (lock `416`): race-guard `selectPatient` 2 editor v2 cùng pattern `selectReqRef` của #374 OpdEditor:
  - **PrescriptionEditor**: guard `?patientId=` preload (MAJOR — `selectPatient(preloadPt)` với FRESH counter có thể đè manual pick) + clear `items[]`+`interactions[]` synchronously khi chọn BN (toa BN-A không leak sang BN-B) + guard 2 await của `selectPatient` + `?examId=` preload 2-point.
  - **BillingEditor**: guard `selectPatient` allSettled + lazy `getPatientDeposits` + `ensureTab` refund + `doPayment` tail snapshot + `saveCreate` deposit/refund post-create refresh snapshot + QR-deposit `onSuccess` snapshot.
  - Verify đối kháng 3 lens (workflow): coverage+interleaving FAIL → 5 defect bổ sung → đã fix (R2); behavior PASS.
- **Build-gate:** `tsc -b --noEmit` EXIT 0 sau R2. **READY_FOR_PUSH** — chờ `git add` 2 file `BillingEditor.tsx PrescriptionEditor.tsx` + `Fixes #416` + push.

## Phiên 2026-07-10 (cửa PORT w480 — #415 QueueDisplay TV board v2, DONE-code READY_FOR_PUSH)
- **#415 [PORT-P9][FE][P2] DONE-code** (lock `415`, gh in-progress + @me): tạo `pages-v2/QueueDisplay.tsx` (~590 dòng) — **port VERBATIM từ v1** (diff cơ học: helpers + LabQueueView + RoomQueueView = 485 dòng identical, chỉ khác comment tiêu đề): TTS `vi-VN` rate 0.9 + beep AudioContext 880Hz/0.2s + audio-unlock overlay + fullscreen btn + blink 5s + first-poll guard ref + polling 4s.
  - **Route:** `/v2/queue-display` standalone PUBLIC trong `router/AppRoutes.tsx` (ngoài TerminalLayout/ProtectedRoute — TV board không auth, giống v1 `/queue-display`; backend đã mask tên BN #406). Không xung đột `/v2` layout (routeConfigs không có `queue-display`).
  - **Param:** `?type=lab` → LabQueueView (5 trạng thái màu riêng + emergency/priority class + KPI 4 chỉ số) · `?type=general`/default → RoomQueueView (`?rooms=id1,id2&queueType=`); chấp nhận `?mode=` legacy URL v1.
  - **KHÔNG port:** Kiosk (v2 đã có KioskSelfService) · ZoneQueueView (ngoài AC #415, giữ ở v1).
  - **CSS:** reuse `styles/QueueDisplay.css` (zero visual drift) + override scoped `.queue-display.v2 .queue-ticket-number{font-size:104px}` đáp ứng AC 96–120px (v1 giữ 72px).
  - **Gate:** tsc -b EXIT 0 · npm run build EXIT 0. **CHỜ user duyệt push** (`Closes #415`; git add tường minh 4 file: pages-v2/QueueDisplay.tsx + router/AppRoutes.tsx + styles/QueueDisplay.css + STATUS.md; ⚠️ RE-CHECK `git diff --cached --name-only` NGAY SÁT commit — index chung đã gây race `9c3b762`).

## Phiên 2026-07-10 (cửa PORT #414 w480 — print gap v1→v2 7 module, ✅ PUSHED Closes #414)
- **#414 [PORT-P8][FE][P1] DONE + PUSHED**: bổ sung print cho 7 module v2 theo pattern `openPrintWindow(html, {focus, print:{delayMs:500}})` + `HOSPITAL_NAME`:
  - **Surgery**: "In phiếu PT/TT" — reuse v1 `pages/surgery/printTemplates.buildSurgeryRecordHtml` (MS:06/BV-02), map từ SurgeryDto.
  - **BloodBank**: "In nhãn đơn vị máu" (BloodStockDetailDto — nhóm máu/Rh lớn, bagCode, HSD, cảnh báo 2-6°C) + "In phiếu yêu cầu truyền máu" (BloodIssueRequestRow).
  - **Quality**: "In phiếu báo cáo sự cố" — `buildIncidentReportHtml` 6 section (thông tin/mô tả/xử lý/điều tra RCA/phòng ngừa/bài học) + 2 chữ ký.
  - **HR**: "In hồ sơ" — `buildEmployeeCardHtml` (field thiếu để trống điền tay — StaffMember chỉ có id/name/role/department/quota).
  - **EmergencyDisaster**: thay `window.print()` → `buildEmergencyCaseHtml` (phiếu tiếp nhận CC: sinh hiệu/GCS/xử trí ban đầu/3 chữ ký) + **"In báo cáo MCI"** toolbar (port v1 handlePrintReport: event info + thống kê triage ĐỎ/VÀNG/XANH/ĐEN + DS nạn nhân; fetch victims fresh khi in).
  - **Inpatient**: "In phiếu" (tóm tắt điều trị nội trú từ InpatientListDto) + **"In bệnh án"** — reuse v1 `pages/inpatient/printTemplates.buildMedicalRecordHtml` (MS 01/BV-01 nội khoa default, field sẵn pre-fill, còn lại dòng chấm điền tay). Tờ điều trị = ĐÃ CÓ sẵn API-blob (`printTreatmentSheet` TreatmentSheetsModal) — không phải gap.
  - **Reports**: "In báo cáo" (card + drawer footer) — `handlePrintReport` tải PDF blob `/reporting/export/pdf/{id}` mở tab mới (revoke URL sau 60s).
- **Gate**: tsc -b EXIT 0 + npm run build EXIT 0. **P3 (Telemedicine/Equipment/HealthExchange/InfectionControl/Nutrition…) = DEFER theo chính AC của issue** ("đưa vào sprint sau hoặc xử lý khi port module đó") — các trang này đã có `window.print()` cơ bản.
- Kế tiếp: **#415** QueueDisplay TV board v2 (P2, chưa claim).

## Phiên 2026-07-10 (cửa FE-restructure — Batch 6 API STRICT-relocate: XÓA HẲN file cũ, KHÔNG barrel — ✅ PUSHED `6f49492`)
- **User đổi strategy giữa chừng:** barrel tạm → **STRICT-relocate ngay** (xóa hẳn 37 file `api/<domain>` + 2 thư mục `api/ris/` `api/system/`, rewrite TOÀN BỘ importer kể cả v1 — exception v1 được user duyệt turn này).
- **Đã move (3 agent phiên trước + hoàn tất tay):** 37 file → `modules/{billing 5, pharmacy 3, radiology 4+11, system 3+6, patient 2, administration 3}/api/`. **Audit content-equivalence bằng diff máy vs git HEAD: 37/37 verbatim** (chỉ khác dòng import-depth), 0 thiếu, 0 đổi logic.
- **Rewrite importer:** script Node resolve-based (không blind prefix-replace) — **122 specifier / 84 file** (v1 pages 33 · v2 43 · contexts/services 2 · intra-api `nangcap25.ts` 1 · modules cross-module 5); idempotent (re-run = 0); CRLF nguyên vẹn. 11 module-component đã sửa tay trước đó dùng `'../api/x'` nội-module — script tự skip (resolve đúng đích mới).
- **Gate:** tsc scope-clean (3 lỗi còn lại = WIP cửa port-408 Immunization/MedicalForensics, không liên quan) · **vite build EXIT 0** · stale-ref sweep toàn repo (src + cypress/playwright/scripts/config + mocks) = **ZERO** · **Workflow verify đối kháng 8 agent: 8/8 PASS, 91 file checked, 0 vấn đề thực** (info: `modules/patient/index.ts` barrel rỗng 0-byte scaffold — vô hại; 7 dynamic import OK).
- **STAGED tách cửa (3 cửa sống cùng cây):** đã `git add` chính xác scope mình (128 file staged, git nhận 37 move = rename giữ history); **4 file overlap với cửa #414** (BloodBank/Inpatient/Quality/Reports v2) stage bằng **patch-filter CHỈ hunk import** (`git apply --cached`) — WIP print-gap của họ vẫn unstaged nguyên; 14 file WIP cửa 408/414 khác không đụng. Verify staged-tree tự nhất quán: HEAD/STAGED của mọi file skip = 0 import path cũ.
- **READY_FOR_PUSH** — chờ user duyệt commit+push. ⚠️ Lưu ý cửa #414/#408: file các bạn khi commit sẽ tự mang theo import mới (đã sửa trong worktree) — KHÔNG `git checkout --` file của mình về HEAD (HEAD còn trỏ path đã xóa).

## Phiên 2026-07-10 (cửa port-408 — ✅ #408 CLOSED: 12/13 stub→CRUD lên origin, DoctorPortal → #417)
- **PUSH + CLOSE (user ra lệnh tường minh "push và đóng task"):** 10 file pages-v2 lên origin — ⚠️ đi **CHUNG commit `9c3b762` của cửa #414** (race staging: cửa #414 `git commit` toàn bộ index đúng lúc 10 file này vừa stage). Nội dung code verify TRƯỚC khi stage: `tsc -b` EXIT 0 + `vite build` EXIT 0 toàn cây (sau batch-6 api relocate). #408 đóng thủ công kèm sha + giải thích.
- **DoctorPortal (trang 13)** → tách **#417** (gate quyết định sản phẩm: giữ portal hay bỏ; đã chuyển đủ context v1 4 khối + acceptance).
- Lock `port-408` ĐÃ RELEASE. Locks còn sống: `415` (w8529 fable) · `components-restructure` (w1262 opus) — cửa khác, không đụng.

## Phiên 2026-07-10 (cửa port-408 — #408 P2 stub→CRUD: 10 trang DONE-code, tsc EXIT 0, CHỜ duyệt commit+push)
- **#408 [PORT-P2] DONE-code** (lock `port-408`): upgrade 10 stub → full CRUD+v2kit:
  SmsManagement (Dashboard+Logs+Tools) · SchoolHealth (CrudModal 19 field+StatusTabs) · ChronicDisease (StatusTabs 3 trạng thái) · HivManagement (4 TopTabs + 90-90-90) · TbHivManagement (StatusTabs 6+print) · OccupationalHealth (TopTabs+hazard filter) · **MedicalForensics** (CrudModal+approve+exams drawer) · **Immunization** (4 TopTabs, xóa antd import) · **CommunityHealth** (3 TopTabs + NCD 14 field) · **MedicalSupply** (2 TopTabs + approve/cancel receipt). DoctorPortal = SKIP (quyết định sản phẩm).
- **Build-gate:** `tsc -b --noEmit` EXIT 0 toàn project. Spot-check side-effects: 0 setInterval/setTimeout/subscribe.
- **READY_FOR_PUSH** — chờ user `git add` tường minh 10 file pages-v2 + `commit "Refs #408"` + push.
- **Khi commit**: `git add` đúng 10 file: SmsManagement · SchoolHealth · ChronicDisease · HivManagement · TbHivManagement · OccupationalHealth · MedicalForensics · Immunization · CommunityHealth · MedicalSupply. TUYỆT ĐỐI không stage BloodBank/Surgery (lock #414) hay component-restructure files.

## Phiên 2026-07-10 (cửa fix-batch — luộc 6 issue P0/P1/security, LOCAL build-green CHỜ user duyệt commit+push)
- **Claim 6 issue** (in-progress + assignee): #373 #374 #401 #406 #402 #403. Test-label (#191, #216-347…) để CUỐI theo HARD RULE; #408/#414 máy khác giữ — không đụng.
- **#373 [P0] ErrorBoundary v2 Outlet** DONE-code: reuse `components/feedback/ErrorBoundary` (nâng: subTitle hiện `error.name: message` + nút "Quay lại" = reset+history.back), wrap `<Outlet/>` trong TerminalLayout với `key={location.pathname}` (lỗi tự reset khi điều hướng).
- **#374 [P0 patient-safety] race đổi BN nhanh** DONE-code: `selectReqRef` counter-ref guard trong `OpdEditor.selectPatient()` — check sau `Promise.allSettled`, response cũ bị bỏ. `PatientContextBar.tsx` CHƯA TỒN TẠI (issue viết trước #376) → không áp. ⚠️ Cùng race còn ở `PrescriptionEditor.selectPatient` + `BillingEditor.selectPatient` → issue follow-up mới (không mở scope #374).
- **#401 [security] ZNS templates** DONE-code: bỏ `[AllowAnonymous]` trên `GET zalo-notification/templates` (class có `[Authorize]`; FE gọi qua apiClient có JWT → không vỡ).
- **#406 [security MEDIUM] mask tên BN queue-display** DONE-code: helper mới `HIS.Core/Common/NameMask.cs` (`Nguyễn Văn An`→`Nguyễn V. An`); áp 5 endpoint anonymous: Examination `waiting-room/{roomId}` + `waiting-rooms/department/{id}` · Reception `queue/display/{roomId}` + `queue/calling/{roomId}` (mask tên + null PatientId/PatientCode) · LIS `queue/display` (mask tên + null PatientCode). Giữ số thứ tự/trạng thái. (2 bản MaskName private cũ KioskService/PublicEmrLookupService để nguyên — dedup sau.)
- **#402 [security HIGH] PACS proxy + RIS PDF** DONE-code: (a) bỏ `[AllowAnonymous]` 3 endpoint `pacs/instances/{id}/preview|rendered|file`; (b) Program.cs OnMessageReceived nhận JWT `?access_token=` cho path `/api/RISComplete/pacs` (mẫu SignalR/print sẵn có — `<img>`/wadouri không gắn header được); (c) FE `DicomViewer.resolveApiUrl` đính `access_token` vào URL `/pacs/instances/`; (d) `pdf/download/{fileName}` bỏ anonymous + chặn path-traversal (GetFileName + `..` + .pdf-only + GetFullPath prefix); (e) tên file PDF `PdfSignatureService` thêm GUID suffix (chống brute-force timestamp). PublicStudyViewer không ảnh hưởng (Orthanc direct).
- **#403 [P1 commercial] tìm BN không dấu** DONE-code: migration **143_patient_search_accent_ci_ai.sql** — collation `Patients.FullName/PhoneNumber/IdentityNumber` → **`Latin1_General_CI_AI`** (KHÔNG phải Vietnamese_CI_AI như issue gợi ý — **verify thật trên SQL Server 2022 container**: Vietnamese_CI_AI trượt Đ/đ `N'Đức'=N'Duc'`→0; Latin1 pass cả ê/đ/ươ). Script động đọc sys.columns (không hard-code kiểu), idempotent (chạy 2 lần OK), test e2e DB giả lập: "nguyen van an"→"Nguyễn Văn An" · "do van dung"→"Đỗ Văn Dũng" · "duc"→"Lê Đức Anh" ✓. ~25 điểm `FullName.Contains()` hưởng lợi, 0 code change. Sau deploy: `/health/schema-drift`=0 + smoke search.
- **Build-gate:** FE `npm run build` EXIT 0 (×2) · BE `dotnet build` 0 error. File: FE 4 (ErrorBoundary/TerminalLayout/OpdEditor/DicomViewer) · BE 8 (NangCap23Controllers · NameMask mới · ExaminationComplete · ReceptionComplete · LISComplete.SubModules · RISComplete.Reports · RISComplete.Signing · Program + PdfSignatureService) · SQL 1 (143).
- **READY_FOR_PUSH** — chưa commit/push (turn không có keyword). Push backend → auto-deploy Cloud Run; smoke cần: DicomViewer load ảnh (access_token flow) · màn TV queue tên rút gọn · search không dấu.

## Phiên 2026-07-10 (cửa PORT-v1→v2 — ✅ PUSHED theo lệnh user "push toàn bộ": #410+#411 CLOSED + gom batch-5 remnant + MH/Screening)
- **PUSH tổng (user ra lệnh tường minh):** 6 commit lên origin/main — `8d2353b` (batch-3+4, cửa FE-restructure) · `e586dd9` (cửa này HOÀN TẤT remnant batch-5: 4 component money/system → modules + **4 shim v1 bị thiếu** — cây lúc đó vỡ build vì MainLayout+pages/Billing trỏ path cũ đã xóa) · `759f6ce` **Closes #410** · `d641368` **Closes #411** · `a73df9e` MentalHealth+Screening stub→CRUD (WIP cửa port-408, gom theo lệnh "toàn bộ", Refs #408) · `b9f3a72` (batch-5, cửa FE-restructure commit lúc 12:47). Gate trước push: tsc EXIT 0 + vite build EXIT 0 (toàn cây, tự chạy lại sau b9f3a72). KHÔNG gom: `.continue/`/`bash.exe.stackdump`/`test.txt` (junk/ghi chú cá nhân user).
- ⚠️ **Điều phối 3 cửa sống cùng cây:** `components-restructure` (w1262 Opus) + `port-408` (w1377 Sonnet) đang chạy song song lúc push — đã đợi index sạch (cửa kia commit xong batch-5) mới commit docs, KHÔNG đụng index/STATUS đang stage của họ. Cửa port-408 lưu ý: MentalHealth/Screening đã lên origin ở `a73df9e` (snapshot build-green giữa chừng) — tiếp tục sửa bình thường, KHÔNG cần revert.

## Phiên 2026-07-10 (cửa PORT-v1→v2 — audit vòng-3 + task tạo)
- **Audit vòng-3 đa chiều HOÀN TẤT** [read-only]: rà 4 chiều bổ sung sau vòng-2:
  - **Chiều 1 Print:** modules core (Billing/Lab/Reception/OPD/Radiology/EMR/Prescription/Pharmacy) dùng API-blob → KHÔNG phải gap. Gap thật: Surgery(7→0) · BloodBank(12→0) · HR(5→0) · Reports(7→0) + partial Inpatient/EmergencyDisaster/InfectionControl/Nutrition → **#414 MỚI tạo**.
  - **Chiều 2 Lite routes:** 12 route `/v2/lite/*` → cùng component với `/v2/*` → KHÔNG gap.
  - **Chiều 3 Cross-module links:** v2 có 30+ link giữa modules (v1 gần 0) → v2 TỐT HƠN, không gap.
  - **Chiều 4 Kiosk/QueueDisplay:** KioskSelfService v2 tốt; nhưng QueueDisplay TV board (màn chờ công cộng: TTS vi-VN + beep + lab queue 5-status + KPI) chưa có v2 → **#415 MỚI tạo**.
- **#414** [PORT-P8][FE][P1] tạo: print gap Surgery/BloodBank/HR/Reports v2 (0 print vs v1 5-12).
- **#415** [PORT-P9][FE][P2] tạo: QueueDisplay TV board v2 (TTS + beep + lab queue 5-status).
- **Comment #352:** audit vòng-3 full summary + bản đồ task #407-#415.
- **Comment #409:** BHYT XML export (C79/C80) = critical sub-scope còn thiếu trong v2 Insurance.

## Phiên 2026-07-10 (cửa PORT-v1→v2 — #410 DONE-code chờ push + #411 DONE-code chờ push)
- **#411 [PORT-P5][FE][P2] DONE-code, tsc EXIT 0, CHỜ push** [feature FE, claim w1131]: `services/menu.service.ts` — thêm **24 mục** vào đúng group (128→152 item): clinical +4 (observation-stay/video-consultation/service-requeue/consultation-register) · paraclinical +8 (analyzer-inbox/sample-receive/radiology-ops/ris-dispatcher/non-dicom-capture/ris-admin/ris-catalog-admin/lis-catalog-admin) · support +5 (dispensing-counter/inpatient-dispensing/clinical-pharmacy-check/stock-report/office-supply-approval) · finance +4 (bhxh-config/payment-transactions/payment-reports/receipt-book-admin) · management +3 (catalogs-admin/employee-profile/workload-report). Labels lấy từ meta.title routeConfigs (không sáng tạo). tsc -b EXIT 0 (type-only change). Palette Ctrl+K tự ăn theo HIS_GROUPS → 14 trang mất-hẳn-lối-vào nay accessible. **Push cùng #410 hoặc riêng lẻ theo user.**

## Phiên 2026-07-10 (cửa PORT-v1→v2 w793 — dọn bảng task nhóm port + #410 P4-tail DONE-code, CHỜ duyệt push)
- **Dọn bảng task loại "port v1→v2"** [governance]: nhóm này 0 issue open (epic #352+#204 bị đóng 2026-07-03 đợt dọn backlog, chưa làm xong). → **Reopen epic #352** (comment cập nhật: #357/#361/`b075c03` đã xong từ khi đóng) + vật-chất-hóa kế hoạch 4 batch thành issue claimable: **#407** PORT-P1 patient-safety residual (OPD/Prescription/EMR/Inpatient/Pharmacy/HospitalPharmacy/Insurance/MRPlanning — cần deploy+smoke) · **#408** PORT-P2 13 stub no-CRUD (re-verify line-count: vẫn stub) · **#409** PORT-P3 Partial còn lại ~40 trang · **#410** PORT-P4 tail. #204 giữ CLOSED tới khi P1-P4 xong.
- **#410 [PORT-P4] DONE-code, build-green, CHỜ push** [feature FE, claim + window-lock 410 đã release]: 3 file `pages-v2/` — (1) **HealthEducation**: TopTabs Chiến dịch/Tài liệu + tab Tài liệu (list + create qua CrudModal→`createMaterial`) + RangePicker lọc ngày (fromDate/toDate như v1) + fix nợ ActBtn lặp; (2) **SampleTracking**: TopTabs Từ chối/Theo đợt/Thống kê + tab Theo đợt (`getSampleBatches`) + tab Thống kê (`getSampleTrackingSummary`) + modal Từ chối mẫu (8 mã lý do→`rejectSample`) + Timeline drawer (`getSampleTimeline`) + Quét barcode (reuse `components/BarcodeScanner` — import ĐƯỜNG DẪN CŨ đi qua shim của cửa FE-restructure, an toàn mọi thứ tự push); (3) **Help**: mở bài → `getHelpArticle(id)` content đầy đủ + viewCount (parity v1). `npm run build` EXIT 0 (53.77s). Chi tiết: comment #410.
- **Push được duyệt** → `git add` tường minh 3 file pages-v2 (HealthEducation/SampleTracking/Help) + commit `Closes #410`; KHÔNG đụng file cửa FE-restructure (components/*, .claude, .gitignore…).
- **Audit vòng-2 "còn gì chưa chuyển v1→v2 ngoài tầng trang"** [read-only, theo yêu cầu user]: rà 6 chiều A-F → tầng trang/route KÍN (149/149, 72 PORT chia đủ); 🔴 3 gap mới → issue: **#411** menu v2 thiếu 23 trang v1-menu (14 mất hẳn lối vào — palette cũng đọc HIS_GROUPS; phối hợp #375) · **#412** AiQueueBadge chỉ có ở MainLayout · **#413** DicomViewer v2 = wrap import v1 cuối cùng (chặn Phase-3 sunset). +8 feature-component chỉ-v1 bổ sung scope qua comment #407 (ClinicalTermSelector/DoctorLicenseBanner/VoiceDictation/StockReservationModal/PatientTimeline/BirthCertificatePrint) + #409 (WebcamCapture/SampleSequenceToolbar); LabCancelChainMenu không cần (v2 có bản riêng). Chi tiết + bằng chứng = §6 `10-assessment/v1-v2-conversion-inventory.md` (sửa local, chưa commit) + comment tổng hợp trên #352. **Bản đồ nhóm port đầy đủ: #407-#413.**

## Phiên 2026-07-10 (cửa FE-restructure — RULE dùng-chung/dùng-riêng + Batch 1 components, LOCAL chờ duyệt)
- **Timer hooks ✅ PUSHED `69f0ebb`**: useInterval/useTimeout/useDebouncedCallback + migrate v2 (TerminalLayout clock + QualityDashboardLive poll).
- **RULE component placement** [governance]: user yêu cầu "ghi vào rule để sau này chuyển dần + tuân thủ dùng chung/dùng riêng" → owner = `his-fe-convention` **§4a** (A dùng-chung=primitive generic→`components/<category>/` 12 category; B dùng-riêng=domain→`modules/<module>/`; C local=1-page; decision domain?→B, generic≥2?→A, else C; move gradual+behavior-preserving+build-gate; DEFER money/external/patient-safety) + REGISTRY row mới + §4 bảng folder + §5 reuse-first trỏ §4a. `bash .claude/lint.sh` LINT OK. Memory project_fe-folder-restructure cập nhật (census ~40 file root: hầu hết B; cross-cutting: BusinessAlertPanel 8/PatientFlagBanner 10/BarcodeScanner 7/PharmacyExpiryBanner 7).
- **Batch 1 chuyển dần** [refactor FE, LOCAL]: 4 file generic thuần (đã verify không domain-logic) — `ErrorBoundary`→`feedback/ErrorBoundary/` (importer MainLayout update 1-dòng, KHÔNG shim) · `BarcodeScanner`→`form/BarcodeScanner/` (shim cũ cho 6 v1; v2 OpdEditor update) · `VoiceDictation`→`form/VoiceDictation/` (shim, 4 v1) · `WebcamCapture`→`upload/WebcamCapture/` (shim, 1 v1). git mv giữ history (4 rename 0-diff) + index.ts per-component + 3 category barrel (feedback/form/upload). tsc 0 · vite ✓ 55s · content-equiv (chỉ CRLF). **v1 pages/ 0 file bị đụng.**
- **Kết luận census:** root còn lại toàn B (domain→modules, user tự carve: prints/viewers/AI/money-modals/banners/lab) hoặc DEFER — không còn file A nào để move. Shim xóa khi v1 retire.
- **Batch 1 + governance ✅ PUSHED** (`c28a6fc` + `a20f2e5`, user duyệt).
- **Batch 2 — tách `_v2kit` primitive → `components/<category>/`** [refactor FE, LOCAL build-green]: `_v2kit.tsx` 993→~370 dòng, thành **barrel + page-glue**. Primitive dời (verbatim): actions/{Btn,ActBtn} · dataDisplay/{KpiStrip,StatusBadge,Loading,EmptyState,ErrorState} · navigation/{Tabs(TopTabs+StatusTabs),Pagination(Pager)} · table/DataTable · form/{SearchBox,Filter,Options(4 field cfg-driven+AbSelect+normalizeOptions),applyServerErrors} · overlay/{DrawerShell(+DrSec/DrField),ModalShell,CrudModal,usePopup nội-bộ}. GIỮ ở _v2kit (page-glue): SimpleV2Page/useListData/useTabCounts/makeStatus/fmt*/tk-ti-tw-te/cf/Ico. **192 importer `_v2kit` 0 churn** (barrel `export *`). Verify: tsc 0 · vite ✓ 47.6s · **53/53 export symbol present** · 5 khối diff VERBATIM (DataTable/CrudModal/DrawerShell/Options/SimpleV2Page). 6 category barrel điền (actions/dataDisplay/navigation/table/overlay + form mở rộng).

## Phiên 2026-07-07 (cửa FE-restructure — timer hooks + research, DONE-local build-green CHỜ push)
- **Nghiên cứu timer + đóng gói hook** [refactor FE]: research "khi nào dùng setInterval/setTimeout, teardown/sync, stale-closure". Đóng gói `hooks/{useInterval,useTimeout,useDebouncedCallback}` (ref-based, auto-teardown, delay=null pause) + `useDebounce` (đã có) + barrel. tsc 0 + vite ✓.
- **Phản biện chốt:** `useMemo`(384)/`useCallback`(715) = memoization PER-COMPONENT → KHÔNG đóng gói dùng chung (anti-pattern), để inline. Imperative money-poll (PaymentQRModal/Kiosk payTimer) = bespoke đã đúng, KHÔNG ép declarative.
- **Migrate scope HẸP (theo user chốt dần):** chỉ v2-internal, KHÔNG v1 (retire), KHÔNG tiền (Payment/Kiosk/BankPayments), KHÔNG tích-hợp-ngoài (AI/SignalR/RIS/DICOM), defer patient-safety (OPD). → migrate `TerminalLayout` (v2 shell clock) + `pages-v2/QualityDashboardLive` (poll 60s). Agent lỡ đụng 4 file v1 (Dashboard/QueueDisplay/HealthTab/SessionsTab) + DocumentLockIndicator (dead) → **đã REVERT**. BackupTab (imperative self-clear) để nguyên.
- Hook sẵn sàng cho code v2 mới + các owner tự migrate money/external/OPD ở phiên có smoke.

## Phiên 2026-07-07 (cửa FE-restructure — router/ + services/ consolidation, ✅ PUSHED origin/main)
- **#375 router/ tách routing** [refactor FE, ✅ PUSHED `f579c80` Refs #375]: App.tsx 834→119; `router/` = AppRouter(BrowserRouter+providers)+AppRoutes(Routes)+RouteGuard(ProtectedRoute)+LayoutResolver(HomeEntry)+`lazy/`+`routeConfigs/` (v2 165 route theo 7 domain, meta{title,group}); v1 dời verbatim (retire #204); dup procurement giữ first-win. parity 165=165. #375 vẫn OPEN (permission-gate #378 chưa làm).
- **services/ gom shared** [refactor FE, ✅ PUSHED `39513d5`]: relocate STRICT không shell — `api/client.ts`→`services/apiClient.ts` (186 import, file xóa) · `utils/logger.ts`→`services/logger.service.ts` (2 import, xóa); extract menu (HIS_GROUPS từ TerminalLayout) + signalr (factory, 3 caller); migrate storage (26 file localStorage→typed) + file (25 file download→downloadBlob). **KHÔNG ép idiomatic** message(214)/console(115)/Modal(39) → notification/dialog/auth/permission.service chỉ là "nhà" cho code mới. Verify inline byte-equiv (apiClient/logger)+semantics (storage auth token/JSON-default)+download filename = 0 bug logic. 6/10 service dùng thật.
- **Kèm rename user** (theo yêu cầu "push phần rename của tôi"): `config/api.ts`→`config/api.config.ts` + `aiLabelingService.ts`→`aiLabeling.service.ts` + move `components/digitalSignature/` (6 component, wired vào EMR/Lab/Prescription — bắt buộc include cho build green). git nhận diện 10 RENAME (giữ history), 0 deletion mồ côi.
- **GIỮ LOCAL chưa push (scaffolding dở của user):** `config/{app,env,index,permission,route,theme}.config.ts` rỗng, `hooks/*`, `modules/`, `components/*/index.ts` rỗng — unreferenced, loại an toàn không vỡ build. User xây tiếp sẽ tự push.
- **config/ gom shared config** [refactor FE, ✅ PUSHED, build-green]: `theme.config.ts` ← theme Antd (App.tsx 119→27 dòng, `getAntdTheme(isDark)` verbatim) · `env.config.ts` ← isDev/isProd + `ORTHANC_URL` (dedup 2 file DicomViewer/PublicStudyViewer) · `index.ts` barrel. **CHỦ ĐỘNG KHÔNG gộp:** `PAGE_SIZE` (13 file giá trị KHÁC 16/18/20 = module-specific) · VGCA env (vgcaSign cố ý giữ tại chỗ) · PublicStudyViewer `API_ORIGIN` (fallback khác api.config, prod-edge). `app/route/permission.config` để RỖNG.
- **hooks/ gom shared hooks** [refactor FE, ✅ PUSHED, build-green]: `useAuth.ts` ← re-export từ AuthContext (home chính thức) + migrate 7 importer · `useDebounce.ts` (canonical, thay 9 pattern inline — KHÔNG ép migrate) · `useLocalStorage.ts` (bọc storage.service) · `usePermission.ts` (bọc permission.service + subscribe useAuth, can() stub → #378) · `index.ts` barrel. tsc 0 + vite ✓. **Toàn bộ chuỗi FE-restructure hôm nay lên origin: router `f579c80` · services `39513d5` · types `4df326c` · config+hooks (commit này).**
- **types/ gom shared type** [refactor FE type-only, ✅ PUSHED `4df326c`, build-green]: gom vào `types/` — `PagedResultDto` (dedup **12 bản TRÙNG shape hệt** → types/pagination) + `PageMeta`/`ApiResponse` (api/types) + `NavGroup/NavItem` (menu.service) + `RouteEntry/RouteMeta` (routeConfigs) + `User`/`LoginRequest` (api/auth); re-export back-compat (16 file sửa, 0 churn downstream). **CHỦ ĐỘNG NÉ:** 8 `PagedResult` (non-Dto) PHÂN KỲ shape (pageIndex vs page) → KHÔNG gộp (gộp = bug logic). 6 taxonomy file (common/dictionary/layout/option/permission/upload) để RỖNG. tsc -b 0 error (type erased → 0 runtime change). CHỜ user duyệt push.
- **#366 AUTHZ-0** Closes + tạo **#401** (Zalo ZNS) **#402** (PACS proxy+RIS PDF) **#406** (queue-display lộ tên BN) từ audit.

## Phiên 2026-07-06 (cửa #202 — thin-controller sweep HOÀN TẤT, ✅ Closes #202)
- **#202 [REFAC-3] DONE** [refactor, ✅ PUSHED Closes #202]: thin 6 controller nghiệp vụ bỏ HISDbContext → service layer (Clean Arch), logic verbatim, build 0 error:
  - **ExaminationComplete** (commit 8aba506): emr-records + prescriptions/recent + search-by-code → IExaminationCompleteService.
  - **LIS + Inpatient.Operations + PatientPortal** (commit cfa1d1b): lab-orders-by-admission + 4 sample-op · shift-handover + medical-record-archive · login(BCrypt+lockout)/doctors/departments/6 ownership-check.
  - **Pharmacy + DigitalSignature** (commit 09a2261): IPharmacyService (dispensing FEFO qua IWarehouseCompleteService + alerts/inventory/reports/transfers) · IDocumentSignatureStore (persistence+query, giữ orchestration PKCS#11/PDF ở controller, SubmitSigned giữ transaction đơn).
- **Còn lại → #365** (đã comment đầy đủ): dev controller (DailySeed/PopulateData/DevLinkRadiology) + deferral có ý (Pdf HttpContext · NonDicom IFormFile · Health schema-drift diagnostic).

## Phiên 2026-07-06 (cửa layout-commercial-redesign — thiết kế thương mại HIS, ✅ DOCS DONE)
- **Research-only** (user chỉ đạo KHÔNG CODE): hoàn tất brief thương mại 14 phần cho PK/TTYT/BV vừa-nhỏ (100–1.000 TK, team 1–3 dev, MVP 9–12 tháng).
- **Docs tạo/cập nhật:** `08-thiet-ke-thuong-mai.md` (14 phần + panel phản biện 4 lens đã điền Opus inline) + `09-permission-catalog.md` (72 mã × 12 role) + `07-implementation-roadmap.md` (thêm commercial timeline + 3 issues mới) + placeholder trong README.
- **Kiến trúc thương mại chốt:** 1 shell + 4 workspace logic (data, không phải code) + 12 role template seed + 72 permission PascalCase + 10 module thương mại + EnabledModules cờ đóng gói.
- **Gap phát hiện:** tìm BN không dấu (#403 — deal-breaker demo VN, plain `Contains()` accent-sensitive).
- **GitHub actions:** 8 comments re-scope (#367/#372/#375/#378/#379/#382/#385 + epic #387) + 3 issues mới (#403 VN-accent · #404 workspace-layer · #405 EnabledModules).
- **✅ PUSHED** (user duyệt): 5 file docs (README + 07 + 08 + 09 + STATUS) → origin/main. Chỉ `git add` tường minh file docs của cửa này; SOURCE CODE + cây dirty cửa khác = KHÔNG đụng.

## Phiên 2026-07-05 (cửa layout-docs-push — commit+push docs research, dedup issue, ✅ DONE)
- **Push docs** [ops-doc, user duyệt]: commit `docs/architecture/layout-architecture/` (8 file: README + 6 chương + roadmap) + `docs/workspace-docs/10-assessment/danh-gia-phan-quyen-rbac-redesign.md` (449 dòng) → origin/main. KHÔNG đụng: `anonymous-surface-whitelist.md` (của cửa #366 đang active) · REGISTRY/SKILL-MAP staged (stash dead-window, cần review) · `.obsidian`/`.continue`/test.txt.
- **⚠️ Sự cố dedup:** cửa này tạo 13 issue layout theo roadmap ĐÚNG LÚC cửa layout-arch cũng tạo bộ #374–#386+epic #387 (số khớp docs) → 13 bản của cửa này thành #388–#400 TRÙNG → **đã đóng cả 13** (not-planned, comment trỏ issue gốc). Bài học: check `gh issue list` NGAY TRƯỚC create, không dựa snapshot cũ trong phiên.
- Đầu phiên đã force-release lock `202` stale 35h — cửa 202 còn sống, đã tự re-claim (23:12). Không ảnh hưởng.

## Phiên 2026-07-06 (cửa này — #366 AUTHZ-0 P0, ✅ PUSHED `Closes #366`)
- **#366 AUTHZ-0 [P0]** [security-fix, ✅ PUSHED]: (a) `FrontendCompatController` bỏ `[AllowAnonymous]` → fallback RequireAuth; (b) `Users` + migration `100_lockout_columns.sql` (FailedLoginCount + LockoutEndAt); lockout ≥5→5' ≥10→10' ≥15→20' ≥20→30' `AuthService.LoginAsync`; (c) rate-limit 10 req/min `[EnableRateLimiting("login")]`; (d) LIS `dev/update-dates-to-today` thêm `[DevelopmentOnly]` (thiếu so với twin RIS); (e) `anonymous-surface-whitelist.md` rà 50+ endpoint, phân loại JUSTIFIED/DEV_ONLY/NEEDS_AUTH/SUSPICIOUS; tạo **#401** (Zalo ZNS low) + **#402** (PACS proxy+RIS PDF high) + issue TBD (queue-display medium). Build 0 error.
- **Stash WIP orphan:** 3 stash (patientportal thin, examinationcomplete) từ dead windows — `git stash list` để recover. `.claude/REGISTRY.md`+`SKILL-MAP.md` staged từ stash@{1} (cần review trước commit riêng).

## Phiên 2026-07-05 (cửa layout-arch — NGHIÊN CỨU Layout Architecture → epic #387 + 14 issues #373-#386)
- **Research-only** (user chỉ đạo: "chỉ nghiên cứu, không code"): khảo sát TerminalLayout 959 dòng, App.tsx 834 dòng, RBAC 0 FE caller, 156 trang v2, 18 vai nhân viên 100–1.000 người.
- **Kiến trúc chốt:** 1 shell TerminalLayout + Module Registry (`src/app/module-registry.ts`) + 3-layer permission (menu→route→button) + custom can()/Can, không CASL.
- **P0 phát hiện:** (a) ErrorBoundary v2 bị thiếu → crash toàn shell; (b) Race condition đổi BN nhanh trong OpdEditor → data BN-A ghi vào BN-B (patient safety).
- **Tài liệu tạo:** `docs/architecture/layout-architecture/` (7 file: README + 01-hien-trang + 02-layout + 03-permission + 04-routing + 05-navigation-ux + 06-theme-perf-security + 07-roadmap)
- **Issues tạo:** epic **#387** + **#373** P0-ErrorBoundary · **#374** P0-race-condition · **#375** Module Registry · **#376** Shell split · **#377** Guards · **#378** Permission codes · **#379** Dashboard · **#380** Notification · **#381** Dark mode · **#382** Command Palette · **#383** Idle lock · **#384** Concurrent login · **#385** Break-glass · **#386** Perf.
- **Phối hợp:** #378 cần RBAC epic #372/#367 xong trước; #373–#377 chạy song song RBAC.
- **Không đụng code/git-ops**; cây dirty của các cửa khác giữ nguyên.

## Phiên 2026-07-05 (cửa này — NGHIÊN CỨU thiết kế lại phân quyền → epic #372 + 6 issue, KHÔNG code theo yêu cầu user)
- **Research-only** (user chỉ đạo: "chỉ nghiên cứu rồi tạo vấn đề, không code ngay"): quét hiện trạng AuthN/AuthZ (agent Explore, có bằng chứng path:line) → thiết kế 12 bước (actor/module/permission/role/matrix/rule đặc biệt/audit/DB/kiến trúc/rủi ro) → **doc đầy đủ `10-assessment/danh-gia-phan-quyen-rbac-redesign.md`** (UNCOMMITTED — push khi user duyệt batch).
- **Kiến trúc chốt:** 4 lớp native ASP.NET Core (L1 permission `Resource.Action` + RequirePermission/policy-provider/cache · L2 scope OWN/DEPT/BRANCH/ORG trên lượt gán · L3 ABAC policy-as-code [quan hệ điều trị/SoD/break-glass/delegation] · L4 DTO field-masking) — KHÔNG OPA/Casbin (phản biện trong doc); multi-org DEFER.
- **Issues tạo:** epic **#372** + **#366** AUTHZ-0 P0 vá bề mặt hở (FrontendCompat anonymous + lockout + rà AllowAnonymous) · **#367** AUTHZ-1 permission enforcement lõi · **#368** AUTHZ-2 refresh/SecurityStamp · **#369** AUTHZ-3 scope/BranchId/treatment-relationship/field-masking · **#370** AUTHZ-4 SoD/delegation/break-glass/temporary · **#371** AUTHZ-5 audit pipeline+retention. Body tự chứa (máy khác làm không cần doc local). #366 phối hợp #202 (FrontendCompat vừa thin). Test ma trận quyền #344/#216 làm CUỐI.
- **Không đụng code/git-ops**; cây dirty 171 file của các cửa khác giữ nguyên.

## Phiên 2026-07-04 (cửa này — #361 DONE + PUSHED `eade0b5` → origin/main)
- **#361 feat(lis): port Microbiology antibiogram data-entry UI to v2** [feature, ✅ CLOSED]: thêm `AddOrganismModal` + `AntibiogramModal` vào `pages-v2/Microbiology.tsx`. Wire "Thêm vi khuẩn" button vào DrawerShell footer + "Kháng sinh đồ" button per organism. tsc -b EXIT 0, npm run build EXIT 0. Commit `eade0b5`, Closes #361.

## Phiên 2026-07-04 (cửa này — #202 thin ĐỦ 13 controller Tier-G + DI wired, build 0 error, CHỜ push+smoke)
- **#202 [REFAC-3] Tier-G sweep 13 controller tách HISDbContext** [HARD, build-only]: bỏ `HISDbContext` khỏi **13 controller guardrail** (money/kho/patient-safety/AI) → service layer, chia 4 nhóm agent song song:
  - **Group A (4):** StockLedgerReport·PatientFlag·ServiceRefund·ClinicalPharmacy (chỉ `PatientSummary` tách; `ImportDrugInteractionsCsv` IFormFile giữ controller).
  - **Group B (5):** RadiologyOperations·ReceiptBook (giữ verbatim `FromSqlRaw` UPDLOCK/ROWLOCK transaction trong `NextNumberAsync`)·PharmacyEnhancement·FrontendCompat (giữ `[AllowAnonymous]`)·StockReport.
  - **Group C (3):** InpatientDispensing (FEFO depletion verbatim)·WriteGap (2 chỗ `ServiceOutcome.Status(404, ApiResponse.Fail(...))` giữ body-shape)·PaymentReports.
  - **AiLabeling (partial):** 5 method dùng `_db` (Save/Review/GetQueue/ByStudy/RunViaProvider+MapAsync) → `IAiLabelingService`; giữ config/filesystem/provider method ở controller.
- Tạo **13 interface** (`HIS.Application/Interfaces/`) + **13 service impl** (`HIS.Infrastructure/Services/`) + **9 DTO folder mới** (`Application/DTOs/*`, 4 controller không có DTO riêng) + xóa **9 `HIS.API/Dtos/*Dtos.cs`** cũ (stale using). Logic verbatim, behavior-preserving. Controller 36 file −6351/+496 dòng.
- **DI:** đã thêm **13 `AddScoped`** vào `DependencyInjection.cs` (block "#202 thin-controller sweep Tier-G" sau Tier-S). Namespace `HIS.Application.Interfaces`+`HIS.Infrastructure.Services` đã có sẵn using.
- **Build:** `dotnet build HIS.API` → **0 error, 4775 warning** (pre-existing). Spot-check 3 điểm rủi ro cao PASS (WriteGap ApiResponse.Fail · ReceiptBook UPDLOCK · ClinicalPharmacy IFormFile ở controller). **CHƯA push** (backend down → chưa smoke; DI resolution runtime chỉ chắc khi smoke). Chờ user + phiên deploy+smoke.
- **Còn lại vẫn inject HISDbContext:** PdfController (DEFER — đã thin sẵn, `_db` chỉ ở ExportFullRecord + HttpContext phức tạp) + PatientPortalController + 6 god-controller PARTIAL (DailySeed/ExaminationComplete/LISComplete/DigitalSignature/InpatientComplete.Operations/Pharmacy) + NonDicon Upload (IFormFile blocker, cố ý).

## Phiên 2026-07-04 (cửa #202-BE `w973` — thin 5 fat controller, build-green, CHỜ push)
- **#202 [REFAC-3] Checkpoint** [HARD, build-only]: bỏ HISDbContext khỏi **5 controller** → service (RadiologyDispatch·RisCatalog·LisCatalog·OfficeSupply·EmployeeProfile = đủ 5 NAMED trong issue). Controller **2173→620 dòng**; logic move verbatim xuống `HIS.Infrastructure/Services/*Service.cs` + `HIS.Application/Interfaces/I*Service.cs`. Envelope mới `ServiceOutcome` (Application/Common) + `.ToActionResult()` (API/Extensions) giữ nguyên status+body; OfficeSupply dời DTO sang Application (money/kho verbatim).
- **Verify build-only:** `dotnet build` toàn sln **0 error**; multi-agent 4 self-review + 12 adversarial (3 lens×4) = **0 drift**; spot-check money-path byte-identical. **CHƯA push** (backend down → chưa smoke; DI resolution runtime chỉ chắc khi smoke). Chờ user + phiên deploy+smoke. Còn ~42 controller khác vẫn inject HISDbContext.

## Phiên 2026-07-04 (cửa này — SKILL core-safe-branch-merge + forensics backup-session-b2)
- **User yêu cầu merge nhánh local → main.** Chỉ 1 nhánh khác = `backup-session-b2` (329-behind, 7 commit). **Workflow verify đối kháng (6 agent):** money-fix `3837270` ĐÃ trên main (`3edf11c`); 12 file BE B2.2/B2.4 superseded + full-merge sẽ **BUILD-BREAK** (trùng class + migration #48/#49); 4 docs never-push → **KHÔNG merge/cherry-pick backup** (leave-as-backup).
- **2 gap FE thật backup lộ ra → đề xuất 2 issue PORT (feature, làm sau):** (1) DICOM annotation FE wiring (BE main sẵn, FE main KHÔNG gọi `saveAnnotation/getAnnotations` → chú thích mất khi reload); (2) v2 Microbiology antibiogram data-entry UI (chỉ có ở v1 retiring #204; api+BE main sẵn).
- **Tạo skill `core-safe-branch-merge`** (CORE portable · LINT OK · REGISTRY+SKILL-MAP): nhúng 12 fix critique (fetch-origin trước · `git cherry` patch-id · `cherry-pick -n` stage-review · worktree teardown đừng `rm -rf` · đừng pull/rebase cây chung · FE push=Vercel deploy).
- **Việc kế (CHỜ user duyệt execution):** #359 fmtVND (worktree, chưa commit) sẵn lên main; fmtDate `bd3e4b0` cửa khác (chưa push) → phối hợp, không push hộ.

## Phiên 2026-07-04 (cửa #205-FE-2 — split 4 god-component v2, LOCAL build-green CHỜ push)
- **#205 [FE-2] Checkpoint 2-5/5 — 4 god-component** [HARD tech-debt, DONE-code CHỜ-push]: tách 4 file `pages-v2/` thành folder phẳng (convention `reception/`/`inpatient/`), mỗi main giữ `export default` (App.tsx lazy import KHÔNG đổi):
  - **Dashboard** 1166→**342** dòng · folder `dashboard/` 16 file (`_shared.ts`+9 section+6 modal). Pure-move verbatim.
  - **Radiology** 1461→**507** dòng · folder `radiology/` 8 file (`_shared.tsx`+CallPatient/SignResult/Biopsy/ResultEntry modal+CoReaderSection+DrawerBody+columns). `onBulkApprove` (clinical bulk-approve) GIỮ trong main. SurgeryReportModal import từ `../shared/SurgeryReportModal` (barrel KHÔNG export).
  - **Laboratory** 1003→**481** dòng · folder `laboratory/` 6 file (`_shared.ts`+columns+DrawerBody+ChainCancel/UtilDrawer/RolesModal). `runChainCancel`/`saveRoles`/`loadUtilData` GIỮ trong main (preserve click-gating, KHÔNG đổi useEffect).
  - **OpdEditor Phase-1** 1648→**1290** dòng · folder `opd-editor/` 5 file (`_shared.ts`+InjurySection+ClsResults/Consult/Template modal). **CONSERVATIVE (Rule 6):** chỉ tách phần KHÔNG-patient-safety; Diagnosis/Orders/Allergy/Vitals/9 disposition-completion-modal/5 useEffect/handlers (persist·completeExamination TT46-guard·doHospitalize·doTransfer) **DEFER Phase-2** phiên deploy+smoke → main còn >500 dòng (có chủ đích).
- **Verify:** `tsc -b` + `npm run build` **EXIT 0** (built 2m20s). **Verify đối kháng ĐỦ 4 lens PASS** (Radiology verbatim full-coverage 1461 dòng · Radiology patient-safety [sign/final-approve/biopsy/bulk/co-reader/statusKey] · Laboratory patient-safety [flagFor HH-LL/chainCancel level≥2/approval-chain/click-gating] · OpdEditor post-fix): equivalent=true, high-conf, **0 issue**; mount-semantics + columns-lift (0 state closure) confirmed. **2 defect OpdEditor đã fix trước verify:** ClsResultsModal+ConsultModal subtitle guard `patientName?` → pre-computed `sub` prop (khôi phục selPt-existence semantics). Side-effect audit Rule 5: 0 timer/subscribe mới sinh trong 32 file tách (2 setTimeout revoke-URL = verbatim-move từ gốc).
- **User duyệt push+close (2026-07-04).** Comment #205 khai báo 4 component để máy-2 tránh; origin đối chiếu trước push: chưa có folder nào từ máy-2. File ngoài scope (KioskSelfService/PatientPortalStandalone/reception/BE #202) — KHÔNG stage.
- **OpdEditor Phase-2 → issue #362** (scope-overlap transfer đủ context trước khi đóng #205): Diagnosis/Orders/Allergy/Vitals + 9 disposition modal + 5 useEffect + handlers TT46/persist/hospitalize/transfer — cần phiên deploy+smoke (Rule 6). TEST làm CUỐI.

## Phiên 2026-07-04 (cửa fmtDate — dedup date/time-formatter ✅ DONE + PUSHED `df20215` → origin/main)
- **SYNC-GATE:** local diverged 2 commit trùng-nội-dung đã lên origin (`4107a7f`/`3968db3`) → rebase `--skip` tự-lành về origin (0/0); STATUS.md lấy bản origin (không giành cửa khác).
- **Backlog tech-debt CẠN/bị claim:** #201/#202/#205 in-progress cửa khác; #191/#212/#213 = test (để cuối). Slice cô lập không-cần-deploy còn lại = gom date-formatter.
- **fmtDate/fmtDateTime/fmtTime** [EASY tech-debt, ✅ DONE + PUSHED `df20215` origin/main (fast-forward `4107a7f..df20215`, 0/0; Vercel auto-deploy)]: thêm **3 helper** vào `utils/format.ts` (mirror CHÍNH XÁC `new Date(x).toLocaleDateString/toLocaleString/toLocaleTimeString('vi-VN')`, không thêm guard) + migrate **24 site (13 date + 7 datetime + 3 time + 1 fold local-helper PatientPortalStandalone)** ở **18 file** v2/shared/utils. Chỉ thay inner substring, giữ nguyên guard/ternary/try-catch/fallback. **tsc -b EXIT 0**, behavior-preserving. Commit `df20215` (amend từ `bd3e4b0`; explicit-add CHỈ 18 file của tôi, KHÔNG đụng #205 god-component/CornerstoneViewer + BE #202).
- **Safe scope date/time-formatter = HẾT** (grep xác nhận: 16 datetime còn lại đều ở v1 `pages/` hoặc `opd-editor/`+`surgery-modals/` của #205; 3 time đã xong; 1 options-datetime lẻ `BedLabResultSection` = không dedup).
- **Non-goals giữ nguyên (cửa khác/deferred):** ~314 **number-formatter** (`n.toLocaleString('vi-VN')`+đ/%) = territory **#359 fmtVND** → KHÔNG đụng · no-arg `new Date()` (HrDecisions:66, current-date) · v1 `pages/` (v1-retire #204) · 5 god-component + split folders #205 · reception `fmtHM` (impl padStart khác, không byte-identical).
- ⚠️ **Trùng file với #359 (đã push TRƯỚC):** `df20215` đã lên origin với `fmtDate/fmtDateTime/fmtTime` sau `fmtNum`. #359 `fmtVND` (worktree, CHỜ push) khi push sẽ append tiếp — không chồng dòng, tự-merge. Push df20215 = fast-forward sạch, KHÔNG cần worktree (0 behind); working-tree dirty của #205/#359/#202 KHÔNG bị đẩy (git push chỉ đẩy commit).
- **Verify trước push:** workflow đối kháng 4 lens (helper-fidelity·guard-preservation·arg/type-refute·isolation) + synthesis gate = **SHIP** (0 blocker/0 fail; mọi finding là note clear/skip đúng).

## Phiên 2026-07-04 (cửa này — #361 v2 Microbiology antibiogram port, build-green, CHỜ push)
- **#361 Microbiology antibiogram v2 port** [FEATURE, build EXIT 0, CHỜ push]: thêm `AddOrganismModal` + `AntibiogramModal` vào `pages-v2/Microbiology.tsx`. AddOrganismModal: 6 field (mã/tên vi khuẩn, khuẩn lạc, Gram, hình thái, PP định danh) → `addOrganism(cultureId, data)`. AntibiogramModal: inline-editable table (kháng sinh/MIC/Zone/S-I-R/PP) pre-populate từ `organism.antibiogram`, thêm/xóa hàng → `saveAntibiogram(organismId, rows[])`. Wired: nút "Thêm vi khuẩn" footer DrawerShell + nút "Kháng sinh đồ" per-organism card. tsc EXIT 0, `npm run build` EXIT 0. File: `frontend/src/pages-v2/Microbiology.tsx`.

## Phiên 2026-07-04 (cửa này — #359+#360 DONE + PUSHED `30f0d71`/`2659d63` → origin/main)
- **#359 fmtVND** ✅ **CLOSED+pushed `30f0d71`**: de-dup 4 bản sao `fmtVND` → `utils/format.ts`; 5 file FE (Billing|Insurance|Inpatient|Pharmacy + format.ts). Vercel auto-deploy.
- **#360 DICOM annotation** ✅ **CLOSED+pushed `2659d63`**: wired save/load per-SOP vào `CornerstoneViewer.tsx` + plumbed `DicomViewer.tsx`; 2 file FE.
- **Skill `core-safe-branch-merge`** đã ship (phiên này), REGISTRY+SKILL-MAP OK.
- **Issues mới tạo:** #361 (v2 Microbiology antibiogram port — open, chưa claim).

## Phiên 2026-07-03 (cửa Opus/Fable — #358 NangCap25 QR động VCB CLOSED+pushed+deployed+verified prod)
- **#358 [NangCap25] QR động Vietcombank kết nối viện phí (BV VN-Thụy Điển Uông Bí) → CLOSED** (`840dac9` feat + `a3a0e09` e2e).
  Trọn 22 mục (I-VI): QR động 5 nguồn (chỉ định CLS/đơn thuốc/quầy thuốc/tạm ứng/ra viện) + kiosk · paid-hook V.1
  (IsPaid+Status 0→1 mở gate LIS/PACS, tạo Deposit, RetailSale.PaidAmount) · chi hộ hoàn tiền IV (MockMode) · báo cáo
  VI.1 người-tạo-QR + VI.2 đối soát NH · nhúng QR 4 phiếu in. Mở rộng `PaymentGatewayService` (KHÔNG dựng cổng mới).
  Migration **141** (`ReferenceType/Id/Data` + bảng `RefundDisbursements`). FE: `QrPaymentCenter` `[25]` route `/v2/qr-payment-center`.
- **2 bug thật phát hiện qua test → fixed + regression-guard:** (1) business guard 500→400 (thiếu `[TypeFilter(DomainExceptionFilter)]`
  trên `PaymentGatewayController`). (2) **ROOT-CAUSE tiềm ẩn:** `PaymentTransaction.Patient` required-nav + BN xóa mềm → filtered
  `.Include(Patient)` ẩn luôn giao dịch (confirm 500 / getById 404 / rớt khỏi đối soát) → fix 1 dòng `HISDbContext`:
  `.Navigation(t=>t.Patient).IsRequired(false)`. Ảnh hưởng MỌI query payment, không riêng NangCap25.
- **Test 2 tầng:** BE `scripts/test-nangcap25.ps1` **30/30** (API + paid-hook + reports + chi hộ + print + regression 3 cổng cũ
  + BN-xóa-mềm + guards 400). FE `frontend/e2e/nangcap25-ui.spec.ts` **4/4** Playwright UI-drive (đối soát+submit chi hộ ·
  kiosk sinh QR canvas · VietQR method · render sạch) + 10 evidence `test-results/NangCap25/`.
- **Deploy verify prod:** GitHub Actions run 28644306427 **success 6m17s** · `/health/schema-drift` **missingCount=0** ·
  endpoint VI.2/IV/query-cột-mới trả 200 (migration 141 áp, bảng `RefundDisbursements` tồn tại). LIVE trên prod.
- **Vận hành còn lại (không chặn nghiệm thu):** chi hộ IV chạy MockMode (cần merchant contract VCB) · confirm bank thủ công
  (VCB chưa cấp webhook IPN) · điền số TK VCB thật qua env `PaymentGateway:Bank:vietcombank:*`.
- Cũng đã push đầu phiên: `8e46244` envelope-standardize 12 controller · `7ade374`/`f7b8784` agent-memory+docs+gitignore dọn.

## Phiên 2026-07-03 (goal: hoàn thành TẤT CẢ issue — fix trước, test cuối)
- **CLOSED hôm nay:** **#292** (Cloud SQL private-IP `10.10.0.3` + gỡ 0.0.0.0/0 + rotate pwd sqlserver; public 1433 BLOCKED; prod healthy) · **#182** (rotate Jwt__Key + PACS Orthanc pwd [VM .env + Cloud Run] + scrub seed-script + fail-fast guard default-key prod, `7bcae24`) · **#293** (shortlist 6 controller gate RBAC, `e0057d5`, smoke admin 14/14=200; test-403 bàn giao #344) · **#183** Phase-2 (verify prod 8 role; gộp RadiologyManager→RadiologistManager orphan-neutral, `52af969`) · **#209** (MOOT theo v1-retire; phần sống → #352).
- ⚠️ Máy này CÓ gcloud (owner) + SSH PACS VM — các defer "thiếu gcloud" trước đây làm được tại đây. DB prod truy cập qua cloud-sql-proxy.
- **Đang chạy (agent nền):** #193 envelope (cây chính) · #195 N+1 write-path (worktree) · #198 audit-log (worktree). Sau đó: #197(4 phần) → #202-thin → #348(+đóng #200) → #355/#356+#201-threshold → #205 → #210 → #214 → epic #352/#204 → TEST (#191/#212/#213 rồi #216-347).

## Phien 2026-06-28..29 (cua OPUS - #354/#201/#203/#202 SHIPPED prod, deploy success)
- **#354** [PERF-2a]: bound 99 list-endpoint type-(a) + verify doi-khang 43 agent bat 8 site bound-nham -> revert. PUSHED cf8962f.
- **#201** [REFAC-2]: tach 7 BE god-file -> 36 file <800 (byte-identical; encoding-fix [IO.File]::ReadAllLines UTF-8). PUSHED a50123a.
- **#203** [REFAC-4] DTO hygiene TRON VEN -> CLOSED (721316d): 11 god-DTO split <800 + 198 inline-DTO ra khoi 48 controller (Dtos/) + dedupe 8 DTO divergent rename role-specific + grab-bag rename. Du 4 acceptance.
- **#202** [REFAC-3]: tach 13 prod god-controller -> partial <800 (route-attr GLOBAL 2919==2919 + string-literal Vietnamese 6764==6764 bao toan; mojibake=0). PUSHED 8ab3a52 + 8602ed6. **OPEN**: thin/bo-DbContext + 2 dev-tool (PopulateData/DailySeed atomic method) = can smoke.
- **Viec ke:** BE tech-debt con lai (#193/#195/#197/#198/#355/#356/#202-thin/#214) deu CAN deploy+smoke -> phien smoke-capable. FE (#206/#210/fe-unused) dang do cua khac giu lock. TEST lam CUOI cung. *(#211+#353 da CLOSED+pushed e628b96/4afcf98 — boi cua nay.)*
- **#357** [bug/P0 patient-safety] **CLOSED+pushed `cd4f583`** (cua nay): khoi phuc co an-toan-BN vao **5 editor v2** (OPD/Prescription/EMR/Billing/Inpatient = dung 5 trang v1 co banner); `EmrEditor` chi `PatientFlagBanner` (parity v1). Reuse `components/PatientFlagBanner`+`BusinessAlertPanel`, behavior-preserving (tu-an khi khong co flag/alert), build FE EXIT 0, 31 insertions/5 file. Residual feature-PORT (lieu-theo-cu S/T/C/Toi · AI-CDS/NEWS2 · in MSS-01) track o #353 §2b-2 + §5 PORT + #352.

## Phiên 2026-06-28→30 (cửa này — #211 + #353 CLOSED+pushed; #357 đã xử lý; #353 Tier-2 verify done)
- **#211** dedup URL `NonDicomCapture` → **CLOSED+pushed `e628b96`**. **#353** inventory v1↔v2 → **CLOSED+pushed `4afcf98`**.
- **#357** [P0 patient-safety] (phát hiện từ #353: v2 mất `PatientFlagBanner`+`BusinessAlertPanel`) → **CLOSED+pushed `cd4f583`** (khôi phục banner vào 5 editor v2).
- **#353 Tier-2 verify HOÀN TẤT (2026-06-30, cửa này):** soi nốt 16 trang batch-Full chưa-verify (D2/E2) → **12 giữ Full, 4 HẠ Partial** (StockReport/DispensingCounter/HealthEducation/SampleTracking). RadiologyOps nghi endpoint-mismatch → **VERIFY = false-alarm** (BE có `paraclinical-services`+`medical-supplies`; convention 2=LIS/3=Radiology; v1 `/catalog/services` mới là endpoint-chết) → giữ Full, KHÔNG cần fix. Inventory doc: **TỔNG CUỐI 45 Full · 58 Partial · 14 Stub**; DELETE-safe **44** (14 verified + 30 superset); DROP-list RỖNG. Đã giải quyết hết vấn đề task → commit+push enrichment cho #353.
- Lock cửa này: `353-tier2`. (Cửa khác: #355/#356, fe-unused-cleanup, 202/206.)

## Phiên 2026-06-28 (#354 wave-2 — XONG-local, build-green, CHƯA push, chờ user duyệt)
- **#354 [PERF-2a][P1] CLAIMED** (in-progress + assignee me): bound type-(a) list endpoint còn lại (~158 file/1130 site `.ToListAsync`
  chưa quét). Nối tiếp #196 wave-1 (foundation `QueryBoundExtensions.ToBoundedListAsync`). **Calibrate rule**: type-(a) =
  `.ToListAsync()` trả rows về client, KHÔNG Skip/Take, KHÔNG aggregate(.Count/.Sum/GroupBy)/write-loop theo sau (kể cả FK-scoped);
  CHỈ bound **bảng tăng-trưởng** (bỏ catalog tham chiếu cố định nhỏ: Genders/Ethnics/Nations…).
- **Phát hiện density THẤP**: nhiều "list service" đã `.Take(200)` sẵn (Chronic/Community/Environmental/HealthEdu/Reproductive ≈0 type-(a)).
- **Cach lam**: gop 2 cua lam-trung ve 1 cua (user dung cua kia). 5 subagent Sonnet quet Set A (124 file raw ToListAsync chua bound) + Set B (26 file wave-1 con gap). Rule conservative skip-when-uncertain (bound nham aggregate = SAI so lieu; bo sot = lanh). KET QUA: 99 site bound o 45 service file + 39 using. Catalog lon (SNOMED/ClinicalTerms/Countries) DUOC bound; vai FK-scoped (caseId/chainId) bound an toan.
- **Kế**: consolidate 3 báo cáo → review từng site (cổng correctness: soi SecurityService:96 report-take, MasterCatalog reference vs growth,
  CheckMissingForms iterated-build) → áp wave-2 type-(a) rõ ràng + build BE EXIT 0 → cập nhật STATUS. **KHÔNG push** (đợi user duyệt; #354 OPEN tới khi hết wave).
- ⚠️ **Điều phối 4 cửa**: #355/#356 (cùng cha #196) **chồng file** với #354 (khác loại site, cùng service file) → liệt kê file đã đụng khi push, tuần tự merge.
- ⚠️ **TRÙNG #354 GIỮA 2 CỬA (2026-06-28):** cửa-thứ-2 cũng bound 3 site type-(a) sạch TRƯỚC khi thấy section wave-2 này (UNCOMMITTED, build Infrastructure EXIT 0):
  `Reception/...Queue.cs` `GetTodayAdmissions`(MedicalRecords toàn-viện/ngày)+`GetServingList`(QueueTickets) · `Examination/...WaitingList.cs` `GetRoomPatientList`(Examinations).
  Defer aggregate-contaminated→#355 (LIS `GetLabQueueDisplay`, Reception `GetWaitingList`=count-derive). **3 site này nằm trong scan 'subdirs' của cửa wave-2** →
  đề xuất **1 cửa OWN #354, cửa kia đổi task** để khỏi lặp; nếu giữ, gộp 3 site này vào wave-2, tránh sửa lại 2 file đó. State-store: comment trên #354.

## Phiên 2026-06-29..30 (#206 [FE-3] adoption design-system — DONE, build-green, push Closes #206)
- **Hướng B (user chốt):** trích hook thay vì ép SimpleV2Page (premise issue "95% giống" chỉ đúng phần vỏ; ép migrate vi phạm
  behavior-preserving — mất drawer-footer-action / thêm pager / vướng server-filter/CRUD). Thêm `useListData<T>` + `useTabCounts<T>`
  + `makeStatus()` (statusConfigs) vào `_v2kit.tsx` (additive export). **Acceptance đủ:** hook + statusConfigs tồn tại; pilot ≥10 page.
- **Adopt 25 page (behavior-preserving, tsc -b EXIT 0):** 1:1 thay boilerplate `[rows]+load+useEffect`→useListData (loader bọc useCallback,
  ổn-định; call-site load()→reload()) và `counts` useMemo→useTabCounts. Pilot+self: FunctionalDiagnostics·LinenManagement·HealthEducation·
  Asset·Booking·ClinicalGuidance·Culture·MRPlanning·Methadone·Micro·PracticeLicense·Consultation·Insurance·HealthExchange·OfficialDocuments·
  PayrollAdmin. Subagent (Opus, verify lại): EInvoices·EmrDataTags·NationalGateways·DeAn06Liaison·BillingGuarantors·HrDecisions·LISConfig·Quality·EmrCloudSync.
- **Cách làm:** classify 111 page (workflow fan-out, rule stable-loader nghiêm) → adopt MỌI page eligible-an-toàn. Bỏ page **server-side-filter**
  (loader đọc search/date → ép adopt = vỡ behavior) + page **cửa khác đang giữ** (fe-unused-cleanup: AnalyzerInbox/BhxhAudit/InfectionControl/
  InterHospitalSharing/MedicalRecordArchive) + **41 page chưa classify** (agent fail vì rate-limit). → **follow-up** khi cửa kia xong + quota hồi.
- **Verify:** tsc -b EXIT 0 (toàn project); eslint KHÔNG lớp-lỗi-mới (set-state-in-effect/only-export-components = pre-existing _v2kit,
  thuộc #210; `no-irregular-whitespace` Consultation:16/Insurance:18 = pre-existing ngoài diff). Spot-check 7 file (gồm multi-panel) behavior-preserving.
- **4 cửa:** chỉ `git add` file của tôi (25 page + `_v2kit.tsx`); KHÔNG đụng NonDicomCapture(#211)/file cửa khác. Push `Closes #206`.

## Phiên 2026-06-25→27
- **#183 Phase-1 SHIPPED** (`fa4e998`, deploy Cloud Run SUCCESS, live prod): gom **518** `[Authorize(Roles="...")]`
  literal ở **45 controller** → `RoleNames` const (`backend/src/HIS.Core/Constants/RoleNames.cs`), **byte-identical**
  (verify độc lập 45 file/518 attr/0 lệch + build 0 err). Acceptance #1+#3 ✅. **Phase-2 DEFER** (gộp chính tả role +
  FE role-compare — cần DB prod xác nhận chính tả thật; máy này thiếu gcloud). Issue **#183 OPEN**, đã gỡ in-progress.
- **#196 wave-1 type-(a) SHIPPED + CLOSED:** foundation `QueryLimits.DefaultListCeiling=5000` + `QueryBoundExtensions.ToBoundedListAsync`
  (static-logger, wire `Program.cs`) + áp **66 site type-(a)** ở **27 file** (`.ToListAsync()`→`.ToBoundedListAsync("Svc.Method")`).
  Byte-safe, build 0 err, verify: không site nào theo sau bởi aggregate/write-bulk reuse (đã đọc 3 ca SaveChanges → đều method kế bên).
  Pre-flight lộ: ~770 ToListAsync vô biên chia 4 loại — chỉ **(a) list-trả-rows** bound an toàn bằng helper.
  **Decompose** phần còn lại thành issue con: **#354** (a-remaining ~124 file chưa quét) · **#355** (b aggregate-in-memory→SQL, bound=sai số liệu)
  · **#356** (c write-bulk→ExecuteUpdate, bound=update thiếu). **#196 CLOSED** (parent đã decompose).
- **Kế tiếp:** #354/#355/#356 cho phiên sau (b/c cần thận trọng correctness; c đụng write-path cần deploy+smoke).
  Khi tới phase TEST → test thẳng prod his-psi (vòng-6) sau khi xong hết fix/tech-debt.

## Đang dở (uncommitted)
- **Governance: mô hình 4 cửa sổ song song** (2026-06-24): `3b1c57b` PUSHED — `.claude/workflow/parallel-windows.md`
  (4 SOẠN + 1 RUNNER · §2b skill-routing theo tầng · §7 TEST mode) + allocation `20-backlog/test-4window-allocation.md`
  (38 phân hệ+12 luồng+cross→C1-C4) + `session-start.sh` cross-identify (chống nhầm Antigravity, mọi máy) + REGISTRY + vite strictPort.
- **Governance: hoàn thiện cơ chế test 4 cửa** (2026-06-24, đang commit batch): §7 **case T7-T32** (A-I + N1-N10 + R1-R8:
  browser/auth/moving-target/PNG-git/uncapturable/RAM/infra-noise/MCP-drop · plan-drift/PHI/test-id/LLM-rogue-prod/sequence/JWT/disk-C/
  done-metric/test≠fix/migration-mid-test · BOM/manifest-vs-image/animation-flake/viewport/seed-effort/quota/DICOM/integrator-SPOF)
  + **§8 BA TRỤ root-fix** + **§7c STAGING DUYỆT** (data giả logic-nhất-quán, module nhạy cảm→staging) + **gitignore ảnh+`manifest.js`**
  (untrack) + **kế hoạch staging** `20-backlog/staging-plan.md` (Cloud SQL `HIS_staging` + Cloud Run revision + seed-generator + MCP read-only).
  **LINT OK.**
- **Red-team vòng-4 + ĐỊNH VỊ LẠI (2026-06-24, UNCOMMITTED):** verify repo **ĐÃ CÓ ~127 Playwright/Cypress** (`e2e/workflows/00-13`=12 luồng ·
  `e2e-prod/*` prod read-only · `clinical-safety-checks` assert patient-safety) + CI `e2e-prod-smoke.yml`. → **4-cửa-MCP THỪA cho correctness**;
  dùng/mở rộng **suite sẵn có** (deterministic·CI). Ghi **§9 parallel-windows.md** (M1-M10 + demote 4-cửa→optional) + reposition `staging-plan.md`
  (staging = chạy Playwright E2E có-ghi trên seed, KHÔNG phải 4-cửa-MCP; **blocker: máy thiếu gcloud → không provision được từ đây**; spike MCP MOOT).
  **ĐÃ CHỐT mục tiêu (2026-06-24):** correctness/regression = **chạy + mở rộng bộ Playwright/Cypress sẵn có** (CI prod read-only; staging cho E2E có-ghi);
  evidence-screenshot sinh bằng Playwright; 4-cửa-MCP = optional. Đã viết **`20-backlog/staging-runbook.md`** (gcloud provisioning + seed-QUA-APP
  logic-nhất-quán [không SQL thô vì Users/Roles seed trong DatabaseSeeder.cs] + chạy Playwright trên staging) — **user execute ở máy có gcloud**.
  Tất cả UNCOMMITTED (§9 parallel-windows + staging-plan reposition + staging-runbook + STATUS). Kế: commit/push (mọi máy) khi user duyệt.
- **Red-team vòng-5 (verify, UNCOMMITTED):** TỰ BÁC kết luận "suite sẵn có=đủ" (xuống THẤP): suite **rot/hard-skip** ("selector stale/route changed", "seed failed") · **27 test.skip** (skip-if-no-data → **xanh giả**) · **69 file localhost** (bulk cần backend) · seed-via-test **circular**. Khắc phục vào runbook: **§3 validate data-layer (không chỉ schema-drift NR4)** · **§4 seed CHUYÊN DỤNG + seed-verify-gate (NR2/NR3)** · **§5 empirical-1-workflow-trước + đếm skip, SKIP≠PASS (NR1/NR6)**. NR5 (sửa-suite) = backlog fix.
- **EMPIRICAL (2026-06-24, ĐO THẬT — bác bớt round-5):** CI prod-smoke **8/8 success** (21-23/06) · chạy live `e2e-prod/smoke.spec.ts` = **4 passed/0 skip/0 fail** (read-only prod) · audit tĩnh: PW **473 test/10 hard-skip/0 .only**, Cypress **1325/0 skip**. → "suite rot/xanh-giả" (NR1) **CƯỜNG ĐIỆU**; prod-smoke **lành thật**. NHƯNG smoke ≠ correctness (M2 giữ); **pass-rate ~1798 test + logic nghiệp vụ CHƯA đo** (cần backend). Backend local DOWN; provision staging chặn (máy thiếu gcloud).
- **VÒNG-6 — BỎ STAGING + test thẳng prod (2026-06-24, empirical):** prod = no-real-data + đang xây (**deploy 1-3 lần/ngày · 26 commit/3 ngày**) → staging **DEPRIORITIZED** (PHI/pollution moot; schema prod chín > staging fresh). Đo `crud-25groups` (audit serial) prod = **22/23 pass · 1 flaky (opd) · 0 API-err**; 4 nhóm empty-data n/a. **Vehicle ỔN = Playwright serial/CI**, KHÔNG phải **4-cửa-MCP song-song** (bất ổn: prod moving-target + 4-ghi-1-DB + flaky + rot — bỏ-staging KHÔNG fix). **🔴 TRIPWIRE: có data thật/pilot/bán → dừng test-ghi-prod.** Quyết định ghi parallel-windows §9 + staging-plan (DEPRIORITIZED).
- **#192 NotEmptyGuid (phần required-id)** (2026-06-23): tạo `Common/NotEmptyGuidAttribute.cs` + 12 `[NotEmptyGuid]` trên
  required-id non-nullable ở 5 DTO (Payment/Billing/Prescription×2/Reception). Build BE EXIT 0, additive sạch (12 attr+5 using,
  0 dòng logic). An toàn: chỉ reject `Guid.Empty` (đã chắc fail hôm nay → nâng 500/not-found thành 400). **CHỜ user quyết push**
  (cây có việc song song — xem dưới). 6 file tách bạch, `git add` tường minh được.
- ⚠️ **Việc song song CHƯA commit (KHÔNG phải phiên này — nghi Antigravity IDE chạy song song):** #200 CurrentUserAccessor
  (`ICurrentUserAccessor.cs`+`CurrentUserAccessor.cs`+`DependencyInjection.cs` +4 DI) · #215 print-dedup (`utils/printWindow.ts`
  + AnesthesiaPrintTemplates/BirthCertificatePrint/HemodialysisSheetPrint) · ~12 FE (`api/assetManagement|laboratory`,
  9× `pages-v2/*`). **TUYỆT ĐỐI KHÔNG commit/push nhóm này** — chờ user xác nhận.
- 📌 **Đính chính (phiên Claude 2026-06-23):** phần **#200 CurrentUserAccessor** ở dòng trên CHÍNH LÀ việc của
  phiên Claude này (không phải Antigravity) — đã làm **abstraction 1/5**: tạo `ICurrentUserAccessor`
  (`HIS.Application/Common`) + impl `CurrentUserAccessor` (`HIS.Infrastructure/Services`) đọc claim **canonical**
  (NameIdentifier·Name→FullName·Roles) + DI `AddScoped`; adopt **behavior-preserving** (delegate shim) 4 service
  EmrAdmin/EmrManagement/Examination/Reporting (bỏ `_http` dead ở 3, giữ EmrManagement vì còn RemoteIpAddress).
  **Build BE EXIT 0.** Đã **PUSH** commit #200 (KHÔNG `Closes` — task còn 4 abstraction). **Defer (cùng #200):**
  PaginationExtensions→PagedResultDto · ICodeGenerator · AuditLog.WriteAsync · DateRange · adopt RIS(OrAdmin)+~94 controller.
  ⚠️ Phần **#215-print + ~12 FE** ở dòng trên đúng là việc song song của user — phiên này **KHÔNG đụng** (vẫn uncommitted).

## Đã xong gần đây (DONE + PUSHED, origin/main)
- **#291 [audit] CreatedBy create-path advanced** (2026-06-22, CLOSED): luồn `userId` qua 15 Create-method 3 module advanced
  (Community/Forensic/PublicHealth) → `entity.CreatedBy=userId`; 9 file (interface+service+controller). Build BE EXIT 0. Closes #291.
- **#192 [API-1] Range validation request-DTO** (2026-06-22, OPEN-partial): 17 `[Range(0,…)]` chặn money/qty/dose ÂM → auto-400,
  5 DTO (Payment/Billing/Prescription/Reception). Additive, build EXIT 0. **Còn (defer)**: empty-Guid (custom `NonEmptyGuid` attr) +
  DTO money khác (Inpatient/CLS) + smoke neg→400 phiên deploy → GIỮ OPEN.
- **#215 [QA-3] dọn leftover FE antd/print/`:any`** (2026-06-23, **CLOSED**): (1) Space→orientation 4 shared component (`1f4c580`).
  (2) Tách `utils/printWindow.ts` `openPrintWindow()` → migrate 13 file print (8 v2 + 3 component + 2 api) khỏi `window.open+
  document.write`, behavior-preserving. (3) Type 2 `:any` v2 (LabQC/DispensingCounter). Commit **`c5f3352` PUSHED, Closes #215**.
  Build FE EXIT 0. **MOOT #204**: Space/List/print 8 v1 `pages/` (v1 retire). **Defer**: `:any` `_v2kit`(13)+CatalogsAdmin generic (eslint cố ý) · smoke in-thật phiên deploy.
- **#205 [FE-2] tách god-component v2 — SurgeryFormModals** (2026-06-23, **OPEN-partial 1/5**): tách `pages-v2/shared/SurgeryFormModals.tsx`
  (1113 dòng) → folder `surgery-modals/` (1 file/modal + `_shared`) + barrel re-export (importer `Surgery.tsx` KHÔNG đổi); 5 file <500
  (325/315/268/196/55). Pure-move behavior-preserving (giữ useEffect/setTimeout/state, 0 đổi logic). Build FE EXIT 0. Commit **`956509d`
  PUSHED, Refs #205**. **Còn (GIỮ OPEN)**: OpdEditor(1643·68 useState)/Radiology(1461)/Dashboard(1166)/Laboratory(1003) — inline-modal/card, cần smoke phiên deploy.
- **Governance: IN-PROGRESS = CLAIM-FIRST** (2026-06-22): siết ordering — claim NGAY khi chốt task, TRƯỚC pre-flight đo-scope —
  ở `CLAUDE.md` §plan/task + `project-rules.md` §2 bước 3; tách sync-check-nhẹ vs scope-pre-flight-nặng. LINT OK. Gỡ #182 stale.
- **Plugin-routing** (`a3bd364`, 2026-06-22): `.claude/plugins.md` (6 plugin — USE chrome-devtools/playwright MCP ·
  DEFER-to-HIS frontend-design/code-review/github) + touchpoint `skill-routes/fe.md`+`test.md` + REGISTRY owner. LINT OK.
- **Cơ chế "Session Ops" + permission rules** (`f6f2682`, 2026-06-22): `session-ops.md` (mở phiên/model/plan-mode/dọn-context/handoff
  + cheat-sheet 4 permission mode + rule allow/ask/deny) · siết STATUS 447→31 dòng (lịch sử → `90-archive/handoffs/`) ·
  `settings.json` baseline deny/ask/allow (**git commit·push → ASK**, deny đọc secrets) · pointer REGISTRY/SKILL-MAP/README. LINT OK.
- **#195 [PERF-1] AsNoTracking + N+1** — batch 1-4 SAFE (8 file read-only BE), build Infra EXIT 0 (`0d6ba69`/`7198bda`).
  **#195 GIỮ OPEN**: phần còn lại = **N+1 write-path tiền/kho/patient-safety** (Warehouse · BloodBank · PharmacyApproval ·
  Examination/Inpatient Prescriptions · Reception OrdersBilling · Billing) → **DEFER** (cần characterization-test + deploy/smoke).
- **Governance dedup `.claude`** (REGISTRY owner-rows + co-link SKILL-MAP/project-rules) — `15204dc`, LINT OK.
- **#190 [DATA-4]** ngừng nuốt exception tài chính/insurance/signing (41 catch · 9 file) — `454fa82`.
- **#171** tách fat FE api client (barrel re-export, behavior-preserving) — DONE + #171 CLOSED.

## Blocker / chờ user
- **#182** rotate secret (cần quyền Cloud Run) · **#183** role-taxonomy (auth-nhạy-cảm, chờ duyệt phương án) ·
  **#24/#25** credential NCC/R2 · **#22/#113/#133/#134** chờ phần cứng (máy XN / thiết bị).

## Việc kế tiếp
1. **User quyết push #192 NotEmptyGuid** (6 file tách bạch, `git add` tường minh) — đang CHỜ vì cây có việc song song Antigravity.
2. Xác nhận với user nhóm uncommitted #200/#215-print/FE là việc song song của họ → KHÔNG đụng; verify deploy #291/#192-Range đã push.
3. **#195** write-path N+1 (tiền/kho/safety) — làm ở phiên có deploy + smoke (không tự sửa mù).
3. **TEST** (#191/#212/#216-347, label `test`) làm **CUỐI CÙNG** — chỉ sau khi 100% fix/tech-debt DONE. KHÔNG ngoại lệ.
4. **LUÔN** `git fetch` + `git pull --ff-only` + đối chiếu CODE (route/feature/issue đã có chưa) **TRƯỚC** khi pick task (2 máy song song).
